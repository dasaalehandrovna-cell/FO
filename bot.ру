# ============================================
# Финансовый бот (версия 9.6.1.8)
# Минимальные изменения, чтобы:
#  - все кнопки работали в одном окне,
#  - редактирование было исправлено,
#  - при запуске приходили уведомления владельцу.
# ============================================

import os
import json
import csv
import logging
import re
import threading
import time
from datetime import datetime
from zoneinfo import ZoneInfo

import telebot
from telebot import types
from flask import Flask, request

# -----------------------------
# Конфигурация
# -----------------------------
TOKEN = os.getenv("BOT_TOKEN")
OWNER_ID = os.getenv("OWNER_ID")
APP_URL = os.getenv("APP_URL", "https://fo-1.onrender.com")
PORT = int(os.environ.get("PORT", 5000))
TZ = ZoneInfo("America/Argentina/Catamarca")

if not TOKEN:
    raise ValueError("Не установлен BOT_TOKEN")

bot = telebot.TeleBot(TOKEN, threaded=True)
app = Flask(__name__)

DATA_FILE = "data.json"
CSV_FILE = "data.csv"
LOG_FILE = "log.txt"

logging.basicConfig(
    level=logging.INFO,
    format="[{asctime}] {levelname}: {message}",
    style="{",
    handlers=[
        logging.FileHandler(LOG_FILE, encoding="utf-8"),
        logging.StreamHandler()
    ]
)
logger = logging.getLogger(__name__)

# -----------------------------
# Работа с файлами
# -----------------------------
def load_data():
    if os.path.exists(DATA_FILE):
        with open(DATA_FILE, "r", encoding="utf-8") as f:
            return json.load(f)
    return {
        "chats": {},
        "overall_balance": 0,
        "next_id": 1,
        "active_messages": {},
        "tracked_messages": {},
        "records": []
    }

def save_data():
    with open(DATA_FILE, "w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)

data = load_data()

# -----------------------------
# Вспомогательные функции
# -----------------------------
def get_today():
    return datetime.now(TZ).strftime("%Y-%m-%d")

def get_chat(chat_id):
    cid = str(chat_id)
    if cid not in data["chats"]:
        data["chats"][cid] = {
            "balance": 0,
            "records": [],
            "daily_records": {}
        }
        save_data()
    return data["chats"][cid]

def set_active_window(chat_id, msg_id):
    today = get_today()
    data["active_messages"].setdefault(today, {})[str(chat_id)] = msg_id
    save_data()

def get_active_window(chat_id):
    today = get_today()
    return data.get("active_messages", {}).get(today, {}).get(str(chat_id))

# -----------------------------
# Клавиатуры
# -----------------------------
def build_main_keyboard(chat_id=None):
    kb = types.InlineKeyboardMarkup(row_width=3)
    kb.row(
        types.InlineKeyboardButton("💰 баланс", callback_data="btn_balance"),
        types.InlineKeyboardButton("📊 отчёт", callback_data="btn_report"),
        types.InlineKeyboardButton("✏️ изменить", callback_data="btn_edit")
    )
    kb.row(
        types.InlineKeyboardButton("⚙️ обнулить", callback_data="btn_reset"),
        types.InlineKeyboardButton("ℹ️", callback_data="btn_info")
    )
    bal = 0
    if chat_id:
        bal = get_chat(chat_id)["balance"]
    kb.add(types.InlineKeyboardButton(f"💰 {bal} ARS", callback_data="noop"))
    return kb

# -----------------------------
# Обновление окна
# -----------------------------
def update_window(chat_id):
    today = get_today()
    chat = get_chat(chat_id)
    recs = chat["daily_records"].get(today, [])
    text = f"📅 {today}\n"
    if not recs:
        text += "Нет операций за сегодня.\n"
    else:
        for r in recs[-20:]:
            sign = "+" if r["amount"] > 0 else ""
            text += f"{r['id']}: {sign}{r['amount']} — {r['note']}\n"
    text += f"\n💰 Баланс: {chat['balance']} ARS"
    kb = build_main_keyboard(chat_id)

    mid = get_active_window(chat_id)
    if mid:
        try:
            bot.edit_message_text(text, chat_id, mid, reply_markup=kb)
            return
        except Exception as e:
            logger.warning(f"Ошибка редактирования: {e}")

    sent = bot.send_message(chat_id, text, reply_markup=kb)
    set_active_window(chat_id, sent.message_id)

# -----------------------------
# Добавление и обновление записей
# -----------------------------
def add_record(chat_id, amount, note, owner):
    chat = get_chat(chat_id)
    rid = data["next_id"]
    rec = {
        "id": rid,
        "amount": amount,
        "note": note,
        "timestamp": datetime.now(TZ).isoformat(timespec="seconds"),
        "owner": owner
    }
    chat["records"].append(rec)
    chat["balance"] += amount
    today = get_today()
    chat["daily_records"].setdefault(today, []).append(rec)
    data["next_id"] += 1
    save_data()
    update_window(chat_id)
    return rec

def update_record(chat_id, rid, amount, note):
    chat = get_chat(chat_id)
    for r in chat["records"]:
        if r["id"] == rid:
            diff = amount - r["amount"]
            r["amount"] = amount
            r["note"] = note
            chat["balance"] += diff
            save_data()
            update_window(chat_id)
            return True
    return False

# -----------------------------
# Обработка кнопок
# -----------------------------
@bot.callback_query_handler(func=lambda call: True)
def callbacks(call):
    cid = call.message.chat.id
    mid = call.message.message_id
    data_cd = call.data

    if data_cd == "btn_balance":
        chat = get_chat(cid)
        txt = f"💰 Баланс: {chat['balance']} ARS"
        try:
            bot.edit_message_text(txt, cid, mid, reply_markup=build_main_keyboard(cid))
        except:
            bot.send_message(cid, txt, reply_markup=build_main_keyboard(cid))

    elif data_cd == "btn_report":
        today = get_today()
        recs = get_chat(cid)["daily_records"].get(today, [])
        if not recs:
            txt = "📊 За сегодня нет записей."
        else:
            txt = "📊 Сегодня:\n" + "\n".join(
                [f"{r['id']}: {r['amount']} — {r['note']}" for r in recs[-20:]]
            )
        bot.edit_message_text(txt, cid, mid, reply_markup=build_main_keyboard(cid))

    elif data_cd == "btn_edit":
        chat = get_chat(cid)
        if not chat["records"]:
            bot.answer_callback_query(call.id, "Нет записей для редактирования.")
            return
        kb = types.InlineKeyboardMarkup()
        for r in chat["records"][-10:]:
            kb.add(types.InlineKeyboardButton(f"{r['id']}: {r['note']}", callback_data=f"edit_{r['id']}"))
        kb.add(types.InlineKeyboardButton("↩️ Назад", callback_data="back_main"))
        bot.edit_message_text("Выберите запись для редактирования:", cid, mid, reply_markup=kb)

    elif data_cd.startswith("edit_"):
        rid = int(data_cd.split("_")[1])
        data["tracked_messages"][str(cid)] = {"action": "edit_wait", "rid": rid}
        save_data()
        bot.edit_message_text(f"Введите новую сумму и комментарий для записи {rid}:", cid, mid)

    elif data_cd == "btn_reset":
        data["chats"].pop(str(cid), None)
        save_data()
        bot.edit_message_text("Все данные чата обнулены!", cid, mid)

    elif data_cd == "back_main":
        update_window(cid)

    elif data_cd == "btn_info":
        txt = "💬 Финансовый бот версии 9.6.1.8\nВсе операции сохраняются локально."
        bot.edit_message_text(txt, cid, mid, reply_markup=build_main_keyboard(cid))

    else:
        bot.answer_callback_query(call.id)

# -----------------------------
# Сообщения
# -----------------------------
@bot.message_handler(func=lambda m: True)
def messages(m):
    cid = m.chat.id
    uid = m.from_user.id
    txt = m.text.strip()

    # Проверка на режим редактирования
    state = data.get("tracked_messages", {}).get(str(cid))
    if state and state.get("action") == "edit_wait":
        rid = state["rid"]
        match = re.search(r"([+-]?\d+)", txt)
        if not match:
            bot.reply_to(m, "Введите число (например, +200 или -150)")
            return
        amount = int(match.group(1))
        note = txt.replace(match.group(1), "").strip()
        update_record(cid, rid, amount, note)
        data["tracked_messages"].pop(str(cid), None)
        save_data()
        bot.reply_to(m, f"Запись {rid} обновлена ✅")
        return

    # Добавление новой записи
    match = re.search(r"([+-]?\d+)", txt)
    if match:
        amount = int(match.group(1))
        note = txt.replace(match.group(1), "").strip()
        add_record(cid, amount, note, m.from_user.first_name)
        bot.reply_to(m, "Запись добавлена ✅")
    else:
        bot.reply_to(m, "Чтобы добавить запись, напишите сумму (например, +100 продукты)")

# -----------------------------
# Flask webhook
# -----------------------------
@app.route(f"/{TOKEN}", methods=["POST"])
def webhook():
    update = telebot.types.Update.de_json(request.data.decode("utf-8"))
    bot.process_new_updates([update])
    return "OK", 200

@app.route("/", methods=["GET"])
def home():
    return "Финансовый бот работает", 200

# -----------------------------
# Установка webhook
# -----------------------------
def set_webhook():
    url = f"{APP_URL}/{TOKEN}"
    try:
        bot.remove_webhook()
        time.sleep(1)
        bot.set_webhook(url=url)
        logger.info(f"Webhook установлен: {url}")
        if OWNER_ID:
            bot.send_message(int(OWNER_ID), f"🌐 Webhook активирован:\n{url}")
    except Exception as e:
        logger.error(f"Ошибка webhook: {e}")

# -----------------------------
# Планировщик
# -----------------------------
def daily_check():
    last_day = get_today()
    while True:
        time.sleep(60)
        now_day = get_today()
        if now_day != last_day:
            for chat_id in data.get("chats", {}):
                update_window(int(chat_id))
            last_day = now_day

# -----------------------------
# Запуск
# -----------------------------
if __name__ == "__main__":
    set_webhook()
    threading.Thread(target=daily_check, daemon=True).start()
    logger.info("Бот запущен.")
    if OWNER_ID:
        try:
            bot.send_message(int(OWNER_ID), "🤖 Бот успешно запущен!")
        except Exception as e:
            logger.warning(f"Не удалось отправить сообщение владельцу: {e}")
    app.run(host="0.0.0.0", port=PORT)
