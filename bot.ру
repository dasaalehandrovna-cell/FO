#Проверь
#Правильно я вставил , это го достаточно?
#1588💾 фо бот — версия 9.8.6-fo_6+autoCSV
# ID Code_019c
# ============================================
# 🧭 Описание:
#  • Управление личными финансами с разделением по дням
#  • Кнопки навигации /prev /next и выбор даты через календарь
#  • Отчёты, CSV-экспорт, пересчёт баланса и пересылка владельцу
#  • Автосохранение и автозагрузка CSV при перезапуске
#  • Автопинг (keep-alive) и webhook для стабильной работы на Render
# ============================================

import os
import json
import csv
import re
import logging
import threading
import time
import html
from datetime import datetime, timedelta, timezone
from zoneinfo import ZoneInfo

import requests
import telebot
from telebot import types
from flask import Flask, request

# -----------------------------
# ⚙️ Конфигурация бота
# -----------------------------
TOKEN = os.getenv("BOT_TOKEN")
OWNER_ID = os.getenv("OWNER_ID")  # ID владельца для уведомлений
APP_URL = os.getenv("APP_URL", "https://yourapp.onrender.com")
BACKUP_CHAT_ID = os.getenv("BACKUP_CHAT_ID")  # 💾 приватный канал для автосохранения файлов (например: -1003244596041)
# -----------------------------
# 🔧 Настройки логики
# -----------------------------
ALLOW_FILE_WRITE = True  # если False — кнопки не создают новые файлы
# ✅ Часовой пояс с защитой от отсутствия tzdata
try:
    TZ = ZoneInfo((os.getenv("TZ") or "America/Argentina/Buenos_Aires").strip())
except Exception:
    TZ = timezone(timedelta(hours=-3))  # fallback UTC−3

DATA_FILE = "data.json"
CSV_FILE = "data.csv"
LOG_FILE = "log.txt"
PORT = int(os.environ.get("PORT", 5000))
VERSION = "9.8.6-fo_6+autoCSV"

# 🔄 Настройки авто-пинга
KEEP_ALIVE_INTERVAL_SECONDS = int(os.getenv("KEEP_ALIVE_INTERVAL_SECONDS", 11 * 60))
KEEP_ALIVE_SEND_TO_OWNER = True

# ✅ Поведение навигации — независимые окна по датам
DELETE_SOURCE_WINDOW_ON_NAV = False        # исходный день не удаляем
DELETE_TARGET_OLD_WINDOW_ON_NAV = True     # удаляем старое окно выбранной даты
ALWAYS_CREATE_NEW_ON_NAV = True            # создаём новое сообщение

if not TOKEN:
    raise ValueError("BOT_TOKEN не установлен!")

bot = telebot.TeleBot(TOKEN, threaded=True)
app = Flask(__name__)

# -----------------------------
# 🧾 Логирование
# -----------------------------
logging.basicConfig(
    level=logging.INFO,
    format="[{asctime}] {levelname}: {message}",
    style="{",
    handlers=[logging.FileHandler(LOG_FILE, encoding="utf-8"), logging.StreamHandler()]
)

def log_info(msg): logging.info(msg)
def log_error(msg): logging.error(msg)

# ----------------------------------
# 📬 Отправка логов владельцу (в чат)
# ----------------------------------
def log_chat(msg: str, error: bool = False):
    """Отправляет лог владельцу и сохраняет в log.txt"""
    log_func = log_error if error else log_info
    log_func(msg)
    try:
        if OWNER_ID:
            bot.send_message(int(OWNER_ID), f"🪵 {msg}")
    except Exception as e:
        log_error(f"log_chat send failed: {e}")

log_info(f"🚀 Запуск приложения. Версия {VERSION}")


# -----------------------------
# 🕒 Работа с временем и датами
# -----------------------------
def now_local() -> datetime:
    """Возвращает текущее локальное время с учётом TZ"""
    return datetime.now(TZ)
# • • • • • • • • •
def today_key() -> str:
    """Возвращает текущую дату в формате YYYY-MM-DD"""
    return now_local().strftime("%Y-%m-%d")
# • • • • • • • • •
def parse_date_key(s: str):
    """Парсит строку даты 'YYYY-MM-DD' → datetime.date"""
    try:
        return datetime.strptime(s, "%Y-%m-%d").date()
    except Exception:
        return None
# • • • • • • • • •
def shift_date_key(day_key: str, days: int) -> str:
    """Сдвигает дату на указанное количество дней"""
    d = parse_date_key(day_key) or now_local().date()
    return (d + timedelta(days=days)).strftime("%Y-%m-%d")
# • • • • • • • • •
def fmt_num(value):
    """Форматирует число с разделителем точкой"""
    try:
        return f"{value:,.0f}".replace(",", ".")
    except Exception:
        return str(value)

# -----------------------------
# 💾 Структура данных и функции ввода/вывода
# -----------------------------
def default_data():
    """Создаёт базовую структуру данных при первом запуске"""
    return {
        "overall_balance": 0,      # общий баланс всех записей
        "records": [],             # список всех операций
        "chats": {},               # данные по каждому чату
        "active_messages": {},     # активные окна (по дням)
        "processed_messages": [],  # список обработанных сообщений
        "forward_targets": [],     # получатели пересылки
        "tracked_messages": {},    # отслеживаемые сообщения
        "next_id": 1               # следующий ID записи
    }

def _load_json(path, fallback):
    """Загружает JSON с диска, при ошибке — возвращает запасную структуру"""
    try:
        if os.path.exists(path):
            with open(path, "r", encoding="utf-8") as f:
                return json.load(f)
    except Exception as e:
        log_error(f"_load_json: {path}: {e}")
    return fallback

def _save_json(path, payload):
    """Сохраняет JSON-файл на диск"""
    try:
        with open(path, "w", encoding="utf-8") as f:
            json.dump(payload, f, ensure_ascii=False, indent=2)
    except Exception as e:
        log_error(f"_save_json: {path}: {e}")

def load_data():
    """Загружает данные из файла data.json"""
    d = _load_json(DATA_FILE, default_data())
    base = default_data()
    for k, v in base.items():
        if k not in d:
            d[k] = v
    return d

data = load_data()

# -----------------------------
# 📂 Автосохранение и восстановление CSV
# -----------------------------
def restore_from_telegram_backup():
    """🔄 Восстанавливает данные из последнего CSV в чате владельца"""
    try:
        #log_chat("🔄 Старт восстановления данных из Telegram CSV...")
        if not OWNER_ID:
            #log_chat("⚠️ Восстановление пропущено — OWNER_ID не задан.")
            return

        owner_id = int(OWNER_ID)
        log_info("🔍 Поиск последнего CSV в чате владельца...")

        # 🧹 Перед getUpdates удаляем активный webhook (иначе ошибка 409)
        try:
            bot.delete_webhook()
            log_info("🔧 Webhook временно отключён для восстановления CSV.")
            time.sleep(4)  # ⏱ даём Telegram сбросить очередь апдейтов
        except Exception as e:
            log_error(f"delete_webhook перед восстановлением: {e}")

        updates = bot.get_updates(limit=100)
        file_id = None
        for u in reversed(updates):
            m = getattr(u, "message", None)
            if not m or not getattr(m, "document", None):
                continue
            fn = m.document.file_name.lower()
            if "data" in fn and fn.endswith(".csv"):
                file_id = m.document.file_id
                log_info(f"📦 Найден файл бэкапа: {fn}")
                break

        # ♻️ Если файл не найден — пробуем через csv_meta.json
        if not file_id:
            meta = _load_csv_meta()
            file_id = meta.get("file_id_csv")
            if file_id:
                log_info(f"♻️ Используется сохранённый file_id из csv_meta.json: {file_id}")
            else:
                log_info("❕ Файл CSV не найден, используется локальный data.json.")
                return

        file_info = bot.get_file(file_id)
        downloaded = bot.download_file(file_info.file_path)
        tmp = "restore.csv"
        with open(tmp, "wb") as f:
            f.write(downloaded)

        # Преобразуем CSV → JSON-структуру
        import csv
        restored = default_data()
        with open(tmp, "r", encoding="utf-8") as f:
            reader = csv.DictReader(f)
            for row in reader:
                chat_id = str(row["chat_id"])
                rec = {
                    "id": int(row["ID"]),
                    "short_id": row["short_id"],
                    "timestamp": row["timestamp"],
                    "amount": int(row["amount"]),
                    "note": row["note"],
                    "owner": row["owner"]
                }
                restored["records"].append(rec)
                restored["chats"].setdefault(chat_id, {"records": [], "daily_records": {}, "balance": 0})
                restored["chats"][chat_id]["records"].append(rec)
                day = row["day_key"]
                restored["chats"][chat_id]["daily_records"].setdefault(day, []).append(rec)
                restored["chats"][chat_id]["balance"] += rec["amount"]
                restored["overall_balance"] += rec["amount"]
                restored["next_id"] = max(restored["next_id"], rec["id"] + 1)

        _save_json(DATA_FILE, restored)
        log_info("✅ Данные успешно восстановлены из Telegram CSV.")
      #log_chat("✅ Восстановление завершено успешно!")
    except Exception as e:
        log_error(f"Ошибка восстановления из Telegram: {e}")
      #log_chat(f"❌ Ошибка восстановления: {e}", error=True)


# --------------------------------------------
# 💾 Умное сохранение данных (без спама файлами)
# --------------------------------------------
def save_data(d):
    """💾 Сохраняет JSON и обновляет CSV (без отправки Telegram)"""
    try:
        _save_json(DATA_FILE, d)
        export_to_csv(d)
        log_info("💾 Данные сохранены локально (без отправки файлов).")
    except Exception as e:
        log_error(f"save_data: {e}")

# -----------------------------
# 🗃 Хранилище данных чата и окон
# -----------------------------
def get_chat_store(chat_id: int) -> dict:
    """Возвращает/создаёт запись чата со всеми полями по умолчанию"""
    cid = str(chat_id)
    if cid not in data["chats"]:
        data["chats"][cid] = {
            "balance": 0,
            "records": [],
            "next_id": 1,
            "daily_records": {},        # {day_key: [records]}
            "active_windows": {},       # для совместимости со старыми версиями
            "edit_wait": None,
            "edit_target": None,
            "forward_enabled": False,
            "forward_target_id": None,
            "current_view_day": today_key()
        }
        save_data(data)
    store = data["chats"][cid]
    # страховка наличия полей
    store.setdefault("forward_enabled", False)
    store.setdefault("forward_target_id", None)
    store.setdefault("current_view_day", today_key())
    save_data(data)
    return store


def get_active_window_id(chat_id: int, day_key: str):
    """Возвращает message_id активного окна выбранной даты"""
    return data.setdefault("active_messages", {}).setdefault(day_key, {}).get(str(chat_id))


def set_active_window(chat_id: int, day_key: str, message_id: int):
    """Сохраняет связь (chat_id, day_key) → message_id"""
    data.setdefault("active_messages", {}).setdefault(day_key, {})[str(chat_id)] = message_id
    save_data(data)


def clear_active_window(chat_id: int, day_key: str):
    """Очищает сохранённый message_id окна даты"""
    am = data.setdefault("active_messages", {}).setdefault(day_key, {})
    if str(chat_id) in am:
        am.pop(str(chat_id), None)
        save_data(data)


def delete_message_safe(chat_id: int, message_id: int):
    """Безопасное удаление сообщения (игнорируем ошибки Telegram API)"""
    try:
        bot.delete_message(chat_id, message_id)
    except Exception:
        pass


def delete_active_window_if_exists(chat_id: int, day_key: str):
    """Удаляет ранее сохранённое окно выбранной даты (если есть)"""
    old_id = get_active_window_id(chat_id, day_key)
    if old_id:
        delete_message_safe(chat_id, old_id)
        clear_active_window(chat_id, day_key)


def send_temp_message(chat_id: int, text: str, delay: int = 5, **kwargs):
    """Временное сообщение (самоудаление через delay секунд)"""
    try:
        msg = bot.send_message(chat_id, text, **kwargs)
        if delay and delay > 0:
            threading.Timer(delay, lambda: delete_message_safe(chat_id, msg.message_id)).start()
        return msg
    except Exception as e:
        log_error(f"send_temp_message: {e}")
        return None


def set_current_view_day(chat_id: int, day_key: str):
    """Запоминает текущую открытую дату для чата"""
    store = get_chat_store(chat_id)
    store["current_view_day"] = day_key
    save_data(data)


def get_current_view_day(chat_id: int) -> str:
    """Возвращает последнюю «текущую» дату, открытую в чате"""
    store = get_chat_store(chat_id)
    return store.get("current_view_day", today_key())


# -----------------------------
# 📤 Экспорт CSV
# -----------------------------
def export_to_csv(d):
    """Экспортирует все записи во общий CSV (data.csv)"""
    try:
        with open(CSV_FILE, "w", newline='', encoding="utf-8") as f:
            writer = csv.writer(f)
            writer.writerow(["chat_id","ID","short_id","timestamp","amount","note","owner","day_key"])
            for cid, cdata in d.get("chats", {}).items():
                for dk, records in cdata.get("daily_records", {}).items():
                    for r in records:
                        writer.writerow([
                            cid,
                            r.get("id"), r.get("short_id"), r.get("timestamp"),
                            r.get("amount"), r.get("note"), r.get("owner"), dk
                        ])
    except Exception as e:
        log_error(f"export_to_csv: {e}")

# -----------------------------
# ☁️ Автообновление CSV в Telegram (устойчивый бэкап с самовосстановлением)
# -----------------------------
_CSV_BACKUP_META = "csv_meta.json"
_CSV_DEBOUNCE_SECONDS = 3
_csv_backup_timer = None
_csv_backup_lock = threading.Lock()
_datajson_lock = threading.Lock()
_datajson_timer = None
_DATAJSON_DEBOUNCE_SECONDS = 5  # ⏱ защита от спама при множественных вызова

def _load_csv_meta():
    """Загружает метаинформацию о CSV-бэкапе (message_id и др.)"""
    try:
        if os.path.exists(_CSV_BACKUP_META):
            with open(_CSV_BACKUP_META, "r", encoding="utf-8") as f:
                return json.load(f)
    except Exception as e:
        log_error(f"_load_csv_meta: {e}")
    return {}


def _save_csv_meta(meta: dict):
    """Сохраняет метаинформацию о CSV-бэкапе"""
    try:
        with open(_CSV_BACKUP_META, "w", encoding="utf-8") as f:
            json.dump(meta, f, ensure_ascii=False, indent=2)
       # log_chat("💾 Файл csv_meta.json обновлён.")
    except Exception as e:
        log_error(f"_save_csv_meta: {e}")
        #log_chat(f"❌ Ошибка записи csv_meta.json: {e}", error=True)

# --------------------------------------------
# 💾 Автобэкап в приватный канал (как «Избранное»)
# --------------------------------------------
def send_backup_to_channel():
    """📦 Отправляет все актуальные файлы (data.json, data.csv, csv_meta.json) в канал BACKUP_CHAT_ID"""
    if not BACKUP_CHAT_ID:
        log_info("⚠️ BACKUP_CHAT_ID не задан — пропуск отправки бэкапа.")
        return
    try:
        success_count = 0
        for fname in [DATA_FILE, CSV_FILE, _CSV_BACKUP_META]:
            if os.path.exists(fname):
                with open(fname, "rb") as f:
                    sent = bot.send_document(
                        int(BACKUP_CHAT_ID),
                        f,
                        caption=f"🧾 {fname} — авто-бэкап ({datetime.now(TZ).strftime('%Y-%m-%d %H:%M')})"
                    )
                    success_count += 1
                    log_info(f"✅ {fname} успешно отправлен в канал (message_id={sent.message_id})")
            else:
                log_warning(f"⚠️ Файл не найден для бэкапа: {fname}")

        if success_count == 0:
            log_warning("⚠️ Ни один файл не был отправлен — возможно, пути неверны или нет данных.")
        else:
            log_info(f"📤 Автобэкап завершён: {success_count} файлов отправлено.")

    except Exception as e:
        log_error(f"❌ Ошибка при отправке файлов в канал бэкапов: {e}")
        try:
            bot.send_message(
                int(OWNER_ID),
                f"⚠️ Ошибка при отправке файлов в канал бэкапов:\n<code>{e}</code>",
                parse_mode="HTML"
            )
        except Exception as e2:
            log_error(f"Ошибка при уведомлении владельца об ошибке бэкапа: {e2}")

def safe_csv_backup_to_telegram():
    """Отправляет или обновляет CSV-файл в чате владельца, пересоздаёт при удалении"""
    try:
        #log_chat("☁️ Запуск авто-бэкапа CSV в Telegram...")
        if not OWNER_ID:
            #log_chat("⚠️ CSV-бэкап пропущен — OWNER_ID не задан.")
            return

        owner_id = int(OWNER_ID)
        export_to_csv(data)
        meta = _load_csv_meta()
        message_id = meta.get("message_id")

        with open(CSV_FILE, "rb") as f:
            try:
                # 🔁 Попытка обновить старый CSV в Telegram
                if message_id:
                    bot.edit_message_media(
                        chat_id=owner_id,
                        message_id=message_id,
                        media=telebot.types.InputMediaDocument(f, caption="📊 Автообновлённый CSV-бэкап данных")
                    )
                    #log_chat("✅ CSV-бэкап успешно обновлён в Telegram.")
                    log_info("✅ CSV-бэкап успешно обновлён в Telegram.")
                    return

            except telebot.apihelper.ApiTelegramException as e:
                err = str(e).lower()
                log_error(f"CSV edit_message_media error: {err}")
                if "message to edit not found" in err or "not found" in err or "invalid message" in err:
                    log_info("♻️ Старый CSV-файл удалён или недоступен. Создаю новый...")
                else:
                    # Если ошибка не связана с отсутствием файла — пробрасываем дальше
                    raise

            # ♻️ Создаём новый CSV, если старый отсутствует
            try:
                f.seek(0)  # возвращаем указатель в начало файла
                sent = bot.send_document(
                    owner_id, f, caption="📊 Новый CSV-бэкап данных (создан повторно автоматически)"
                )
                meta["message_id"] = sent.message_id
                meta["timestamp"] = datetime.now(TZ).isoformat(timespec="seconds")
                _save_csv_meta(meta)
                #log_chat("♻️ Новый CSV-бэкап создан заново.")
                log_info("✅ CSV-бэкап пересоздан и теперь будет обновляться автоматически.")
            except Exception as e2:
                log_error(f"Ошибка при повторном создании CSV-бэкапа: {e2}")

    except Exception as e:
        log_error(f"safe_csv_backup_to_telegram: {e}")


# --------------------------------------------
# 🧩 Поиск file_id для data.json перед включением webhook
# --------------------------------------------
def find_file_id_message_before_webhook():
    """🔍 Ищет сообщение с текстом 'Актуальный file_id для data.json', сохраняет file_id и выводит найденное сообщение"""
    try:
        if not OWNER_ID:
            log_info("⚠️ OWNER_ID не задан — поиск file_id пропущен.")
            return

        owner_id = int(OWNER_ID)
        log_info("🔍 Поиск сообщения с file_id для data.json...")

        try:
            bot.delete_webhook()
            log_info("🧩 Webhook временно отключён для поиска по истории.")
        except Exception as e:
            log_error(f"delete_webhook error: {e}")

        updates = bot.get_updates(limit=200)
        found_fid = None
        found_text = None

        for u in reversed(updates):
            m = getattr(u, "message", None)
            if not m or not getattr(m, "text", None):
                continue
            text = m.text.strip()
            if "Актуальный file_id для data.json" in text:
                found_text = text
                parts = text.split("\n")
                if len(parts) > 1:
                    fid_candidate = parts[1].strip()
                    if fid_candidate and fid_candidate.startswith("BQAC"):
                        found_fid = fid_candidate
                        break

        if found_fid:
            meta = _load_csv_meta()
            meta["file_id_datajson"] = found_fid
            meta["timestamp_datajson"] = datetime.now(TZ).isoformat(timespec="seconds")
            _save_csv_meta(meta)
            log_info(f"🪄 Найден file_id для data.json: {found_fid}")
            bot.send_message(
                owner_id,
                f"✅ Найдено сообщение с file_id:\n\n<code>{found_text}</code>",
                parse_mode="HTML"
            )
        else:
            log_info("⚠️ Сообщение с file_id для data.json не найдено.")
            bot.send_message(owner_id, "⚠️ Сообщение с file_id для data.json не найдено.")

    except Exception as e:
        log_error(f"find_file_id_message_before_webhook error: {e}")
        bot.send_message(owner_id, f"❌ Ошибка при поиске file_id:\n{e}")
    finally:
        try:
            bot.set_webhook(WEBHOOK_URL)
            log_info("🌐 Webhook снова включён.")
        except Exception as e:
            log_error(f"Ошибка при включении webhook: {e}")

# --------------------------------------------
# 🧩 Поиск первого сообщения в чате владельца (для диагностики)
# --------------------------------------------
def find_first_message_in_chat_before_webhook():
    """🔍 Ищет первое сообщение в чате владельца и выводит его в лог и чат"""
    try:
        if not OWNER_ID:
            log_info("⚠️ OWNER_ID не задан — поиск первого сообщения пропущен.")
            return

        owner_id = int(OWNER_ID)
        log_info("🔍 Поиск первого сообщения в чате владельца...")

        # 1️⃣ Временно выключаем webhook
        try:
            bot.delete_webhook()
            log_info("🧩 Webhook временно отключён для поиска первого сообщения.")
        except Exception as e:
            log_error(f"delete_webhook error: {e}")

        # 2️⃣ Получаем апдейты
        updates = bot.get_updates(limit=200)
        if not updates:
            log_info("⚠️ Нет доступных сообщений в истории (get_updates пуст).")
            bot.send_message(owner_id, "⚠️ Нет доступных сообщений в истории (get_updates пуст).")
            return

        # Берём самое первое по времени
        first_msg = None
        for u in updates:
            m = getattr(u, "message", None)
            if not m or not getattr(m, "text", None):
                continue
            if m.chat and m.chat.id == owner_id:
                first_msg = m
                break

        if not first_msg:
            log_info("⚠️ В истории нет сообщений владельца.")
            bot.send_message(owner_id, "⚠️ В истории нет сообщений владельца.")
        else:
            log_info(f"📜 Первое сообщение в чате владельца: {first_msg.text}")
            bot.send_message(
                owner_id,
                f"📜 *Первое сообщение в чате владельца:*\n\n{first_msg.text}",
                parse_mode="Markdown"
            )

    except Exception as e:
        log_error(f"find_first_message_in_chat_before_webhook error: {e}")
        bot.send_message(int(OWNER_ID), f"❌ Ошибка при поиске первого сообщения:\n{e}")
    finally:
        # 3️⃣ Включаем webhook обратно
        try:
            bot.set_webhook(WEBHOOK_URL)
            log_info("🌐 Webhook снова включён (после поиска первого сообщения).")
        except Exception as e:
            log_error(f"Ошибка при включении webhook: {e}")

# # --------------------------------------------
# 💾 Умная отправка файла data.json владельцу
#   • защита от спама
#   • автоповтор при сбоях
#   • удаление старых сообщений и файлов
# --------------------------------------------
_datajson_lock = threading.Lock()
_datajson_timer = None
_DATAJSON_DEBOUNCE_SECONDS = 5

def send_data_json_to_telegram():
    """Планирует безопасную отправку data.json (антиспам)"""
    global _datajson_timer
    with _datajson_lock:
        if _datajson_timer and _datajson_timer.is_alive():
            _datajson_timer.cancel()
        _datajson_timer = threading.Timer(_DATAJSON_DEBOUNCE_SECONDS, _send_data_json_actual)
        _datajson_timer.daemon = True
        _datajson_timer.start()

def _send_data_json_actual():
    """Отправляет data.json, удаляя старое сообщение и публикуя file_id (HTML)."""
    try:
        if not OWNER_ID:
            log_chat("⚠️ Пропущена отправка data.json — OWNER_ID не задан.")
            return
        if not os.path.exists(DATA_FILE):
            log_chat("⚠️ Файл data.json не найден для отправки.")
            return

        owner_id = int(OWNER_ID)
        meta = _load_csv_meta()

        # 🗑 Удаляем старые сообщения, если есть
        old_msg_id = meta.get("message_id_datajson")
        old_msg_id_fid = meta.get("message_id_datajson_fid")
        try:
            if old_msg_id:
                bot.delete_message(owner_id, old_msg_id)
            if old_msg_id_fid:
                bot.delete_message(owner_id, old_msg_id_fid)
        except Exception as e_del:
            log_error(f"Удаление старых сообщений data.json: {e_del}")

        # 📤 Отправляем data.json (до 3 попыток)
        sent = None
        for attempt in range(1, 4):
            try:
                with open(DATA_FILE, "rb") as f:
                    sent = bot.send_document(
                        owner_id,
                        f,
                        caption="🧾 Текущий файл data.json (локальные данные)"
                    )
                log_info(f"📤 data.json успешно отправлен (попытка {attempt})")
                break
            except Exception as e_send:
                log_error(f"⚠️ Ошибка при отправке data.json (попытка {attempt}): {e_send}")
                if attempt < 3:
                    time.sleep(2)

        if not sent:
            log_chat("❌ Не удалось отправить data.json после 3 попыток.", error=True)
            return

        # 💾 Сохраняем мета и публикуем file_id через HTML
        if getattr(sent, "document", None) and getattr(sent.document, "file_id", None):
            fid = sent.document.file_id
            meta["file_id_datajson"] = fid
            meta["message_id_datajson"] = sent.message_id
            meta["timestamp_datajson"] = datetime.now(TZ).isoformat(timespec="seconds")
            _save_csv_meta(meta)

            fid_msg = bot.send_message(
                owner_id,
                f"🪄 Актуальный file_id для data.json:\n<code>{html.escape(fid)}</code>",
                parse_mode="HTML"
            )
            meta["message_id_datajson_fid"] = fid_msg.message_id
            _save_csv_meta(meta)
            log_info(f"💾 file_id для data.json сохранён и отправлен: {fid}")

    except Exception as e:
        log_chat(f"❌ Ошибка при отправке data.json: {e}", error=True)
        log_error(f"_send_data_json_actual: {e}")# 💾
# --------------------------------------------
#⬇️⬇️⬇️⬇️⬇️⬇️
def export_day_csv(chat_id: int, day_key: str):
    """Экспортирует записи конкретного дня в отдельный CSV data_YYYY-MM-DD.csv"""
    store = get_chat_store(chat_id)
    day_records = store.get("daily_records", {}).get(day_key, [])
    filename = f"data_{day_key}.csv"
    try:
        with open(filename, "w", newline='', encoding="utf-8") as f:
            writer = csv.writer(f)
            writer.writerow(["chat_id","ID","short_id","timestamp","amount","note","owner","day_key"])
            for r in day_records:
                writer.writerow([
                    chat_id,
                    r.get("id"), r.get("short_id"), r.get("timestamp"),
                    r.get("amount"), r.get("note"), r.get("owner"), day_key
                ])
    except Exception as e:
        log_error(f"export_day_csv: {e}")
        return None
    return filename


# -----------------------------
# ➕/✏️/🗑 Операции с записями
# -----------------------------
def add_record_to_chat(chat_id: int, amount: int, note: str, owner):
    """Добавляет новую запись, обновляет балансы, сохраняет и обновляет CSV"""
    store = get_chat_store(chat_id)
    rid = data.get("next_id", 1)
    rec = {
        "id": rid,
        "short_id": f"R{rid}",
        "timestamp": now_local().isoformat(timespec="seconds"),
        "amount": amount,
        "note": note,
        "owner": owner
    }
    data.setdefault("records", []).append(rec)
    store.setdefault("records", []).append(rec)
    day = today_key()
    store.setdefault("daily_records", {}).setdefault(day, []).append(rec)

        # обновляем балансы
    store["balance"] = store.get("balance", 0) + amount
    data["overall_balance"] = data.get("overall_balance", 0) + amount
    data["next_id"] = rid + 1

    # 💾 сохраняем данные и отправляем обновлённые файлы владельцу
    global ALLOW_FILE_WRITE
    ALLOW_FILE_WRITE = True
    save_data(data)
    safe_csv_backup_to_telegram()
    send_data_json_to_telegram()
    send_backup_to_channel()
    ALLOW_FILE_WRITE = False

    #log_chat(f"➕ Добавлена запись: {amount} ({note}) — чат {chat_id}")
    return rec


def update_record_in_chat(chat_id: int, rid: int, new_amount: int, new_note: str, user=None):
    """Изменяет сумму/заметку записи с указанным ID"""
    store = get_chat_store(chat_id)
    found = None
    for r in store.get("records", []):
        if r["id"] == rid:
            r["amount"] = new_amount
            r["note"] = new_note
            found = r
            break

    if found:
        # синхронизация дневных срезов
        for day_recs in store.get("daily_records", {}).values():
            for r in day_recs:
                if r["id"] == rid:
                    r.update(found)

        # пересчёт балансов
        store["balance"] = sum(x["amount"] for x in store.get("records", []))
        data["records"] = [x if x["id"] != rid else found for x in data.get("records", [])]
        data["overall_balance"] = sum(x["amount"] for x in data.get("records", []))

        
        # 💾 сохраняем и отправляем обновлённые файлы владельцу
        global ALLOW_FILE_WRITE
        ALLOW_FILE_WRITE = True
        save_data(data)
        safe_csv_backup_to_telegram()
        send_data_json_to_telegram()
        send_backup_to_channel()
        ALLOW_FILE_WRITE = False
        
        return True, found
    return False, None


def delete_record_in_chat(chat_id: int, rid: int, user=None):
    """Удаляет запись и пересчитывает балансы"""
    store = get_chat_store(chat_id)
    removed = None
    for r in list(store.get("records", [])):
        if r["id"] == rid:
            removed = r
            store["records"].remove(r)
            break

    if removed:
        # убрать запись из всех дневных списков
        for dk, recs in store.get("daily_records", {}).items():
            store["daily_records"][dk] = [x for x in recs if x["id"] != rid]

        # пересчитать балансы
        store["balance"] = sum(x["amount"] for x in store.get("records", []))
        data["records"] = [x for x in data.get("records", []) if x["id"] != rid]
        data["overall_balance"] = sum(x["amount"] for x in data.get("records", []))

      
        # 💾 сохраняем и отправляем обновлённые файлы владельцу
        global ALLOW_FILE_WRITE
        ALLOW_FILE_WRITE = True
        save_data(data)
        safe_csv_backup_to_telegram()
        send_data_json_to_telegram()
        ALLOW_FILE_WRITE = False
        return True, removed
    return False, None


# -----------------------------
# ⌨️ Клавиатуры интерфейса
# -----------------------------
def build_main_keyboard(day_key: str, chat_id: int | None = None):
    """Главное окно дня — компактное меню из 5 кнопок + общий остаток"""
    kb = types.InlineKeyboardMarkup(row_width=2)
    kb.row(
        types.InlineKeyboardButton("💰 Баланс", callback_data=f"d:{day_key}:balance"),
        types.InlineKeyboardButton("📊 Отчёт", callback_data=f"d:{day_key}:report"),
    )
    kb.row(
        types.InlineKeyboardButton("🔄 Обновить", callback_data=f"d:{day_key}:refresh"),
        types.InlineKeyboardButton("✏️ Редактировать", callback_data=f"d:{day_key}:edit_menu"),
    )
    total = fmt_num(data.get("overall_balance", 0))
    kb.add(types.InlineKeyboardButton(f"💰 Общий остаток: {total} ARS", callback_data=f"d:{day_key}:total"))
    return kb


def build_edit_menu_keyboard(day_key: str, chat_id=None):
    """Меню редактирования — расширенные инструменты"""
    kb = types.InlineKeyboardMarkup(row_width=2)
    kb.row(
        types.InlineKeyboardButton("📝 Редактировать запись", callback_data=f"d:{day_key}:edit_list"),
        types.InlineKeyboardButton("📂 Общий CSV", callback_data=f"d:{day_key}:csv_all")
    )
    kb.row(
        types.InlineKeyboardButton("📅 CSV за день", callback_data=f"d:{day_key}:csv_day"),
        types.InlineKeyboardButton("📨 Форвард", callback_data=f"d:{day_key}:toggle_forward")
    )
    kb.row(
        types.InlineKeyboardButton("👤 Получатель", callback_data=f"d:{day_key}:set_forward_target"),
        types.InlineKeyboardButton("⚙️ Обнулить", callback_data=f"d:{day_key}:reset")
    )
    kb.row(
        types.InlineKeyboardButton("📅 Сегодня", callback_data=f"d:{today_key()}:open"),
        types.InlineKeyboardButton("📆 Выбрать день", callback_data=f"d:{day_key}:pick_date")
    )
    kb.row(
        types.InlineKeyboardButton("ℹ️ Инфо", callback_data=f"d:{day_key}:info"),
        types.InlineKeyboardButton("🔙 Назад", callback_data=f"d:{day_key}:back_main")
    )
    return kb


def build_edit_keyboard_for_record(day_key: str, rid: int):
    """Клавиатура редактирования конкретной записи"""
    kb = types.InlineKeyboardMarkup(row_width=1)
    kb.add(types.InlineKeyboardButton("✏ Ввести вручную", callback_data=f"d:{day_key}:edit_manual:{rid}"))
    kb.add(types.InlineKeyboardButton("🗑 Удалить", callback_data=f"d:{day_key}:edit_delete:{rid}"))
    kb.add(types.InlineKeyboardButton("🔙 Назад", callback_data=f"d:{day_key}:edit_back"))
    return kb


def build_confirm_delete_keyboard(day_key: str, rid: int):
    """Подтверждение удаления записи"""
    kb = types.InlineKeyboardMarkup(row_width=2)
    kb.row(
        types.InlineKeyboardButton("✅ Да, удалить", callback_data=f"d:{day_key}:confirm_delete:{rid}"),
        types.InlineKeyboardButton("❌ Отмена", callback_data=f"d:{day_key}:cancel_delete")
    )
    return kb


def build_reset_confirm_keyboard(day_key: str):
    """Подтверждение обнуления всех данных чата"""
    kb = types.InlineKeyboardMarkup(row_width=2)
    kb.row(
        types.InlineKeyboardButton("✅ Да, обнулить", callback_data=f"d:{day_key}:confirm_reset_all"),
        types.InlineKeyboardButton("❌ Отмена", callback_data=f"d:{day_key}:cancel_reset_all")
    )
    return kb


# -----------------------------
# 🗓 Календарь «последние 31 день» (закрывается после выбора)
# -----------------------------
def build_last_31days_calendar():
    """Возвращает inline-календарь с последними 31 днём"""
    today = now_local().date()
    days = [today - timedelta(days=i) for i in range(0, 31)]
    kb = types.InlineKeyboardMarkup()
    row = []
    for idx, d in enumerate(days, start=1):
        label = d.strftime("%d/%m")
        dk = d.strftime("%Y-%m-%d")
        row.append(types.InlineKeyboardButton(text=label, callback_data=f"d:{dk}:calopen"))
        if idx % 7 == 0:
            kb.row(*row); row = []
    if row:
        kb.row(*row)
    return kb


# -----------------------------
# 🖼 Рендер окна дня (текст)
# -----------------------------
def render_day_text(store: dict, day_key: str) -> str:
    """Формирует текст состояния выбранного дня"""
    day_records = store.get("daily_records", {}).get(day_key, [])
    daily_income = sum(r["amount"] for r in day_records if r["amount"] > 0)
    daily_expense = sum(r["amount"] for r in day_records if r["amount"] < 0)
    overall_balance = store.get("balance", 0)

    if not day_records:
        text = (
            f"📅 {day_key}\n"
            f"Пока нет операций в этот день.\n"
            f"💵 Приход: {fmt_num(daily_income)} ARS\n"
            f"💸 Расход: {fmt_num(abs(daily_expense))} ARS\n"
            f"💰 Общий остаток: {fmt_num(overall_balance)} ARS"
        )
    else:
        lines = [f"📅 {day_key}", "📋 Операции:"]
        for r in day_records[-50:]:
            sign = "+" if r["amount"] > 0 else "-"
            lines.append(f"{r['short_id']}: {sign}{fmt_num(abs(r['amount']))} — {r.get('note','')}")
        lines.append(
            f"\n💵 Приход: {fmt_num(daily_income)} ARS\n"
            f"💸 Расход: {fmt_num(abs(daily_expense))} ARS\n"
            f"💰 Общий остаток: {fmt_num(overall_balance)} ARS"
        )
        text = "\n".join(lines)

    try:
        if store.get("forward_enabled"):
            tgt = store.get("forward_target_id") or OWNER_ID
            text += f"\n\n📨 Пересылка: Включена → {tgt}"
        else:
            text += f"\n\n📨 Пересылка: Выключена"
    except:
        pass
    return text


# -----------------------------
# 🔁 Обновление/создание окна дня
# -----------------------------
def update_or_send_day_window(
    chat_id: int,
    day_key: str = None,
    create_if_missing: bool = True,
    force_new: bool = False,
    delete_target_old: bool = False,
    delete_source_old: bool = False,
    source_day_key: str | None = None
):
    """Обновляет существующее окно или создаёт новое для выбранной даты"""
    day_key = day_key or today_key()
    store = get_chat_store(chat_id)

    if delete_target_old:
        delete_active_window_if_exists(chat_id, day_key)
    if delete_source_old and source_day_key and source_day_key != day_key:
        delete_active_window_if_exists(chat_id, source_day_key)

    set_current_view_day(chat_id, day_key)

    text = render_day_text(store, day_key)
    kb = build_main_keyboard(day_key, chat_id)
    active_id = get_active_window_id(chat_id, day_key)

    if not force_new and active_id:
        try:
            bot.edit_message_text(text, chat_id, active_id, reply_markup=kb)
            return
        except telebot.apihelper.ApiException as e:
            log_error(f"edit_message_text: {e}")

    if not create_if_missing and not force_new:
        return

    try:
        sent = bot.send_message(chat_id, text, reply_markup=kb)
        set_active_window(chat_id, day_key, sent.message_id)
    except Exception as e:
        log_error(f"send new window: {e}")


# -----------------------------
# 🧲 Callback-обработчик (часть 1)
# -----------------------------
@bot.callback_query_handler(func=lambda c: True)
def handle_callback(call):
    """Обрабатывает все нажатия inline-кнопок"""
    try:
        chat_id = call.message.chat.id
    except Exception:
        return

    data_str = call.data or ""
    if not data_str.startswith("d:"):
        update_or_send_day_window(chat_id, today_key())
        bot.answer_callback_query(call.id)
        return

    parts = data_str.split(":")
    if len(parts) < 3:
        bot.answer_callback_query(call.id)
        return

    day_key = parts[1]
    action_tail = parts[2:]
    action = action_tail[0] if action_tail else ""
    store = get_chat_store(chat_id)
    prev_day = get_current_view_day(chat_id)
    set_current_view_day(chat_id, day_key)

    # --- Календарь: открыть выбранную дату ---
    if action == "calopen":
        update_or_send_day_window(
            chat_id,
            day_key,
            create_if_missing=True,
            force_new=ALWAYS_CREATE_NEW_ON_NAV,
            delete_target_old=DELETE_TARGET_OLD_WINDOW_ON_NAV,
            delete_source_old=True,
            source_day_key=prev_day
        )
        bot.answer_callback_query(call.id, f"Открыт день {day_key}")
        return

    # --- Открыть обычный день (кнопки Сегодня/дата) ---
    if action == "open":
        update_or_send_day_window(
            chat_id,
            day_key,
            create_if_missing=True,
            force_new=ALWAYS_CREATE_NEW_ON_NAV,
            delete_target_old=DELETE_TARGET_OLD_WINDOW_ON_NAV,
            delete_source_old=DELETE_SOURCE_WINDOW_ON_NAV,
            source_day_key=prev_day
        )
        bot.answer_callback_query(call.id, f"Открыт день {day_key}")
        return

    # --- Показать календарь последних 31 дня ---
    if action == "pick_date":
        kb = build_last_31days_calendar()
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text("🗓 Выберите дату (последние 31 день):", chat_id, active_id, reply_markup=kb)
            except telebot.apihelper.ApiException as e:
                log_error(f"calendar edit: {e}")
        else:
            update_or_send_day_window(chat_id, day_key)
        bot.answer_callback_query(call.id)
        return

    # --- Меню редактирования ---
    if action == "edit_menu":
        text = (
            "✏️ Меню редактирования\n\n"
            "📝 Редактировать запись — изменить/удалить операцию\n"
            "📂 Общий CSV — полный экспорт\n"
            "📅 CSV за день — экспорт выбранной даты\n"
            "📨 Форвард — включить/выключить пересылку\n"
            "👤 Получатель — задать ID для пересылки\n"
            "⚙️ Обнулить — очистить все данные чата\n\n"
            "📅 Сегодня / 📆 Выбрать день / ℹ️ Инфо / 🔙 Назад"
        )
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=build_edit_menu_keyboard(day_key, chat_id))
            except telebot.apihelper.ApiException as e:
                log_error(f"edit_menu edit: {e}")
        else:
            update_or_send_day_window(chat_id, day_key, create_if_missing=True)
        bot.answer_callback_query(call.id, "Меню редактирования ✏️")
        return

    # --- Обновить текущее окно ---
    if action == "refresh":
        active_id = get_active_window_id(chat_id, day_key)
        text = render_day_text(store, day_key)
        if active_id:
            try:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=build_main_keyboard(day_key, chat_id))
            except telebot.apihelper.ApiException as e:
                log_error(f"refresh edit: {e}")
        else:
            update_or_send_day_window(chat_id, day_key, create_if_missing=True)
        bot.answer_callback_query(call.id, "Обновлено 🔄")
        return

    # --- Инфо ---
    if action == "info":
        info_text = (
            f"ℹ️ Финансовый бот — версия {VERSION}\n\n"
            "📌 Команды:\n"
            "/start — открыть окно за сегодня\n"
            "/view YYYY-MM-DD — открыть день\n"
            "/prev /next — навигация\n"
            "/balance — остаток за день\n"
            "/report — список расходов\n"
            "/csv — общий CSV\n"
            "/reset — обнуление чата\n"
            "/ping — проверка активности\n"
            "/json \n"
            "/file_id \n"
             "/file_id_all \n\n"
            
            "📝 Добавление записей: +500 Зарплата или -150 Обед\n"
            "⚙️ Системно: CSV, форвард, webhook, keep-alive."
        )
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(info_text, chat_id, active_id, reply_markup=build_main_keyboard(day_key, chat_id))
            except telebot.apihelper.ApiException as e:
                log_error(f"info edit: {e}")
        else:
            update_or_send_day_window(chat_id, day_key)
        bot.answer_callback_query(call.id)
        return

    # --- Баланс за день ---
    if action == "balance":
        day_records = store.get("daily_records", {}).get(day_key, [])
        income = sum(r["amount"] for r in day_records if r["amount"] > 0)
        expense = sum(r["amount"] for r in day_records if r["amount"] < 0)
        overall = store.get("balance", 0)
        text = (
            f"📅 {day_key}\n\n"
            f"💵 Приход за день: {fmt_num(income)} ARS\n"
            f"💸 Расход за день: {fmt_num(abs(expense))} ARS\n"
            f"💰 Остаток: {fmt_num(overall)} ARS"
        )
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=build_main_keyboard(day_key, chat_id))
            except telebot.apihelper.ApiException as e:
                log_error(f"balance edit: {e}")
        else:
            update_or_send_day_window(chat_id, day_key)
        bot.answer_callback_query(call.id, "Баланс обновлён ✅")
        return

    # --- Общий итог за всё время ---
    if action == "total":
        all_records = data.get("records", [])
        total = data.get("overall_balance", 0)
        total_income = sum(r["amount"] for r in all_records if r["amount"] > 0)
        total_expense = sum(r["amount"] for r in all_records if r["amount"] < 0)
        text = (
            f"📊 Общие данные за всё время:\n\n"
            f"💵 Общий приход: {fmt_num(total_income)} ARS\n"
            f"💸 Общий расход: {fmt_num(abs(total_expense))} ARS\n"
            f"💰 Общий остаток: {fmt_num(total)} ARS"
        )
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=build_main_keyboard(day_key, chat_id))
            except telebot.apihelper.ApiException as e:
                log_error(f"total edit: {e}")
        else:
            update_or_send_day_window(chat_id, day_key)
        bot.answer_callback_query(call.id, "Общий итог 📊")
        return

    # --- Отчёт (расходы за день) ---
    if action == "report":
        day_records = store.get("daily_records", {}).get(day_key, [])
        expenses = [r for r in day_records if r["amount"] < 0]
        daily_expense_total = sum(abs(r["amount"]) for r in expenses)
        if not expenses:
            text = f"📅 {day_key}\nПока нет расходов за этот день.\n💸 Расход: 0 ARS"
        else:
            lines = [f"📅 {day_key}", "📋 Расходы за день:"]
            for r in expenses[-30:]:
                lines.append(f"{r['short_id']}: -{fmt_num(abs(r['amount']))} — {r.get('note','')}")
            lines.append(f"\n💸 Расход за день: {fmt_num(daily_expense_total)} ARS")
            text = "\n".join(lines)
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=build_main_keyboard(day_key, chat_id))
            except telebot.apihelper.ApiException as e:
                log_error(f"report edit: {e}")
        else:
            update_or_send_day_window(chat_id, day_key)
        bot.answer_callback_query(call.id, "Отчёт обновлён 📊")
        return

        # --- CSV: общий и дневной ---
    if action == "csv_all":
        try:
            export_to_csv(data)
            with open(CSV_FILE, "rb") as f:
                sent = bot.send_document(chat_id, f, caption="📂 Полный экспорт: data.csv")

            # 🧩 Если файл выслан владельцу — считаем это новым бэкапом
            if OWNER_ID and chat_id == int(OWNER_ID):
                meta = {
                    "message_id": sent.message_id,
                    "timestamp": datetime.now(TZ).isoformat(timespec="seconds")
                }
                _save_csv_meta(meta)
                log_info("✅ Новый CSV назначен как активный Telegram-бэкап.")

            bot.answer_callback_query(call.id, "CSV отправлен 📁")

        except Exception as e:
            send_temp_message(chat_id, f"❌ Ошибка при отправке CSV: {e}")
            bot.answer_callback_query(call.id)
        return

    if action == "csv_day":
        filename = export_day_csv(chat_id, day_key)
        if not filename:
            send_temp_message(chat_id, "❌ Не удалось подготовить CSV за день.")
            bot.answer_callback_query(call.id); return
        try:
            with open(filename, "rb") as f:
                bot.send_document(chat_id, f, caption=f"📅 CSV за день: {filename}")
            bot.answer_callback_query(call.id, f"CSV за день {day_key} отправлен")
        except Exception as e:
            send_temp_message(chat_id, f"❌ Ошибка при отправке {filename}: {e}")
            bot.answer_callback_query(call.id)
        return
    # --- Пересылка: вкл/выкл ---
    if action == "toggle_forward":
        store["forward_enabled"] = not store.get("forward_enabled", False)
        save_data(data)
        status = "включена ✅" if store["forward_enabled"] else "выключена ❌"
        update_or_send_day_window(chat_id, day_key)
        bot.answer_callback_query(call.id, f"Пересылка {status}")
        return

    # --- Пересылка: задать получателя ---
    if action == "set_forward_target":
        try:
            bot.answer_callback_query(call.id)
            sent = send_temp_message(chat_id, "Введите ID (можно отрицательный для чатов) или 0 для сброса:", delay=25)
            if sent:
                bot.register_next_step_handler(sent, lambda m: process_forward_target(m, day_key))
        except Exception as e:
            log_error(f"set_forward_target: {e}")
            bot.answer_callback_query(call.id, "Не удалось запросить ID получателя.")
        return

    # --- Сброс (подсказка) ---
    if action == "reset":
        update_or_send_day_window(chat_id, day_key)
        bot.answer_callback_query(call.id, "Для обнуления используйте команду /reset")
        return

    # --- Стандартные возвраты в главное окно ---
    if action in ("back_main", "edit_back", "noop", "info_ok"):
        update_or_send_day_window(chat_id, day_key)
        bot.answer_callback_query(call.id)
        return

    # --- Редактирование: список записей ---
    if action == "edit_list":
        day_records = store.get("daily_records", {}).get(day_key, [])
        if not day_records:
            text = f"📅 {day_key}\nПока нет операций в этот день."
            active_id = get_active_window_id(chat_id, day_key)
            if active_id:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=build_edit_menu_keyboard(day_key, chat_id))
            bot.answer_callback_query(call.id, "Нет записей для редактирования")
            return

        lines = [f"📅 {day_key}", "Выберите запись для редактирования:"]
        kb = types.InlineKeyboardMarkup(row_width=1)
        for r in reversed(day_records[-20:]):
            sign = "+" if r["amount"] > 0 else "-"
            label = f"{r['short_id']}: {sign}{fmt_num(abs(r['amount']))} — {r.get('note','')}"
            kb.add(types.InlineKeyboardButton(label, callback_data=f"d:{day_key}:rec:{r['id']}"))
        kb.add(types.InlineKeyboardButton("🔙 Назад", callback_data=f"d:{day_key}:back_main"))

        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text("\n".join(lines), chat_id, active_id, reply_markup=kb)
            except telebot.apihelper.ApiException as e:
                log_error(f"Ошибка показа списка редактирования: {e}")
        bot.answer_callback_query(call.id)
        return

    # --- Открыть запись ---
    if action == "rec" and len(action_tail) >= 2:
        try:
            rid = int(action_tail[1])
        except:
            bot.answer_callback_query(call.id, "Неверный ID"); return

        day_records = store.get("daily_records", {}).get(day_key, [])
        rec = next((r for r in day_records if r["id"] == rid), None)
        if not rec:
            bot.answer_callback_query(call.id, "Запись не найдена"); return

        sign = "+" if rec["amount"] > 0 else "-"
        text = f"Запись {rec['short_id']}\n{sign}{fmt_num(abs(rec['amount']))} — {rec.get('note','')}\nДобавлена: {rec.get('timestamp')}"
        kb = build_edit_keyboard_for_record(day_key, rid)

        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=kb)
            except telebot.apihelper.ApiException as e:
                log_error(f"Ошибка показа детали записи: {e}")
        bot.answer_callback_query(call.id)
        return

    # --- Редактирование вручную (в том же окне) ---
    if action == "edit_manual" and len(action_tail) >= 2:
        try:
            rid = int(action_tail[1])
        except:
            bot.answer_callback_query(call.id, "Неверный ID"); return

        store["edit_wait"] = f"change_value:{day_key}"
        store["edit_target"] = rid
        save_data(data)

        text = (
            f"✏️ Редактирование записи R{rid}\n\n"
            "Введите новую сумму и заметку:\n"
            "+500 Зарплата  или  ^+^500^ Зарплата  или  ^-^150^ Обед"
        )
        kb = types.InlineKeyboardMarkup(row_width=1)
        kb.add(types.InlineKeyboardButton("🔙 Назад", callback_data=f"d:{day_key}:edit_list"))

        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=kb)
            except telebot.apihelper.ApiException as e:
                log_error(f"edit_manual edit: {e}")
        else:
            update_or_send_day_window(chat_id, day_key, create_if_missing=True)

        bot.answer_callback_query(call.id, "Ожидание новой суммы ✏️")
        return

    # --- Удаление записи (подтверждение) ---
    if action == "edit_delete" and len(action_tail) >= 2:
        try:
            rid = int(action_tail[1])
        except:
            bot.answer_callback_query(call.id, "Неверный ID"); return

        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(
                    f"❗ Подтвердите удаление записи R{rid}:",
                    chat_id,
                    active_id,
                    reply_markup=build_confirm_delete_keyboard(day_key, rid)
                )
            except telebot.apihelper.ApiException as e:
                log_error(f"edit_delete confirm edit: {e}")
        bot.answer_callback_query(call.id)
        return

    # --- Удаление записи (выполнение) ---
    if action == "confirm_delete" and len(action_tail) >= 2:
        try:
            rid = int(action_tail[1])
        except:
            bot.answer_callback_query(call.id, "Неверный ID"); return

        success, _ = delete_record_in_chat(chat_id, rid, user=call.from_user)
        text = "✅ Запись удалена" if success else "⚠️ Запись не найдена"
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            bot.edit_message_text(text, chat_id, active_id, reply_markup=build_edit_menu_keyboard(day_key, chat_id))

        bot.answer_callback_query(call.id, "Запись удалена" if success else "Запись не найдена")
        return

    # --- Удаление записи (отмена) ---
    if action == "cancel_delete":
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            text = "✏️ Меню редактирования"
            bot.edit_message_text(text, chat_id, active_id, reply_markup=build_edit_menu_keyboard(day_key, chat_id))
        bot.answer_callback_query(call.id, "Отмена удаления")
        return
    
    # --- Обнуление всех данных (подтверждение) ---
    if action == "confirm_reset_all":
        try:
            # удаляем данные конкретного чата
            cid = str(chat_id)
            if cid in data.get("chats", {}):
                data["chats"].pop(cid, None)
            # пересчёт общего баланса
            data["records"] = [
                r for r in data.get("records", [])
                if str(r.get("owner")) != cid
            ]
            data["overall_balance"] = sum(x["amount"] for x in data.get("records", []))
            save_data(data)

            # сообщение об успешном сбросе
            text = "⚙️ Все данные этого чата успешно обнулены."
            active_id = get_active_window_id(chat_id, day_key)
            if active_id:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=build_main_keyboard(today_key(), chat_id))
            else:
                bot.send_message(chat_id, text)

            bot.answer_callback_query(call.id, "Данные обнулены ✅")
        except Exception as e:
            log_error(f"confirm_reset_all: {e}")
            send_temp_message(chat_id, "❌ Ошибка при обнулении данных.")
            bot.answer_callback_query(call.id, "Ошибка обнуления")
        return
    
    
    bot.answer_callback_query(call.id)
    return


# -----------------------------
# 👤 Ручной ввод получателя пересылки
# -----------------------------
def process_forward_target(message, day_key_for_refresh=None):
    """Обрабатывает ввод ID получателя пересылки (или 0 для сброса)"""
    try:
        chat_id = message.chat.id
        store = get_chat_store(chat_id)
        text = (message.text or "").strip()

        if text == "0":
            store["forward_target_id"] = None
            save_data(data)
            send_temp_message(chat_id, "✅ Получатель пересылки сброшен (будет использоваться OWNER_ID, если он задан).")
            update_or_send_day_window(chat_id, day_key_for_refresh or today_key())
            return

        if not (text.startswith("-") or text.isdigit()):
            send_temp_message(chat_id, "❌ Некорректный ID. Пример: -1001234567890\nПопробуйте снова или введите 0 для сброса.")
            return

        try:
            target = int(text)
        except Exception:
            send_temp_message(chat_id, "❌ Не удалось распознать ID. Попробуйте снова.")
            return

        store["forward_target_id"] = target
        save_data(data)
        send_temp_message(chat_id, f"✅ Теперь пересылка будет идти пользователю с ID {target}.")
        update_or_send_day_window(chat_id, day_key_for_refresh or today_key())

    except Exception as e:
        log_error(f"process_forward_target: {e}")
        try:
            send_temp_message(message.chat.id, "❌ Ошибка при установке получателя. Попробуйте ещё раз.")
        except:
            pass


# -----------------------------
# 🔢 Regex и парсер сумм
# -----------------------------
num_re = re.compile(r'(\^[+-]\^[0-9]+|\^[+-]?[0-9]+\^|[+-]?\s*\d+)')

def parse_amount_token(token: str) -> int:
    """Разбирает сумму с любыми разделителями тысяч (пробел, точка, запятая, тире, подчёркивание, кавычки)"""
    t = token.strip()
    # убираем все возможные разделители
    for ch in ["^", " ", ".", ",", "-", "_", "'", '"']:
        t = t.replace(ch, "")
    # допускаем знак в начале
    if not t:
        return 0
    if t[0] == "+":
        return int(t[1:])
    if t[0] == "-":
        return -int(t[1:])
    # если знак не указан — считаем расходом
    return -int(t)

# -----------------------------
# 🧰 Команды бота
# -----------------------------
@bot.message_handler(commands=["start", "info"])
def cmd_start(msg):
    """/start — открыть окно текущего дня"""
    chat_id = msg.chat.id
    update_or_send_day_window(
        chat_id, today_key(),
        create_if_missing=True, force_new=True,
        delete_target_old=True, delete_source_old=False
    )


@bot.message_handler(commands=["help"])
def cmd_help(msg):
    """/help — краткая справка по форматам и командам"""
    chat_id = msg.chat.id
    send_temp_message(
        chat_id,
        "📘 Добавляйте записи так: +500 Зарплата или -150 Обед.\n\n"
        "Команды:\n"
        "/start /view YYYY-MM-DD /prev /next /balance /report /csv /reset /ping /help /json",
        delay=15
    )


@bot.message_handler(commands=["ping"])
def cmd_ping(msg):
    """/ping — проверка активности"""
    send_temp_message(msg.chat.id, "PONG — бот работает ✅")


@bot.message_handler(commands=["view"])
def cmd_view(msg):
    """/view YYYY-MM-DD — открыть указанную дату"""
    chat_id = msg.chat.id
    parts = (msg.text or "").split()
    if len(parts) < 2:
        send_temp_message(chat_id, "Укажите дату: /view YYYY-MM-DD"); return
    d = parse_date_key(parts[1])
    if not d:
        send_temp_message(chat_id, "Неверный формат даты. Пример: /view 2025-11-02"); return
    dk = d.strftime("%Y-%m-%d")
    prev_day = get_current_view_day(chat_id)
    update_or_send_day_window(
        chat_id, dk,
        create_if_missing=True, force_new=True,
        delete_target_old=True, delete_source_old=DELETE_SOURCE_WINDOW_ON_NAV,
        source_day_key=prev_day
    )


@bot.message_handler(commands=["prev"])
def cmd_prev(msg):
    """/prev — перейти к предыдущему дню"""
    chat_id = msg.chat.id
    current = get_current_view_day(chat_id)
    prev_key = shift_date_key(current, -1)
    update_or_send_day_window(
        chat_id, prev_key,
        create_if_missing=True, force_new=True,
        delete_target_old=True, delete_source_old=DELETE_SOURCE_WINDOW_ON_NAV,
        source_day_key=current
    )


@bot.message_handler(commands=["next"])
def cmd_next(msg):
    """/next — перейти к следующему дню"""
    chat_id = msg.chat.id
    current = get_current_view_day(chat_id)
    next_key = shift_date_key(current, +1)
    update_or_send_day_window(
        chat_id, next_key,
        create_if_missing=True, force_new=True,
        delete_target_old=True, delete_source_old=DELETE_SOURCE_WINDOW_ON_NAV,
        source_day_key=current
    )


@bot.message_handler(commands=["balance"])
def cmd_balance(msg):
    """/balance — показать остаток за сегодня"""
    chat_id = msg.chat.id
    store = get_chat_store(chat_id)
    dk = today_key()
    day_records = store.get("daily_records", {}).get(dk, [])
    inc = sum(r["amount"] for r in day_records if r["amount"] > 0)
    exp = sum(r["amount"] for r in day_records if r["amount"] < 0)
    bal = store.get("balance", 0)
    text = (
        f"📅 {dk}\n\n"
        f"💰 Общий остаток: {fmt_num(bal)} ARS\n"
        f"💵 Приход за день: {fmt_num(inc)} ARS\n"
        f"💸 Расход за день: {fmt_num(abs(exp))} ARS"
    )
    bot.send_message(chat_id, text)


@bot.message_handler(commands=["report"])
def cmd_report(msg):
    """/report — список расходов за сегодня"""
    chat_id = msg.chat.id
    store = get_chat_store(chat_id)
    dk = today_key()
    day_records = store.get("daily_records", {}).get(dk, [])
    expenses = [r for r in day_records if r["amount"] < 0]
    if not expenses:
        bot.send_message(chat_id, f"📅 {dk}\nНет расходов за сегодня.\n💸 Расход: 0 ARS"); return
    lines = [f"📅 {dk}", "📋 Расходы за день:"]
    for r in expenses[-30:]:
        lines.append(f"{r['short_id']}: -{fmt_num(abs(r['amount']))} — {r.get('note','')}")
    lines.append(f"\n💸 Расход за день: {fmt_num(sum(abs(r['amount']) for r in expenses))} ARS")
    bot.send_message(chat_id, "\n".join(lines))

@bot.message_handler(commands=["reset"])
def cmd_reset(msg):
    """/reset — подтверждение обнуления (inline)"""
    chat_id = msg.chat.id
    dk = today_key()
    active_id = get_active_window_id(chat_id, dk)
    if active_id:
        try:
            bot.edit_message_text(
                "⚠️ Вы уверены, что хотите обнулить все данные этого чата?",
                chat_id,
                active_id,
                reply_markup=build_reset_confirm_keyboard(dk)
            )
        except Exception as e:
            log_error(f"/reset edit_message_text: {e}")
    else:
        bot.send_message(
            chat_id,
            "⚠️ Вы уверены, что хотите обнулить все данные этого чата?",
            reply_markup=build_reset_confirm_keyboard(dk)
        )

@bot.message_handler(commands=["csv"])
def cmd_csv(msg):
    if str(msg.chat.id) != str(OWNER_ID):
        return
    bot.send_message(msg.chat.id, "📊 Отправляю актуальный CSV...")
    safe_csv_backup_to_telegram()

@bot.message_handler(commands=["json"])
def cmd_json(msg):
    if str(msg.chat.id) != str(OWNER_ID):
        return
    bot.send_message(msg.chat.id, "🧾 Отправляю актуальный data.json...")
    send_data_json_to_telegram()
    send_backup_to_channel()


@bot.message_handler(commands=["file_id"])
def cmd_file_id(msg):
    meta = _load_csv_meta()
    fid = meta.get("file_id_datajson")
    if fid:
        bot.send_message(msg.chat.id, f"🪄 Актуальный file_id для data.json:\n<code>{html.escape(fid)}</code>", parse_mode="HTML")
    else:
        bot.send_message(msg.chat.id, "⚠️ file_id для data.json ещё не найден (файл не отправлялся).")

@bot.message_handler(commands=["file_id_all"])
def cmd_file_id_all(msg):
    """Показывает file_id всех последних бэкап-файлов (CSV, data.json, csv_meta.json)"""
    try:
        meta = _load_csv_meta()

        fid_csv = meta.get("message_id") or meta.get("file_id_csv")
        fid_json = meta.get("file_id_datajson")
        fid_meta = meta.get("file_id_csvmeta") or meta.get("file_id_meta")

        text_lines = ["📦 *Актуальные file_id бэкапов:*"]

        if fid_csv:
            text_lines.append(f"📊 CSV: `{fid_csv}`")
        else:
            text_lines.append("📊 CSV: _не найден_")

        if fid_json:
            text_lines.append(f"🧾 data.json: `{fid_json}`")
        else:
            text_lines.append("🧾 data.json: _не найден_")

        if fid_meta:
            text_lines.append(f"🧠 csv_meta.json: `{fid_meta}`")
        else:
            text_lines.append("🧠 csv_meta.json: _не найден_")

        text = "\n".join(text_lines)
        bot.send_message(msg.chat.id, text, parse_mode="Markdown")

    except Exception as e:
        log_error(f"/file_id_all error: {e}")
        bot.send_message(msg.chat.id, f"❌ Ошибка при получении file_id: {e}")

# -----------------------------
# 📨 Основной обработчик текстовых сообщений
# -----------------------------
@bot.message_handler(func=lambda m: True)
def handle_message(msg):
    """Добавление/редактирование записей и подсказки формата"""
    chat_id = msg.chat.id
    store = get_chat_store(chat_id)
    wait_action = store.get("edit_wait")

    # 1) Ручное изменение записи
    if wait_action and wait_action.startswith("change_value"):
        day_key = today_key()
        if ":" in wait_action:
            _, day_key = wait_action.split(":", 1)

        rid = store.get("edit_target")
        if not rid:
            store["edit_wait"] = None
            store["edit_target"] = None
            save_data(data)
            return

        parts = (msg.text or "").strip().split(" ", 1)
        try:
            amount_token = parts[0]
            amount = parse_amount_token(amount_token)
            note = parts[1] if len(parts) > 1 else ""
            success, _ = update_record_in_chat(chat_id, rid, amount, note, user=msg.from_user)

            # сброс состояния
            store["edit_wait"] = None
            store["edit_target"] = None
            save_data(data)

            # перерисовать окно
            active_id = get_active_window_id(chat_id, day_key)
            text = render_day_text(store, day_key)
            if active_id:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=build_main_keyboard(day_key, chat_id))
            else:
                update_or_send_day_window(chat_id, day_key)

            send_temp_message(chat_id, f"✅ Запись R{rid} {'изменена' if success else 'не найдена'}")

        except Exception as e:
            log_error(f"edit manual: {e}")
            send_temp_message(chat_id, "❌ Ошибка формата. Пример: +500 Зарплата или -150 Обед", delay=10)
        return
    
    # ✅ 2. Добавление новой записи (многострочный ввод)
    if not msg.text:
        return

    # Каждая строка — отдельная операция (доход или расход)
    lines = (msg.text or "").strip().split("\n")
    for line in lines:
        line = line.strip()
        if not line:
            continue
        m = re.search(num_re, line)
        if not m:
            continue
        try:
            raw = m.group(0)
            raw_clean = raw.replace("^", "").replace(" ", "")
            amount = parse_amount_token(raw_clean)
            note = line.replace(m.group(0), "").strip()

            add_record_to_chat(chat_id, amount, note, msg.from_user.id)

        except Exception as e:
            log_error(f"auto-add record: {e}")
            send_temp_message(chat_id, f"❌ Ошибка при добавлении: {line}")

    # после добавления обновляем окно текущего дня
    dk = today_key()
    active_id = get_active_window_id(chat_id, dk)
    if active_id:
        text = render_day_text(store, dk)
        bot.edit_message_text(text, chat_id, active_id, reply_markup=build_main_keyboard(dk, chat_id))
    else:
        update_or_send_day_window(chat_id, dk)

    send_temp_message(chat_id, "✅ Записи добавлены")
    return
    
    # 3) Иные сообщения — подсказка
    send_temp_message(chat_id, "📘 Чтобы добавить запись, отправьте сумму в формате +500 Заметка. /help", delay=10)

# -----------------------------
# 🧩 Отладка: показать «всё, что под капотом»
# -----------------------------
# -----------------------------
# 🧩 Отладка: показать всё, что под капотом
# -----------------------------
#@bot.message_handler(content_types=["document"])
#def show_raw_message(msg):
    #"""Когда приходит документ — показать всю структуру message"""
    #import json
    #try:
        #raw = msg.json if hasattr(msg, "json") else msg.__dict__
        #text = json.dumps(raw, ensure_ascii=False, indent=2)
        # чтобы не было ошибки "message too long" — делим на части
        #parts = [text[i:i+3500] for i in range(0, len(text), 3500)]
        #for chunk in parts:
            #bot.send_message(msg.chat.id, f"```json\n{chunk}\n```", parse_mode="Markdown")
    #except Exception as e:
        #bot.send_message(msg.chat.id, f"Ошибка показа raw: {e}")

# --------------------------------------------
# 🔄 Восстановление данных из файла data.json (через Telegram)
# --------------------------------------------
@bot.message_handler(content_types=["document"])
def handle_document(msg):
    """Принимает data.json или другие файлы от владельца"""
    try:
        doc = msg.document
        if not doc or not doc.file_name:
            return

        fname = doc.file_name.lower()
        owner_id = int(OWNER_ID) if OWNER_ID else None

        # --- Восстановление из data.json ---
        if fname == "data.json" and msg.chat.id == owner_id:
            #log_chat("📥 Получен файл data.json от владельца — начинаю восстановление...")
            file_info = bot.get_file(doc.file_id)
            downloaded = bot.download_file(file_info.file_path)
            with open(DATA_FILE, "wb") as f:
                f.write(downloaded)

            # пробуем загрузить и проверить структуру
            restored = _load_json(DATA_FILE, {})
            if isinstance(restored, dict) and "records" in restored:
                global data
                data = restored
                #log_chat("✅ Данные успешно восстановлены из полученного файла data.json!")
                send_temp_message(msg.chat.id, "✅ Восстановление завершено успешно!")
                export_to_csv(data)
                _schedule_csv_backup_debounced()
            else:
                #log_chat("⚠️ Файл data.json не содержит корректных данных.")
                send_temp_message(msg.chat.id, "⚠️ Файл не содержит правильной структуры данных.")
            return

        # --- Просто показать содержимое других файлов ---
        elif fname.endswith(".json") or fname.endswith(".csv"):
            file_info = bot.get_file(doc.file_id)
            downloaded = bot.download_file(file_info.file_path)
            text = downloaded.decode("utf-8", errors="ignore")
            parts = [text[i:i+3500] for i in range(0, len(text), 3500)]
            for chunk in parts:
                bot.send_message(msg.chat.id, f"```{chunk}```", parse_mode="Markdown")
            return

        # --- Прочие документы ---
        else:
            bot.send_message(msg.chat.id, "📁 Файл получен, но не распознан для восстановления.")
    except Exception as e:
        log_error(f"handle_document error: {e}")
        send_temp_message(msg.chat.id, f"❌ Ошибка при обработке файла: {e}")

# -----------------------------
# 🌐 Flask webhook
# -----------------------------
@app.route(f"/{TOKEN}", methods=["POST"])
def webhook():
    """Обработка обновлений от Telegram через webhook"""
    try:
        update = telebot.types.Update.de_json(request.get_data().decode("utf-8"))
        bot.process_new_updates([update])
    except Exception as e:
        log_error(f"webhook error: {e}")
    return "OK", 200


@app.route("/", methods=["GET"])
def index():
    """Проверка состояния приложения"""
    return f"Финансовый бот — версия {VERSION} работает", 200


# -----------------------------
# 🟢 Keep-alive (самопинг и уведомление владельца)
# -----------------------------
def keep_alive_task():
    """Фоновая задача keep-alive: периодический пинг self и опционально владельца"""
    while True:
        try:
            log_info("Keep-alive: пинг self и уведомление владельца.")
            if APP_URL:
                try:
                    resp = requests.get(APP_URL, timeout=10)
                    log_info(f"Keep-alive self-ping {APP_URL} -> {getattr(resp, 'status_code', '?')}")
                except Exception as e:
                    log_error(f"Keep-alive self-ping error: {e}")

            if KEEP_ALIVE_SEND_TO_OWNER and OWNER_ID:
                try:
                    tgt = int(OWNER_ID)
                    send_temp_message(
                        tgt,
                        f"🟢 Keep-alive: бот {VERSION} активен.\n{now_local().isoformat(timespec='seconds')}"
                    )
                except Exception as e:
                    log_error(f"Keep-alive notify owner: {e}")

        except Exception as e:
            log_error(f"Keep-alive loop: {e}")

        time.sleep(max(10, KEEP_ALIVE_INTERVAL_SECONDS))


def start_keep_alive_thread():
    """Запуск фонового потока keep-alive"""
    threading.Thread(target=keep_alive_task, daemon=True).start()


# -----------------------------
# 🗓 Автосоздание окна нового дня (по наступлению следующей даты)
# -----------------------------
def schedule_daily_window_creation():
    """Автоматически создаёт новое окно при смене календарной даты"""
    def task():
        last_day = today_key()
        while True:
            try:
                time.sleep(60)
                current_day = today_key()
                if current_day != last_day:
                    for chat_id_str in list(data.get("chats", {}).keys()):
                        try:
                            chat_id = int(chat_id_str)
                        except:
                            continue
                        try:
                            update_or_send_day_window(
                                chat_id, current_day,
                                create_if_missing=True, force_new=True,
                                delete_target_old=True, delete_source_old=False
                            )
                        except Exception as e:
                            log_error(f"daily new window {chat_id}: {e}")
                    last_day = current_day
            except Exception as e:
                log_error(f"daily loop: {e}")
                time.sleep(5)

    threading.Thread(target=task, daemon=True).start()


# -----------------------------
# 🪝 Webhook и запуск приложения
# -----------------------------
def set_webhook():
    """Устанавливает webhook для Telegram"""
    url = f"{APP_URL}/{TOKEN}"
    try:
        bot.remove_webhook()
        time.sleep(0.5)
        bot.set_webhook(url=url)
        log_info(f"Webhook установлен: {url}")
    except Exception as e:
        log_error(f"set_webhook: {e}")

if __name__ == "__main__":
    """Точка входа — запуск приложения"""
   # log_chat("🚀 Запуск приложения и попытка восстановления CSV...")

    try:
        # 🧹 Сначала удаляем webhook, чтобы Telegram разрешил get_updates
        bot.delete_webhook()
        log_info("🔧 Webhook временно отключён для восстановления CSV.")
        time.sleep(10)

        # 🔄 Пробуем восстановить CSV из чата владельца
        restore_from_telegram_backup()

        log_info("🚀 Запуск приложения. Поиск актуального file_id...")
        find_file_id_message_before_webhook()
        log_info("✅ Поиск завершён. Теперь ищем первое сообщение в чате владельца...")
        find_first_message_in_chat_before_webhook()
        log_info("✅ Поиск первого сообщения завершён. Запуск webhook-сервера...")

        # 🕐 Короткая пауза, чтобы Telegram не вернул пустые апдейты
        time.sleep(2)

        # 🪝 Ставим webhook обратно
        set_webhook()
        #log_chat("🪝 Webhook установлен успешно.")

    except Exception as e:
      #  log_chat(f"❌ Ошибка при восстановлении/установке webhook: {e}", error=True)
        log_error(f"startup sequence error: {e}")

    # 🗓 Плановое создание нового окна при смене дня
    schedule_daily_window_creation()

    # 🕒 Фоновое автосохранение каждые 3 часа
  #  threading.Thread(target=auto_update_task, daemon=True).start()

    # 🟢 Keep-alive
    start_keep_alive_thread()

    log_info(f"Бот версии {VERSION} запущен")

    try:
        if OWNER_ID:
            bot.send_message(int(OWNER_ID), f"✅ Бот запущен и работает! (версия {VERSION})")
    except Exception as e:
        log_error(f"send startup msg: {e}")

    # 🌐 Запуск Flask-приложения
    app.run(host="0.0.0.0", port=PORT)