import telebot
from telebot import types
import json
import os
from datetime import date

# 🔹 вставь свой токен сюда (или используй переменную окружения)
TOKEN = os.getenv("BOT_TOKEN") or "ВСТАВЬ_СВОЙ_ТОКЕН_СЮДА"

bot = telebot.TeleBot(TOKEN)

DATA_FILE = "finance_data.json"

# Загружаем старые данные
if os.path.exists(DATA_FILE):
    with open(DATA_FILE, "r", encoding="utf-8") as f:
        finance_data = json.load(f)
else:
    finance_data = {}

# Сохраняем данные в файл
def save_data():
    with open(DATA_FILE, "w", encoding="utf-8") as f:
        json.dump(finance_data, f, ensure_ascii=False, indent=2)

# Получаем текущую дату
def today():
    return str(date.today())

# Формируем окно статистики
def get_summary(user_id):
    user_id = str(user_id)
    user_data = finance_data.get(user_id, {})
    all_time = sum([sum(d.values()) for d in user_data.values()]) if user_data else 0

    today_data = user_data.get(today(), {"income": 0, "expense": 0})
    income = today_data.get("income", 0)
    expense = today_data.get("expense", 0)

    text = (
        f"📅 <b>{today()}</b>\n"
        f"💸 Приход дня: <b>{income:.2f}</b>\n"
        f"💰 Расход дня: <b>{abs(expense):.2f}</b>\n"
        f"📊 Общий остаток: <b>{all_time:.2f}</b>"
    )
    return text

# Кнопка редактирования
def get_keyboard():
    kb = types.InlineKeyboardMarkup()
    kb.add(types.InlineKeyboardButton("✏️ Редактировать расход дня", callback_data="edit_day"))
    return kb

@bot.message_handler(commands=["start"])
def start(message):
    bot.reply_to(message, "👋 Привет! Отправь числа (например: -1000хлеб или +500 зп), я посчитаю твои финансы.")

@bot.message_handler(func=lambda msg: True)
def handle_message(message):
    user_id = str(message.from_user.id)
    text = message.text.strip()

    # Ищем первое число в строке
    num_str = ""
    for ch in text:
        if ch in "+-0123456789.":
            num_str += ch
        else:
            break

    try:
        number = float(num_str)
    except:
        return  # если нет корректного числа — игнорируем

    # Инициализация данных пользователя
    if user_id not in finance_data:
        finance_data[user_id] = {}
    if today() not in finance_data[user_id]:
        finance_data[user_id][today()] = {"income": 0, "expense": 0}

    # Запись данных
    if number >= 0:
        finance_data[user_id][today()]["income"] += number
    else:
        finance_data[user_id][today()]["expense"] += number

    save_data()

    # Отправка нового окна
    summary_text = get_summary(user_id)
    sent = bot.send_message(message.chat.id, summary_text, parse_mode="HTML", reply_markup=get_keyboard())

    # Удаляем предыдущее сообщение (если оно было)
    finance_data[user_id][today()]["last_msg"] = sent.message_id
    save_data()

    # Пробуем удалить предыдущее
    try:
        old_id = finance_data[user_id][today()].get("prev_msg")
        if old_id:
            bot.delete_message(message.chat.id, old_id)
        finance_data[user_id][today()]["prev_msg"] = sent.message_id
        save_data()
    except:
        pass

@bot.callback_query_handler(func=lambda call: call.data == "edit_day")
def edit_day(call):
    bot.answer_callback_query(call.id)
    bot.send_message(call.message.chat.id, "✏️ Функция редактирования расходов пока в разработке.")

print("✅ Бот запущен...")
bot.polling(non_stop=True)
