# ===========================================
# 1. Импорты и инициализация
# ===========================================
import os, json, time, re, threading
from datetime import datetime
from flask import Flask, request
import telebot
import csv

# Настройки
TOKEN = "ВАШ_ТОКЕН_БОТА"
APP_URL = "https://yourapp.onrender.com"  # заменить на свой
PORT = int(os.environ.get("PORT", 5000))
VERSION = "v3.0"

bot = telebot.TeleBot(TOKEN)
app = Flask(__name__)

DATA_FILE = "data.json"
CSV_FILE = "data.csv"
data = {"chats": {}}


# ===========================================
# 2. Загрузка и сохранение данных
# ===========================================
def load_data():
    global data
    if os.path.exists(DATA_FILE):
        with open(DATA_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
    else:
        data = {"chats": {}}

def save_data(d):
    with open(DATA_FILE, "w", encoding="utf-8") as f:
        json.dump(d, f, ensure_ascii=False, indent=2)

load_data()


# ===========================================
# 3. Вспомогательные функции
# ===========================================
def get_chat_store(chat_id):
    sid = str(chat_id)
    if sid not in data["chats"]:
        data["chats"][sid] = {
            "balance": 0,
            "records": [],
            "next_id": 1,
            "daily_records": {},
            "active_windows": {},
            "edit_wait": None,
            "edit_target": None,
            "forward_target": None
        }
    return data["chats"][sid]

def get_today_key():
    return datetime.now().strftime("%Y-%m-%d")

def format_amount(v):
    return f"{v:,.2f}".replace(",", " ").replace(".00", "")

def log_info(msg): print(f"[INFO] {msg}")
def log_error(msg): print(f"[ERROR] {msg}")


# ===========================================
# 4. Построение клавиатур и окна
# ===========================================
from telebot import types

def build_main_keyboard(chat_id, confirm_reset=False):
    kb = types.InlineKeyboardMarkup()
    row1 = [types.InlineKeyboardButton("💰 Баланс", callback_data="btn_balance"),
            types.InlineKeyboardButton("📊 Отчёт", callback_data="btn_report")]
    row2 = [types.InlineKeyboardButton("✏️ Изменить", callback_data="btn_edit"),
            types.InlineKeyboardButton("♻️ Обнулить", callback_data="btn_reset")]
    row3 = [types.InlineKeyboardButton("📤 Экспорт CSV", callback_data="export_csv"),
            types.InlineKeyboardButton("🔁 Обновить", callback_data="btn_start")]
    row4 = [types.InlineKeyboardButton("ℹ️ Инфо", callback_data="btn_cod")]
    kb.row(*row1); kb.row(*row2); kb.row(*row3); kb.row(*row4)
    if confirm_reset:
        kb.row(types.InlineKeyboardButton("✅ Да", callback_data="confirm_reset"),
               types.InlineKeyboardButton("❌ Нет", callback_data="cancel_reset"))
    return kb


def update_or_send_today_window(chat_id, extra_text=None):
    store = get_chat_store(chat_id)
    today = get_today_key()
    records = store.get("daily_records", {}).get(today, [])
    total = sum(r["amount"] for r in records)
    inc = sum(r["amount"] for r in records if r["amount"] > 0)
    exp = sum(r["amount"] for r in records if r["amount"] < 0)
    text = (f"📅 {today}\n"
            f"Приход: +{format_amount(inc)}\n"
            f"Расход: {format_amount(exp)}\n"
            f"Остаток дня: {format_amount(total)}\n"
            f"Общий баланс: {format_amount(store['balance'])}")
    if extra_text:
        text += f"\n\n{extra_text}"

    kb = build_main_keyboard(chat_id)
    msg_id = store.get("active_windows", {}).get("main")
    if msg_id:
        try:
            bot.edit_message_text(text, chat_id, msg_id, reply_markup=kb)
        except:
            m = bot.send_message(chat_id, text, reply_markup=kb)
            store["active_windows"]["main"] = m.message_id
    else:
        m = bot.send_message(chat_id, text, reply_markup=kb)
        store["active_windows"]["main"] = m.message_id
    save_data(data)


# ===========================================
# 5. Работа с таблицей и CSV
# ===========================================
def export_to_csv(d):
    with open(CSV_FILE, "w", newline="", encoding="utf-8") as f:
        writer = csv.writer(f)
        writer.writerow(["chat_id", "record_id", "date", "amount", "type"])
        for sid, store in d["chats"].items():
            for rec in store["records"]:
                writer.writerow([sid, rec["id"], rec["date"], rec["amount"], rec["type"]])
    log_info("CSV обновлён")


def add_record_to_chat(chat_id, amount):
    store = get_chat_store(chat_id)
    rid = store["next_id"]
    date = get_today_key()
    typ = "income" if amount > 0 else "expense"
    rec = {"id": rid, "date": date, "amount": amount, "type": typ}
    store["records"].append(rec)
    store["next_id"] += 1
    store["balance"] += amount
    store.setdefault("daily_records", {}).setdefault(date, []).append(rec)
    save_data(data)
    export_to_csv(data)
    update_or_send_today_window(chat_id, extra_text=f"✅ Добавлено: {format_amount(amount)} ({'+' if amount>0 else ''}{amount})")
    return rid


def delete_record_in_chat(chat_id, rid):
    store = get_chat_store(chat_id)
    records = store.get("records", [])
    for r in records:
        if r["id"] == rid:
            store["balance"] -= r["amount"]
            records.remove(r)
            for daylist in store["daily_records"].values():
                for dr in list(daylist):
                    if dr["id"] == rid:
                        daylist.remove(dr)
            save_data(data)
            export_to_csv(data)
            return True, r
    return False, None


# ===========================================
# 6. Разбор сообщений пользователя
# ===========================================
def parse_numbers_from_text(text):
    # поддержка разделителей: пробел, точка, запятая, кавычки
    text = text.replace("'", "").replace(",", "").replace(" ", "").replace(" ", "")
    lines = text.strip().splitlines()
    results = []
    for line in lines:
        match = re.findall(r"[-+]?\d+(?:[ .]\d+)*", line)
        if not match: continue
        try:
            val = match[0]
            val = float(val.replace(" ", "").replace(".", ""))
            if line.strip().startswith("-"):
                val = -abs(val)
            results.append(val)
        except:
            continue
    return results


# ===========================================
# 7. Обработка входящих сообщений
# ===========================================
@bot.message_handler(commands=["start"])
def start_cmd(msg):
    chat_id = msg.chat.id
    update_or_send_today_window(chat_id, extra_text="👋 Добро пожаловать! Введите сумму с + или - для учёта.")


@bot.message_handler(content_types=["text"])
def handle_text(msg):
    chat_id = msg.chat.id
    text = msg.text.strip()
    store = get_chat_store(chat_id)

    # 7.1 Перехват ожидания действий (редактирование / пересылка)
    if store.get("edit_wait"):
        handle_edit_or_forward(chat_id, text)
        return

    # 7.2 Если сообщение содержит числа — это запись прихода/расхода
    nums = parse_numbers_from_text(text)
    if nums:
        for n in nums:
            add_record_to_chat(chat_id, n)
        return

    # 7.3 Остальные сообщения — пересылка (если настроена)
    if store.get("forward_target"):
        try:
            bot.send_message(store["forward_target"], text)
        except Exception as e:
            update_or_send_today_window(chat_id, extra_text=f"⚠️ Ошибка пересылки: {e}")
        return

    update_or_send_today_window(chat_id, extra_text="ℹ️ Сообщение не распознано.")


# ===========================================
# 8. Управление редактированием и пересылкой
# ===========================================
def handle_edit_or_forward(chat_id, text):
    store = get_chat_store(chat_id)
    action = store.get("edit_wait")

    # 8.1 Изменение записи по ID
    if action == "change_id":
        try:
            rid = int(text.strip())
            for r in store["records"]:
                if r["id"] == rid:
                    store["edit_target"] = rid
                    store["edit_wait"] = "change_value"
                    update_or_send_today_window(chat_id, extra_text=f"✏️ Введите новое значение для записи {rid}")
                    return
            update_or_send_today_window(chat_id, extra_text="❌ Запись не найдена.")
        except:
            update_or_send_today_window(chat_id, extra_text="⚠️ Введите корректный ID.")
        return

    # 8.2 Изменение суммы
    if action == "change_value":
        rid = store.get("edit_target")
        try:
            val = float(text.replace(" ", "").replace(",", "."))
            for r in store["records"]:
                if r["id"] == rid:
                    old = r["amount"]
                    store["balance"] -= old
                    store["balance"] += val
                    r["amount"] = val
                    save_data(data)
                    export_to_csv(data)
                    update_or_send_today_window(chat_id, extra_text=f"✅ Изменено: {format_amount(old)} → {format_amount(val)}")
                    store["edit_wait"] = None
                    store["edit_target"] = None
                    return
        except:
            update_or_send_today_window(chat_id, extra_text="⚠️ Некорректное значение.")
        return

    # 8.3 Удаление записи
    if action == "delete_id":
        try:
            rid = int(text.strip())
            success, _ = delete_record_in_chat(chat_id, rid)
            if success:
                update_or_send_today_window(chat_id, extra_text=f"✅ Запись {rid} удалена.")
            else:
                update_or_send_today_window(chat_id, extra_text="❌ Не найдено.")
        except:
            update_or_send_today_window(chat_id, extra_text="⚠️ Ошибка удаления.")
        store["edit_wait"] = None
        return

    # 8.4 Настройка пересылки
    if action == "forward_id":
        try:
            target_id = int(text.strip())
            store["forward_target"] = target_id
            update_or_send_today_window(chat_id, extra_text=f"📤 Пересылка включена в чат {target_id}.")
        except:
            update_or_send_today_window(chat_id, extra_text="⚠️ Неверный ID чата.")
        store["edit_wait"] = None
        return


# ===========================================
# 10. Flask webhook
# ===========================================
@app.route(f"/{TOKEN}", methods=["POST"])
def webhook():
    update = telebot.types.Update.de_json(request.get_data().decode("utf-8"))
    bot.process_new_updates([update])
    return "OK", 200


@app.route("/", methods=["GET"])
def index():
    return f"Финансовый бот «ФО» — версия {VERSION} работает стабильно ✅", 200


# ===========================================
# 11. Планировщик ежедневного обновления окна
# ===========================================
def schedule_daily_window_creation():
    def task():
        last_day = get_today_key()
        while True:
            try:
                time.sleep(60)
                current_day = get_today_key()
                if current_day != last_day:
                    # Архивируем предыдущее окно
                    for chat_id_str, store in list(data.get("chats", {}).items()):
                        try:
                            chat_id = int(chat_id_str)
                            old_window = data.get("active_messages", {}).get(last_day, {}).get(chat_id_str)
                            if old_window:
                                bot.send_message(chat_id, f"📦 День {last_day} завершён. Архив сохранён.")
                            update_or_send_today_window(chat_id)
                        except Exception as e:
                            log_error(f"Ошибка обновления дня для {chat_id_str}: {e}")
                    last_day = current_day
            except Exception as e:
                log_error(f"Ошибка в цикле планировщика: {e}")
                time.sleep(5)
    thread = threading.Thread(target=task, daemon=True)
    thread.start()


# ===========================================
# 12. Установка Webhook и запуск приложения
# ===========================================
def set_webhook():
    url = f"{APP_URL}/{TOKEN}"
    try:
        bot.remove_webhook()
        time.sleep(0.5)
        bot.set_webhook(url=url)
        log_info(f"Webhook установлен: {url}")
    except Exception as e:
        log_error(f"Не удалось установить webhook: {e}")


if __name__ == "__main__":
    log_info(f"Бот ФО версии {VERSION} запущен.")
    if OWNER_ID:
        try:
            bot.send_message(int(OWNER_ID), f"🚀 Бот ФО версии {VERSION} запущен и готов к работе.")
        except Exception as e:
            log_error(f"Ошибка уведомления владельца: {e}")

    set_webhook()
    schedule_daily_window_creation()
    app.run(host="0.0.0.0", port=PORT)




