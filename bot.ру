# ============================================
# Финансовый бот — версия 9.6.1.7
# Webhook-only, минимальные изменения от оригинала:
# - одно окно в день, все кнопки работают в одном окне
# - редактирование/удаление записей (подтверждение)
# - кнопка "Назад"
# - ежедневное создание нового окна (при смене даты)
# - если окно удалено — создаётся новое с теми же данными
# - временные уведомления удаляются через 5 секунд (везде)
# - управление пересылкой и allowlist
# ============================================

import os
import json
import csv
import logging
import re
import threading
import time
from datetime import datetime
from zoneinfo import ZoneInfo
import traceback

import telebot
from flask import Flask, request
from telebot import types

# --------------------------------------------
# Настройки окружения и константы
# --------------------------------------------
TOKEN = os.getenv("BOT_TOKEN")
OWNER_ID = os.getenv("OWNER_ID")  # владелец для уведомлений (строка/число)
APP_URL = os.getenv("APP_URL")  # обязателен для webhook
TZ = ZoneInfo("America/Argentina/Catamarca")
DATA_FILE = "data.json"
CSV_FILE = "data.csv"
LOG_FILE = "log.txt"
PORT = int(os.environ.get("PORT", 5000))
VERSION = "9.6.1.7"

if not TOKEN:
    raise ValueError("BOT_TOKEN не установлен!")
if not APP_URL:
    raise ValueError("APP_URL не установлен! (нужен для webhook)")

bot = telebot.TeleBot(TOKEN, threaded=True)
app = Flask(__name__)

# -----------------------------
# Логирование
# -----------------------------
logging.basicConfig(
    level=logging.INFO,
    format="[{asctime}] {levelname}: {message}",
    style="{",
    handlers=[
        logging.FileHandler(LOG_FILE, encoding="utf-8"),
        logging.StreamHandler()
    ]
)

def log_info(msg):
    logging.info(msg)

def log_error(msg):
    logging.error(msg)

log_info(f"Старт приложения. Версия {VERSION}")

# -----------------------------
# Данные по-умолчанию / IO
# -----------------------------
def default_data():
    return {
        "overall_balance": 0,
        "records": [],
        "chats": {},                # chat_id -> {balance, records, daily_records, next_id}
        "active_messages": {},      # YYYY-MM-DD -> { chat_id_str -> message_id }
        "processed_messages": [],
        "forward_targets": [],      # list of chat ids (int)
        "allowed_forwarders": [],   # list of user ids (int) allowed to message bot in private to be processed
        "tracked_messages": {},     # chat_id_str -> { user_id_str -> state dict }
        "next_id": 1
    }

def load_data():
    if os.path.exists(DATA_FILE):
        try:
            with open(DATA_FILE, "r", encoding="utf-8") as f:
                d = json.load(f)
        except Exception as e:
            log_error(f"Ошибка загрузки data.json: {e}")
            d = default_data()
    else:
        d = default_data()
    # ensure keys present
    base = default_data()
    for k, v in base.items():
        if k not in d:
            d[k] = v
    return d

def save_data():
    try:
        with open(DATA_FILE, "w", encoding="utf-8") as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
    except Exception as e:
        log_error(f"Ошибка сохранения data.json: {e}")

data = load_data()

# Если OWNER_ID задан, добавляем его в forward_targets (как раньше было)
if OWNER_ID:
    try:
        oi = int(OWNER_ID)
        if oi not in data.get("forward_targets", []):
            data["forward_targets"].append(oi)
            save_data()
    except Exception:
        pass

# -----------------------------
# Вспомогательные функции (дата/чат)
# -----------------------------
def get_today_key():
    return datetime.now(TZ).strftime("%Y-%m-%d")

def get_chat_store(chat_id):
    cid = str(chat_id)
    if cid not in data["chats"]:
        data["chats"][cid] = {
            "balance": 0,
            "records": [],
            "next_id": 1,
            "daily_records": {}
        }
        save_data()
    return data["chats"][cid]

def set_today_active_window(chat_id, message_id):
    today = get_today_key()
    if "active_messages" not in data:
        data["active_messages"] = {}
    if today not in data["active_messages"]:
        data["active_messages"][today] = {}
    data["active_messages"][today][str(chat_id)] = message_id
    save_data()

def get_today_active_window(chat_id):
    today = get_today_key()
    return data.get("active_messages", {}).get(today, {}).get(str(chat_id))

# -----------------------------
# Экспорт CSV
# -----------------------------
def export_to_csv(d):
    try:
        with open(CSV_FILE, "w", newline='', encoding="utf-8") as f:
            writer = csv.writer(f)
            writer.writerow(["chat_id","ID","short_id","timestamp","amount","note","owner","day_key"])
            for cid, cdata in d.get("chats", {}).items():
                for day_key, records in cdata.get("daily_records", {}).items():
                    for r in records:
                        writer.writerow([cid, r.get("id"), r.get("short_id"), r.get("timestamp"),
                                         r.get("amount"), r.get("note"), r.get("owner"), day_key])
    except Exception as e:
        log_error(f"Ошибка экспорта CSV: {e}")

# -----------------------------
# Работа с записями
# -----------------------------
def add_record_to_chat(chat_id, amount, note, owner):
    store = get_chat_store(chat_id)
    rid = data.get("next_id", 1)
    rec = {
        "id": rid,
        "short_id": f"R{rid}",
        "timestamp": datetime.now(TZ).isoformat(timespec="seconds"),
        "amount": amount,
        "note": note,
        "owner": owner
    }
    data.setdefault("records", []).append(rec)
    store.setdefault("records", []).append(rec)
    day = get_today_key()
    store.setdefault("daily_records", {}).setdefault(day, []).append(rec)
    store["balance"] = store.get("balance", 0) + amount
    data["overall_balance"] = data.get("overall_balance", 0) + amount
    data["next_id"] = rid + 1
    save_data()
    try:
        export_to_csv(data)
    except Exception:
        pass
    return rec

def update_record_in_chat(chat_id, rid, new_amount, new_note):
    store = get_chat_store(chat_id)
    found = None
    for r in store.get("records", []):
        if r["id"] == rid:
            r["amount"] = new_amount
            r["note"] = new_note
            found = r
            break
    if found:
        for day_recs in store.get("daily_records", {}).values():
            for r in day_recs:
                if r["id"] == rid:
                    r.update(found)
        store["balance"] = sum([x["amount"] for x in store.get("records", [])])
        data["overall_balance"] = sum([x["amount"] for x in data.get("records", [])])
        save_data()
        export_to_csv(data)
        return True, found
    return False, None

def delete_record_in_chat(chat_id, rid):
    store = get_chat_store(chat_id)
    removed = None
    for r in list(store.get("records", [])):
        if r["id"] == rid or str(r["id"]) == str(rid) or r.get("short_id") == f"R{rid}":
            removed = r
            store["records"].remove(r)
            break
    if removed:
        for day_key, recs in store.get("daily_records", {}).items():
            store["daily_records"][day_key] = [x for x in recs if x["id"] != rid]
        store["balance"] = sum([x["amount"] for x in store.get("records", [])])
        data["records"] = [x for x in data.get("records", []) if x["id"] != rid]
        data["overall_balance"] = sum([x["amount"] for x in data.get("records", [])])
        save_data()
        export_to_csv(data)
        return True, removed
    return False, None

def get_daily_records(chat_id, day_key=None):
    store = get_chat_store(chat_id)
    if not day_key:
        day_key = get_today_key()
    return store.get("daily_records", {}).get(day_key, [])

# -----------------------------
# Парсинг чисел
# -----------------------------
num_re = re.compile(r'([+-]?\s*\d+)')

def extract_first_number(s):
    if not s:
        return None, None
    m = num_re.search(s)
    if not m:
        return None, None
    token = m.group(1)
    token_clean = token.replace(" ", "")
    try:
        amt = int(token_clean)
    except:
        return None, None
    return amt, token.strip()

# -----------------------------
# Временные сообщения (удаляются через seconds)
# -----------------------------
def send_temporary_message(chat_id, text, reply_markup=None, seconds=5):
    try:
        sent = bot.send_message(chat_id, text, reply_markup=reply_markup)
    except Exception as e:
        log_error(f"send_temporary_message send error: {e}")
        return None

    def _del():
        try:
            bot.delete_message(chat_id, sent.message_id)
        except Exception as e:
            # возможно уже удалено или нет прав
            log_error(f"Не удалось удалить временное сообщение: {e}")

    t = threading.Timer(seconds, _del)
    t.daemon = True
    t.start()
    return sent

# -----------------------------
# Клавиатуры (компактные)
# -----------------------------
def build_main_keyboard(chat_id=None, confirm_reset=False):
    kb = types.InlineKeyboardMarkup(row_width=3)
    kb.row(
        types.InlineKeyboardButton("💰баланс", callback_data="btn_balance"),
        types.InlineKeyboardButton("📊отчёт", callback_data="btn_report"),
        types.InlineKeyboardButton("✏️изменить", callback_data="btn_edit")
    )
    if confirm_reset:
        kb.row(
            types.InlineKeyboardButton("✅ Да", callback_data="confirm_reset"),
            types.InlineKeyboardButton("❌ Нет", callback_data="cancel_reset")
        )
    else:
        kb.row(
            types.InlineKeyboardButton("⚙️обнулить", callback_data="btn_reset"),
            types.InlineKeyboardButton("🚀старт", callback_data="btn_start"),
            types.InlineKeyboardButton("ℹ️о коде", callback_data="btn_cod")
        )
    kb.row(
        types.InlineKeyboardButton("⬅️ Назад", callback_data="btn_start")
    )
    balance_text = ""
    if chat_id is not None:
        store = get_chat_store(chat_id)
        balance_text = f"💰 {store.get('balance', 0)} ARS"
    kb.add(types.InlineKeyboardButton(balance_text or "💰 0 ARS", callback_data="noop"))
    return kb

def build_edit_row_keyboard(rec):
    kb = types.InlineKeyboardMarkup(row_width=2)
    kb.add(
        types.InlineKeyboardButton("📝 Запись", callback_data=f"showrec_{rec['id']}"),
        types.InlineKeyboardButton("✏️ Изменить", callback_data=f"editrec_{rec['id']}")
    )
    kb.add(
        types.InlineKeyboardButton("🗑 Удалить", callback_data=f"deleterec_{rec['id']}"),
        types.InlineKeyboardButton("⬅️ Назад", callback_data="btn_edit")
    )
    return kb

# -----------------------------
# Обновление/создание окна (создаёт новое, если удалено)
# -----------------------------
def update_or_send_today_window(chat_id):
    today_key = get_today_key()
    store = get_chat_store(chat_id)
    day_records = store.get("daily_records", {}).get(today_key, [])

    if not day_records:
        text = f"📅 {today_key}\nПока нет операций.\n💰 Баланс: {store.get('balance', 0)} ARS"
    else:
        lines = [f"📅 {today_key}", "📋 Операции:"]
        for r in day_records[-50:]:
            sign = "+" if r["amount"] > 0 else ""
            lines.append(f"{r['short_id']}: {sign}{r['amount']} — {r.get('note','')}")
        lines.append(f"\n💰 Баланс: {store.get('balance', 0)} ARS")
        text = "\n".join(lines)

    kb = build_main_keyboard(chat_id)
    today_msgs = data.setdefault("active_messages", {}).setdefault(today_key, {})
    active_id = today_msgs.get(str(chat_id))

    if active_id:
        # попытка редактировать
        try:
            bot.edit_message_text(text, chat_id, active_id, reply_markup=kb)
            return
        except Exception as e:
            log_error(f"[update_or_send_today_window] редактирование не удалось: {e}")
            # создаём новое окно, если редактирование не удалось (например, удалено)
            try:
                sent = bot.send_message(chat_id, text, reply_markup=kb)
                set_today_active_window(chat_id, sent.message_id)
                log_info(f"[update_or_send_today_window] Создано новое окно после ошибки редактирования для чата {chat_id}")
                return
            except Exception as e2:
                log_error(f"[update_or_send_today_window] Не удалось создать окно после ошибки: {e2}")
                return

    # если активного окна нет — создаём
    try:
        sent = bot.send_message(chat_id, text, reply_markup=kb)
        set_today_active_window(chat_id, sent.message_id)
        log_info(f"[update_or_send_today_window] Создано новое окно для чата {chat_id}, message_id={sent.message_id}")
    except Exception as e:
        log_error(f"[update_or_send_today_window] Не удалось создать окно для chat {chat_id}: {e}")

# -----------------------------
# Обработка callback'ов (edit/delete flows, back, etc.)
# -----------------------------
@bot.callback_query_handler(func=lambda c: True)
def handle_callback(call):
    try:
        data_cd = call.data
        cid = call.message.chat.id
        mid = call.message.message_id
        uid = call.from_user.id

        def ack(text=None):
            try:
                if text:
                    bot.answer_callback_query(call.id, text)
                else:
                    bot.answer_callback_query(call.id)
            except Exception:
                pass

        # Защита: только владелец может делать btn_reset, setforward в UI
        if str(uid) != str(OWNER_ID):
            if data_cd in ("confirm_reset", "btn_reset", "setforward_cmd"):
                ack("Только владелец может выполнять это действие.")
                return

        # Баланс (редактируем текущее окно)
        if data_cd == "btn_balance":
            store = get_chat_store(cid)
            text = f"💰 Баланс: {store.get('balance', 0)} ARS"
            try:
                bot.edit_message_text(text, cid, mid, reply_markup=build_main_keyboard(cid))
            except Exception:
                bot.send_message(cid, text, reply_markup=build_main_keyboard(cid))
            ack()
            return

        # Отчёт
        if data_cd == "btn_report":
            recs = get_daily_records(cid)
            if not recs:
                text = "📋 Пока нет записей."
            else:
                lines = ["📋 Последние операции:"]
                for r in recs[-20:]:
                    sign = "+" if r["amount"] > 0 else ""
                    lines.append(f"{r['short_id']}: {sign}{r['amount']} ARS — {r.get('note','')}")
                text = "\n".join(lines)
            try:
                bot.edit_message_text(text, cid, mid, reply_markup=build_main_keyboard(cid))
            except Exception:
                bot.send_message(cid, text, reply_markup=build_main_keyboard(cid))
            ack()
            return

        # Сброс (запрос подтверждения)
        if data_cd == "btn_reset":
            kb = build_main_keyboard(cid, confirm_reset=True)
            text = "⚠️ Сбросить все данные чата?"
            try:
                bot.edit_message_text(text, cid, mid, reply_markup=kb)
            except:
                bot.send_message(cid, text, reply_markup=kb)
            ack()
            return

        if data_cd == "confirm_reset":
            chat_store = data["chats"].get(str(cid))
            if chat_store:
                chat_store["records"] = []
                chat_store["balance"] = 0
                chat_store["daily_records"] = {}
                chat_store["next_id"] = 1
            save_data()
            text = "✅ Данные чата сброшены."
            try:
                bot.edit_message_text(text, cid, mid, reply_markup=build_main_keyboard(cid))
            except:
                bot.send_message(cid, text, reply_markup=build_main_keyboard(cid))
            ack()
            return

        if data_cd == "cancel_reset":
            try:
                bot.edit_message_text("❌ Отмена сброса.", cid, mid, reply_markup=build_main_keyboard(cid))
            except:
                bot.send_message(cid, "❌ Отмена сброса.", reply_markup=build_main_keyboard(cid))
            ack()
            return

        # Главное меню / старт
        if data_cd == "btn_start":
            try:
                update_or_send_today_window(cid)
            except Exception:
                bot.send_message(cid, "Главное меню", reply_markup=build_main_keyboard(cid))
            ack()
            return

        # О коде
        if data_cd == "btn_cod":
            info_text = (
                f"🤖 Финансовый бот — версия {VERSION}\n\n"
                "• Учёт по дням (одно окно)\n"
                "• Редактирование записей (выбор / редактировать / удалить)\n"
                "• Пересылка сообщений настроена владельцем\n"
            )
            try:
                bot.edit_message_text(info_text, cid, mid, reply_markup=build_main_keyboard(cid))
            except:
                bot.send_message(cid, info_text, reply_markup=build_main_keyboard(cid))
            ack()
            return

        # noop
        if data_cd == "noop":
            ack()
            return

        # Редактирование: показать недавние записи
        if data_cd == "btn_edit":
            recs = get_daily_records(cid)
            if not recs:
                try:
                    bot.edit_message_text("Нет записей для редактирования.", cid, mid, reply_markup=build_main_keyboard(cid))
                except:
                    bot.send_message(cid, "Нет записей для редактирования.", reply_markup=build_main_keyboard(cid))
                ack()
                return
            kb = types.InlineKeyboardMarkup(row_width=2)
            for r in recs[-10:]:
                lab = f"{r['short_id']} {r['amount']}"
                kb.add(types.InlineKeyboardButton(lab, callback_data=f"selrec_{r['id']}"))
            kb.add(types.InlineKeyboardButton("⬅️ Назад", callback_data="btn_start"))
            try:
                bot.edit_message_text("Выберите запись:", cid, mid, reply_markup=kb)
            except:
                bot.send_message(cid, "Выберите запись:", reply_markup=kb)
            ack()
            return

        # Выбрали запись — показать действие
        if data_cd.startswith("selrec_"):
            try:
                rec_id = int(data_cd.split("_", 1)[1])
            except:
                ack("Ошибка id")
                return
            rec_obj = None
            for r in get_chat_store(cid).get("records", []):
                if r["id"] == rec_id:
                    rec_obj = r
                    break
            if not rec_obj:
                try:
                    bot.edit_message_text("Запись не найдена.", cid, mid, reply_markup=build_main_keyboard(cid))
                except:
                    bot.send_message(cid, "Запись не найдена.", reply_markup=build_main_keyboard(cid))
                ack()
                return
            kb = build_edit_row_keyboard(rec_obj)
            txt = f"Запись {rec_obj.get('short_id')}: {rec_obj.get('amount')} — {rec_obj.get('note','')}"
            try:
                bot.edit_message_text(txt, cid, mid, reply_markup=kb)
            except:
                bot.send_message(cid, txt, reply_markup=kb)
            ack()
            return

        # Показать запись
        if data_cd.startswith("showrec_"):
            try:
                rec_id = int(data_cd.split("_", 1)[1])
            except:
                ack("Ошибка")
                return
            rec_obj = None
            for r in get_chat_store(cid).get("records", []):
                if r["id"] == rec_id:
                    rec_obj = r
                    break
            if not rec_obj:
                ack("Не найдено")
                return
            txt = f"📝 {rec_obj.get('short_id')}\n{rec_obj.get('amount')} ARS\n{rec_obj.get('note','')}\nАвтор: {rec_obj.get('owner')}"
            try:
                bot.edit_message_text(txt, cid, mid, reply_markup=build_main_keyboard(cid))
            except:
                bot.send_message(cid, txt, reply_markup=build_main_keyboard(cid))
            ack()
            return

        # Начать редактирование (перевести пользователя в состояние ожидания)
        if data_cd.startswith("editrec_"):
            try:
                rec_id = int(data_cd.split("_", 1)[1])
            except:
                ack("Ошибка")
                return
            data.setdefault("tracked_messages", {}).setdefault(str(cid), {})[str(uid)] = {
                "action": "edit_wait_value",
                "rid": rec_id
            }
            save_data()
            try:
                bot.edit_message_text(f"Введи новую строку для записи R{rec_id} (пример: +500 зарплата):", cid, mid)
            except:
                bot.send_message(cid, f"Введи новую строку для записи R{rec_id} (пример: +500 зарплата):")
            ack()
            return

        # Подготовка удаления (подтверждение)
        if data_cd.startswith("deleterec_"):
            try:
                rec_id = int(data_cd.split("_", 1)[1])
            except:
                ack("Ошибка")
                return
            kb = types.InlineKeyboardMarkup(row_width=2)
            kb.add(
                types.InlineKeyboardButton("✅ Да, удалить", callback_data=f"confirm_del_{rec_id}"),
                types.InlineKeyboardButton("❌ Отмена", callback_data=f"cancel_del_{rec_id}")
            )
            try:
                bot.edit_message_text(f"Удалить запись R{rec_id}?", cid, mid, reply_markup=kb)
            except:
                bot.send_message(cid, f"Удалить запись R{rec_id}?", reply_markup=kb)
            ack()
            return

        # Подтверждение удаления
        if data_cd.startswith("confirm_del_"):
            parts = data_cd.split("_")
            rec_id = None
            if len(parts) >= 3 and parts[-1].isdigit():
                rec_id = int(parts[-1])
            if rec_id is None:
                ack("Ошибка id")
                return
            ok, removed = delete_record_in_chat(cid, rec_id)
            if ok:
                try:
                    bot.edit_message_text(f"✅ Запись R{rec_id} удалена.", cid, mid, reply_markup=build_main_keyboard(cid))
                except:
                    bot.send_message(cid, f"✅ Запись R{rec_id} удалена.", reply_markup=build_main_keyboard(cid))
                update_or_send_today_window(cid)
            else:
                try:
                    bot.edit_message_text("Не удалось удалить запись (не найдена).", cid, mid, reply_markup=build_main_keyboard(cid))
                except:
                    bot.send_message(cid, "Не удалось удалить запись (не найдена).", reply_markup=build_main_keyboard(cid))
            ack()
            return

        if data_cd.startswith("cancel_del_"):
            try:
                bot.edit_message_text("Отмена удаления.", cid, mid, reply_markup=build_main_keyboard(cid))
            except:
                bot.send_message(cid, "Отмена удаления.", reply_markup=build_main_keyboard(cid))
            ack()
            return

        # Управление пересылкой (placeholder in UI)
        if data_cd == "setforward_cmd":
            ack("Используйте /setforward <chat_id> для добавления цели.")
            return

        ack()
    except Exception as e:
        log_error(f"Ошибка callback: {e}\n{traceback.format_exc()}")

# -----------------------------
# Обработка входящих сообщений (включая режим редактирования)
# -----------------------------
@bot.message_handler(func=lambda m: True, content_types=[
    "text", "photo", "document", "voice", "video", "audio", "sticker"
])
def handle_message(msg):
    try:
        if not getattr(msg, "from_user", None) or msg.from_user.is_bot:
            return

        chat_id = msg.chat.id
        user_id = msg.from_user.id
        is_private = (msg.chat.type == "private")

        # 1) Проверка режима редактирования
        chat_state = data.setdefault("tracked_messages", {})
        user_states = chat_state.get(str(chat_id), {})
        state = None
        if user_states:
            state = user_states.get(str(user_id))

        if state and state.get("action") == "edit_wait_value":
            rid = state.get("rid")
            lines = msg.text.splitlines() if msg.text else []
            if not lines:
                send_temporary_message(chat_id, "Введите строку с числом, например: +500 продажа.", reply_markup=build_main_keyboard(chat_id))
                return
            first_line = lines[0].strip()
            amt, token = extract_first_number(first_line)
            if amt is None:
                send_temporary_message(chat_id, "Число не обнаружено. Попробуйте ещё раз.", reply_markup=build_main_keyboard(chat_id))
                return
            m = num_re.search(first_line)
            note = first_line[m.end():].strip() if m else ""
            final_amt = amt if token.lstrip().startswith("+") else -abs(amt)
            ok, updated = update_record_in_chat(chat_id, rid, final_amt, note)
            # remove state
            user_states.pop(str(user_id), None)
            data["tracked_messages"][str(chat_id)] = user_states
            save_data()
            if ok:
                update_or_send_today_window(chat_id)
                send_temporary_message(chat_id, f"✅ Запись R{rid} обновлена.", reply_markup=build_main_keyboard(chat_id))
            else:
                send_temporary_message(chat_id, "Не удалось обновить запись (не найдена).", reply_markup=build_main_keyboard(chat_id))
            return

        # 2) Пропуск дубликатов
        msg_key = f"{chat_id}:{msg.message_id}"
        processed = data.setdefault("processed_messages", [])
        if msg_key in processed:
            return

        # 3) Обработка PRIVATE сообщений от allowed_forwarders (учёт и пересылка)
        if is_private:
            allowed = [str(x) for x in data.get("allowed_forwarders", [])]
            if (OWNER_ID and str(user_id) == str(OWNER_ID)) or str(user_id) in allowed:
                # treat content as potential records and forward the message to forward_targets
                lines = []
                if msg.content_type == "text" and msg.text:
                    lines = msg.text.splitlines()
                elif getattr(msg, "caption", None):
                    lines = msg.caption.splitlines()
                added = False
                for line in lines:
                    line = line.strip()
                    if not line:
                        continue
                    amt, token = extract_first_number(line)
                    if amt is None:
                        continue
                    m = num_re.search(line)
                    note = line[m.end():].strip() if m else ""
                    final_amt = amt if token.lstrip().startswith("+") else -abs(amt)
                    # record into owner's chat by default (or into forward targets)
                    targets = data.get("forward_targets", [])
                    if targets:
                        for t in targets:
                            try:
                                add_record_to_chat(int(t), final_amt, note, user_id)
                                added = True
                            except Exception as e:
                                log_error(f"Ошибка добавления записи в целевой чат {t}: {e}")
                    else:
                        # no forward targets -> put in owner's chat if exists
                        if OWNER_ID:
                            try:
                                add_record_to_chat(int(OWNER_ID), final_amt, note, user_id)
                                added = True
                            except Exception as e:
                                log_error(f"Ошибка добавления записи в OWNER_ID: {e}")
                # forward original message copy to targets
                for t in data.get("forward_targets", []):
                    try:
                        bot.copy_message(chat_id=int(t), from_chat_id=chat_id, message_id=msg.message_id)
                    except Exception as e:
                        log_error(f"Ошибка пересылки из лички в {t}: {e}")
                if added:
                    send_temporary_message(chat_id, "✅ Сообщение принято и учтено.", seconds=5)
                else:
                    send_temporary_message(chat_id, "Сообщение принято (не содержало сумм).", seconds=5)
                processed.append(msg_key)
                if len(processed) > 2000:
                    processed = processed[-1000:]
                data["processed_messages"] = processed
                save_data()
                return
            else:
                send_temporary_message(chat_id, "Вам запрещено пересылать/участвовать. Свяжитесь с владельцем.", seconds=5)
                return

        # 4) Стандартная обработка в чатах/группах и личке (если не режим edit)
        lines = []
        if msg.content_type == "text" and msg.text:
            lines = msg.text.splitlines()
        elif getattr(msg, "caption", None):
            lines = msg.caption.splitlines()

        added = False
        for line in lines:
            line = line.strip()
            if not line:
                continue
            amt, token = extract_first_number(line)
            if amt is None:
                continue
            m = num_re.search(line)
            note = line[m.end():].strip() if m else ""
            final_amt = amt if token.lstrip().startswith("+") else -abs(amt)
            add_record_to_chat(chat_id, final_amt, note, user_id)
            added = True

        # mark processed
        processed.append(msg_key)
        if len(processed) > 2000:
            processed = processed[-1000:]
        data["processed_messages"] = processed
        save_data()

        # обновляем единое окно
        if added:
            update_or_send_today_window(chat_id)
            send_temporary_message(chat_id, "✅ Добавлено", reply_markup=build_main_keyboard(chat_id), seconds=5)

        # пересылка сообщений от владельца (как раньше)
        owner_int = int(OWNER_ID) if OWNER_ID and str(OWNER_ID).isdigit() else None
        is_owner_msg = (owner_int is not None and user_id == owner_int)
        if is_owner_msg:
            for target in data.get("forward_targets", []):
                try:
                    t = int(target)
                except:
                    continue
                if t == chat_id:
                    update_or_send_today_window(owner_int)
                    continue
                try:
                    sent = bot.copy_message(chat_id=t, from_chat_id=chat_id, message_id=msg.message_id)
                    data.setdefault("processed_messages", []).append(f"{sent.chat.id}:{sent.message_id}")
                except Exception as e:
                    log_error(f"Ошибка пересылки в {t}: {e}")
            save_data()

    except Exception as e:
        log_error(f"Ошибка handle_message: {e}\n{traceback.format_exc()}")

# -----------------------------
# Команды (start, myid, chatid, groups, forwards management, allowlist)
# -----------------------------
@bot.message_handler(commands=["start"])
def cmd_start(msg):
    chat_id = msg.chat.id
    text = (
        "👋 Добро пожаловать в Финансовый бот «ФО»\n\n"
        "Команды:\n"
        "/start — показать это сообщение\n"
        "/myid — ваш user ID\n"
        "/chatid — ID текущего чата\n"
        "/groups — список чатов (где есть данные)\n"
        "/setforward <chat_id> — добавить цель пересылки (владелец)\n"
        "/removeforward <chat_id> — удалить цель пересылки (владелец)\n"
        "/listforwards — показать цели пересылки\n"
        "/allowforward <user_id> — разрешить пересылать в личку\n"
        "/disallowforward <user_id> — запретить пересылать\n"
        "/about — информация о боте\n\n"
        "Отправьте строку с суммой: +1000 зарплата или -200 хлеб\n"
        "Все операции отображаются в одном активном окне этого чата."
    )
    today_key = get_today_key()
    msg_info = data.get("active_messages", {}).get(today_key, {}).get(str(chat_id))
    try:
        if msg_info:
            bot.edit_message_text(f"💬 Версия: {VERSION}\n\n{text}", chat_id=chat_id, message_id=msg_info, reply_markup=build_main_keyboard(chat_id))
        else:
            sent = bot.send_message(chat_id, f"💬 Версия: {VERSION}\n\n{text}", reply_markup=build_main_keyboard(chat_id))
            set_today_active_window(chat_id, sent.message_id)
    except Exception as e:
        log_error(f"Ошибка /start: {e}")
        try:
            sent = bot.send_message(chat_id, f"💬 Версия: {VERSION}\n\n{text}", reply_markup=build_main_keyboard(chat_id))
            set_today_active_window(chat_id, sent.message_id)
        except Exception as e2:
            log_error(f"Ошибка /start отправки fallback: {e2}")

@bot.message_handler(commands=["myid"])
def cmd_myid(msg):
    bot.reply_to(msg, f"🆔 Ваш user ID: {msg.from_user.id}")

@bot.message_handler(commands=["chatid"])
def cmd_chatid(msg):
    bot.reply_to(msg, f"💬 ID чата: {msg.chat.id}")

@bot.message_handler(commands=["groups"])
def cmd_groups(msg):
    chs = list(data.get("chats", {}).keys())
    if not chs:
        bot.reply_to(msg, "Нет сохранённых чатов.")
        return
    bot.reply_to(msg, "Сохранённые чаты (ID):\n" + "\n".join(chs))

@bot.message_handler(commands=["setforward"])
def cmd_setforward(msg):
    parts = msg.text.split()
    if str(msg.from_user.id) != str(OWNER_ID):
        bot.reply_to(msg, "Только владелец может управлять целями пересылки.")
        return
    if len(parts) < 2:
        bot.reply_to(msg, "Использование: /setforward <chat_id>")
        return
    try:
        cid = int(parts[1])
        if cid in data.get("forward_targets", []):
            bot.reply_to(msg, "Этот чат уже в списке пересылки.")
            return
        data.setdefault("forward_targets", []).append(cid)
        save_data()
        bot.reply_to(msg, f"✅ Цель пересылки добавлена: {cid}")
    except:
        bot.reply_to(msg, "Неверный chat_id (число).")

@bot.message_handler(commands=["removeforward"])
def cmd_removeforward(msg):
    parts = msg.text.split()
    if str(msg.from_user.id) != str(OWNER_ID):
        bot.reply_to(msg, "Только владелец может управлять целями пересылки.")
        return
    if len(parts) < 2:
        bot.reply_to(msg, "Использование: /removeforward <chat_id>")
        return
    try:
        cid = int(parts[1])
        if cid not in data.get("forward_targets", []):
            bot.reply_to(msg, "Этот чат не в списке пересылки.")
            return
        data["forward_targets"].remove(cid)
        save_data()
        bot.reply_to(msg, f"✅ Цель пересылки удалена: {cid}")
    except:
        bot.reply_to(msg, "Неверный chat_id (число).")

@bot.message_handler(commands=["listforwards"])
def cmd_listforwards(msg):
    targets = data.get("forward_targets", [])
    if not targets:
        bot.reply_to(msg, "Список пересылки пуст.")
        return
    bot.reply_to(msg, "📨 Цели пересылки:\n" + "\n".join(map(str, targets)))

@bot.message_handler(commands=["allowforward"])
def cmd_allowforward(msg):
    if str(msg.from_user.id) != str(OWNER_ID):
        bot.reply_to(msg, "Только владелец может добавлять разрешённых отправителей.")
        return
    parts = msg.text.split()
    if len(parts) < 2:
        bot.reply_to(msg, "Использование: /allowforward <user_id>")
        return
    try:
        uid = int(parts[1])
        if uid in data.get("allowed_forwarders", []):
            bot.reply_to(msg, "Пользователь уже в списке разрешённых.")
            return
        data.setdefault("allowed_forwarders", []).append(uid)
        save_data()
        bot.reply_to(msg, f"✅ Разрешён пересылать: {uid}")
    except:
        bot.reply_to(msg, "Неверный user_id.")

@bot.message_handler(commands=["disallowforward"])
def cmd_disallowforward(msg):
    if str(msg.from_user.id) != str(OWNER_ID):
        bot.reply_to(msg, "Только владелец может удалять разрешённых отправителей.")
        return
    parts = msg.text.split()
    if len(parts) < 2:
        bot.reply_to(msg, "Использование: /disallowforward <user_id>")
        return
    try:
        uid = int(parts[1])
        if uid not in data.get("allowed_forwarders", []):
            bot.reply_to(msg, "Пользователь не в списке.")
            return
        data["allowed_forwarders"].remove(uid)
        save_data()
        bot.reply_to(msg, f"✅ Удалён из разрешённых: {uid}")
    except:
        bot.reply_to(msg, "Неверный user_id.")

@bot.message_handler(commands=["about"])
def cmd_about(msg):
    bot.reply_to(msg, f"Финансовый бот «ФО» — версия {VERSION}\nОдно окно в день, редактирование и пересылка.")

# -----------------------------
# Webhook endpoint & root
# -----------------------------
@app.route(f"/{TOKEN}", methods=["POST"])
def webhook_entry():
    try:
        update = telebot.types.Update.de_json(request.get_data().decode("utf-8"))
        bot.process_new_updates([update])
    except Exception as e:
        log_error(f"Ошибка webhook_entry: {e}\n{traceback.format_exc()}")
    return "OK", 200

@app.route("/", methods=["GET"])
def index():
    return f"Финансовый бот «ФО» — версия {VERSION} работает", 200

# -----------------------------
# Установка webhook (и уведомления владельцу)
# -----------------------------
def set_webhook():
    url = f"{APP_URL.rstrip('/')}/{TOKEN}"
    try:
        bot.remove_webhook()
        time.sleep(0.5)
        bot.set_webhook(url=url)
        log_info(f"Webhook установлен: {url}")
        # уведомление владельцу
        try:
            if OWNER_ID:
                bot.send_message(int(OWNER_ID), f"🌐 Webhook активирован: {url}")
        except Exception as e:
            log_error(f"Не удалось уведомить владельца о webhook: {e}")
    except Exception as e:
        log_error(f"Не удалось установить webhook: {e}\n{traceback.format_exc()}")

# -----------------------------
# Планировщик смены дня: при смене даты создаёт новое окно для каждого чата (сохраняет общий баланс)
# -----------------------------
def schedule_daily_window_creation():
    def task():
        last_day = get_today_key()
        while True:
            try:
                time.sleep(60)
                current_day = get_today_key()
                if current_day != last_day:
                    # create new window for all chats (balances remain)
                    for chat_id_str in list(data.get("chats", {}).keys()):
                        try:
                            chat_id = int(chat_id_str)
                        except:
                            continue
                        try:
                            update_or_send_today_window(chat_id)
                        except Exception as e:
                            log_error(f"Ошибка при создании окна нового дня для {chat_id}: {e}\n{traceback.format_exc()}")
                    last_day = current_day
            except Exception as e:
                log_error(f"Ошибка в daily loop: {e}\n{traceback.format_exc()}")
                time.sleep(5)
    thread = threading.Thread(target=task, daemon=True)
    thread.start()

# -----------------------------
# Запуск
# -----------------------------
if __name__ == "__main__":
    set_webhook()
    schedule_daily_window_creation()
    log_info(f"Бот ФО версии {VERSION} запущен")
    # уведомление владельцу при старте
    try:
        if OWNER_ID:
            bot.send_message(int(OWNER_ID), "🤖 Бот успешно запущен!")
    except Exception as e:
        log_error(f"Не удалось уведомить владельца о запуске: {e}\n{traceback.format_exc()}")
    # Запускаем Flask (webhook)
    app.run(host="0.0.0.0", port=PORT)
