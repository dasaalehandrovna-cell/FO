# ------------------------------------------------------------
# Финансовый бот «ФО» — версия 9.8.1
# Полная безопасная сборка для Render (Flask + Webhook)
# Авторизация, пересылка, отчёты, автосоздание базы
# ------------------------------------------------------------

import os
import json
import csv
from datetime import datetime
from zoneinfo import ZoneInfo
from flask import Flask, request
import telebot
from telebot import types

# ------------------------------------------------------------
# НАСТРОЙКИ
# ------------------------------------------------------------
TOKEN = os.getenv("BOT_TOKEN")
OWNER_ID = int(os.getenv("OWNER_ID", "0"))
WEBHOOK_URL = os.getenv("WEBHOOK_URL")  # пример: https://fo-1.onrender.com
DATA_FILE = "data.json"
CSV_FILE = "data.csv"
PORT = int(os.environ.get("PORT", 5000))
TZ = ZoneInfo("America/Argentina/Catamarca")

if not TOKEN or not WEBHOOK_URL:
    raise ValueError("❌ Переменные окружения BOT_TOKEN и WEBHOOK_URL должны быть заданы!")

bot = telebot.TeleBot(TOKEN)
app = Flask(__name__)

VERSION = "9.8.1"

# ------------------------------------------------------------
# ФУНКЦИИ ХРАНЕНИЯ ДАННЫХ
# ------------------------------------------------------------

def default_data():
    return {
        "overall_balance": 0,
        "records": [],            # список всех записей
        "next_id": 1,
        "forward_targets": [],    # ID чатов, куда пересылаются сообщения
    }

def load_data():
    if not os.path.exists(DATA_FILE):
        save_data(default_data())
    with open(DATA_FILE, "r", encoding="utf-8") as f:
        return json.load(f)

def save_data(data):
    with open(DATA_FILE, "w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)

data = load_data()

# ------------------------------------------------------------
# ВСПОМОГАТЕЛЬНЫЕ
# ------------------------------------------------------------

def export_to_csv():
    with open(CSV_FILE, "w", newline="", encoding="utf-8") as f:
        writer = csv.writer(f)
        writer.writerow(["ID", "Дата", "Сумма", "Комментарий"])
        for r in data["records"]:
            writer.writerow([r["id"], r["timestamp"], r["amount"], r["note"]])

def format_record(rec):
    sign = "➕" if rec["amount"] > 0 else "➖"
    return f"{sign} {rec['amount']} — {rec['note']} ({rec['timestamp']})"

# ------------------------------------------------------------
# КОМАНДЫ
# ------------------------------------------------------------

@bot.message_handler(commands=["start"])
def start(message):
    bot.reply_to(
        message,
        f"👋 Привет! Это финансовый бот «ФО» (версия {VERSION}).\n"
        f"Добавляй записи вида:\n\n"
        f"  +1000 зарплата\n"
        f"  -200 хлеб\n\n"
        f"Команды:\n"
        f"/баланс — показать баланс\n"
        f"/отчет — список всех записей\n"
        f"/экспорт — выгрузка CSV\n"
        f"/сброс — очистить данные\n"
        f"/addforward — добавить пересылку\n"
        f"/removeforward — удалить пересылку\n"
        f"/listforwards — список пересылок"
    )

@bot.message_handler(commands=["баланс"])
def cmd_balance(message):
    bot.reply_to(message, f"💰 Текущий баланс: {data['overall_balance']}")

@bot.message_handler(commands=["отчет"])
def cmd_report(message):
    if not data["records"]:
        bot.reply_to(message, "📄 Записей пока нет.")
        return
    text = "\n".join(format_record(r) for r in data["records"][-20:])
    bot.reply_to(message, f"📊 Последние записи:\n\n{text}")

@bot.message_handler(commands=["экспорт"])
def cmd_export(message):
    export_to_csv()
    with open(CSV_FILE, "rb") as f:
        bot.send_document(message.chat.id, f, caption="Экспорт CSV выполнен ✅")

@bot.message_handler(commands=["сброс"])
def cmd_reset(message):
    if message.from_user.id != OWNER_ID:
        bot.reply_to(message, "⛔ Только владелец может сбросить данные.")
        return
    save_data(default_data())
    global data
    data = load_data()
    bot.reply_to(message, "✅ Данные успешно сброшены.")

# ------------------------------------------------------------
# УПРАВЛЕНИЕ ПЕРЕСЫЛКОЙ
# ------------------------------------------------------------

@bot.message_handler(commands=["addforward"])
def add_forward(message):
    if message.from_user.id != OWNER_ID:
        bot.reply_to(message, "⛔ Только владелец может управлять пересылкой.")
        return
    if not message.reply_to_message:
        bot.reply_to(message, "🔁 Ответьте на сообщение из чата, чтобы добавить пересылку.")
        return
    target_id = message.reply_to_message.chat.id
    if target_id not in data["forward_targets"]:
        data["forward_targets"].append(target_id)
        save_data(data)
        bot.reply_to(message, f"✅ Пересылка в чат {target_id} добавлена.")
    else:
        bot.reply_to(message, "⚠️ Этот чат уже есть в списке.")

@bot.message_handler(commands=["removeforward"])
def remove_forward(message):
    if message.from_user.id != OWNER_ID:
        bot.reply_to(message, "⛔ Только владелец может управлять пересылкой.")
        return
    if not message.reply_to_message:
        bot.reply_to(message, "🗑 Ответьте на сообщение из чата для удаления.")
        return
    target_id = message.reply_to_message.chat.id
    if target_id in data["forward_targets"]:
        data["forward_targets"].remove(target_id)
        save_data(data)
        bot.reply_to(message, f"✅ Пересылка в чат {target_id} удалена.")
    else:
        bot.reply_to(message, "❌ Этот чат не найден в списке пересылки.")

@bot.message_handler(commands=["listforwards"])
def list_forwards(message):
    if not data["forward_targets"]:
        bot.reply_to(message, "📭 Список пересылок пуст.")
        return
    text = "\n".join(str(cid) for cid in data["forward_targets"])
    bot.reply_to(message, f"📤 Активные пересылки:\n{text}")

# ------------------------------------------------------------
# ОБРАБОТКА ДОХОДОВ И РАСХОДОВ
# ------------------------------------------------------------

@bot.message_handler(func=lambda m: re.match(r"^[\+\-]?\d+", m.text.strip()))
def add_record(message):
    text = message.text.strip()
    sign = 1 if text.startswith("+") else -1 if text.startswith("-") else None
    num_match = re.match(r"^[\+\-]?(\d+)", text)
    if not num_match:
        bot.reply_to(message, "⚠️ Не удалось распознать сумму.")
        return

    amount = int(num_match.group(1)) * (sign if sign else 1)
    note = text[num_match.end():].strip() or "(без комментария)"

    new_record = {
        "id": data["next_id"],
        "timestamp": datetime.now(TZ).strftime("%Y-%m-%d %H:%M:%S"),
        "amount": amount,
        "note": note,
    }
    data["next_id"] += 1
    data["records"].append(new_record)
    data["overall_balance"] += amount
    save_data(data)

    bot.reply_to(message, f"✅ Добавлено: {format_record(new_record)}")
    for target in data["forward_targets"]:
        try:
            bot.forward_message(target, message.chat.id, message.message_id)
        except Exception:
            pass

# -------------------------
# Экспорт данных в CSV
# -------------------------
import csv
from datetime import datetime
from io import StringIO
import os

@bot.message_handler(commands=['экспорт'])
def export_csv(message):
    """Экспорт всех записей в CSV (доступно только владельцу)"""
    user_id = str(message.from_user.id)
    if str(message.from_user.id) != str(OWNER_ID):
        bot.reply_to(message, "⛔️ Только владелец может экспортировать данные.")
        return

    if not data["records"]:
        bot.reply_to(message, "📂 Нет данных для экспорта.")
        return

    # Имя файла с датой
    date_str = datetime.now(TZ).strftime("%Y-%m-%d_%H-%M")
    csv_filename = f"data_{date_str}.csv"

    # Создание CSV в памяти
    output = StringIO()
    writer = csv.writer(output)
    writer.writerow(["ID", "Дата", "Сумма", "Комментарий"])

    for rec in data["records"]:
        dt = datetime.fromisoformat(rec["timestamp"]).strftime("%Y-%m-%d %H:%M:%S")
        writer.writerow([rec["short_id"], dt, rec["amount"], rec["note"]])

    csv_content = output.getvalue()
    output.close()

    # Сохранение в файл
    with open(csv_filename, "w", encoding="utf-8", newline="") as f:
        f.write(csv_content)

    # Отправка файла владельцу
    with open(csv_filename, "rb") as f:
        bot.send_document(
            message.chat.id,
            f,
            caption=f"📊 Экспорт данных завершён.\nФайл: {csv_filename}"
        )

    # Очистка старых CSV (опционально)
    for file in os.listdir():
        if file.startswith("data_") and file.endswith(".csv") and file != csv_filename:
            try:
                os.remove(file)
            except Exception:
                pass

# ------------------------------------------------------------
# FLASK — WEBHOOK
# ------------------------------------------------------------

@app.route("/" + TOKEN, methods=["POST"])
def receive_update():
    update = telebot.types.Update.de_json(request.data.decode("utf-8"))
    bot.process_new_updates([update])
    return "OK", 200

@app.route("/", methods=["GET"])
def index():
    return f"🤖 Бот «ФО» v{VERSION} работает!", 200

# ------------------------------------------------------------
# АВТО-УСТАНОВКА WEBHOOK
# ------------------------------------------------------------
def set_webhook():
    bot.remove_webhook()
    bot.set_webhook(url=f"{WEBHOOK_URL}/{TOKEN}")

set_webhook()
print(f"✅ Webhook установлен: {WEBHOOK_URL}/{TOKEN}")
print(f"🚀 Бот «ФО» v{VERSION} запущен на порту {PORT}")

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=PORT)

# ------------------------------------------------------------
# Кол-во строк: 310
# ------------------------------------------------------------