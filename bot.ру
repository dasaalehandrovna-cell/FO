# === ФО БОТ №9.2 ===
# Исправлена логика вывода и удаления окон: всегда одно актуальное окно, задержка 5 сек
# Основные функции и кнопки сохранены из версии №9.1

from flask import Flask, request
import telebot
import json
import os
from datetime import datetime
import time
from threading import Thread
import csv

TOKEN = os.environ.get("BOT_TOKEN")
bot = telebot.TeleBot(TOKEN)
app = Flask(__name__)

DATA_FILE = "data.json"
CSV_FILE = "data.csv"


# --------------------- Работа с данными ---------------------
def load_data():
    if not os.path.exists(DATA_FILE):
        return {"records": [], "last_message_id": None, "last_date": None}
    with open(DATA_FILE, "r", encoding="utf-8") as f:
        return json.load(f)


def save_data(data):
    with open(DATA_FILE, "w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)


def export_csv(data):
    with open(CSV_FILE, "w", newline="", encoding="utf-8") as f:
        writer = csv.writer(f)
        writer.writerow(["ID", "Дата", "Время", "Сумма", "Комментарий", "Тип"])
        for r in data["records"]:
            writer.writerow([r["id"], r["date"], r["time"], r["amount"], r["desc"], r["type"]])


# --------------------- Вспомогательные функции ---------------------
def get_balance(records):
    return sum(float(r["amount"]) for r in records)


def format_record(r):
    return f"{r['time']} | {r['amount']} {r['desc']}"


def create_message_text(data):
    today = datetime.now().strftime("%Y-%m-%d")
    text = f"📅 {today}\n"
    if not data["records"]:
        text += "Нет данных за сегодня.\n"
    else:
        for r in data["records"]:
            text += f"{r['time']} | {r['amount']} {r['desc']}\n"
    text += f"\n💰 Баланс: {get_balance(data['records'])}"
    return text


def delete_message_with_delay(chat_id, message_id, delay=5):
    def delayed_delete():
        time.sleep(delay)
        try:
            bot.delete_message(chat_id, message_id)
        except Exception:
            pass
    Thread(target=delayed_delete).start()


# --------------------- Кнопки ---------------------
from telebot.types import InlineKeyboardMarkup, InlineKeyboardButton


def main_menu():
    kb = InlineKeyboardMarkup()
    kb.add(InlineKeyboardButton("✏️ Редактировать", callback_data="edit"))
    kb.add(InlineKeyboardButton("🗑 Сбросить всё", callback_data="reset"))
    kb.add(InlineKeyboardButton("📄 Экспорт CSV", callback_data="export"))
    return kb


def edit_menu():
    kb = InlineKeyboardMarkup()
    kb.add(InlineKeyboardButton("Удалить запись", callback_data="delete"))
    kb.add(InlineKeyboardButton("📁 Отправить файл", callback_data="export"))
    kb.add(InlineKeyboardButton("⬅️ Назад", callback_data="back"))
    return kb


def confirm_delete_menu(id):
    kb = InlineKeyboardMarkup()
    kb.add(
        InlineKeyboardButton("✅ Да", callback_data=f"confirm_delete_{id}"),
        InlineKeyboardButton("❌ Нет", callback_data="back"),
    )
    return kb


# --------------------- Обработка команд ---------------------
@bot.message_handler(commands=["start", "баланс", "отчет"])
def start_message(message):
    data = load_data()
    text = create_message_text(data)
    sent = bot.send_message(message.chat.id, text, reply_markup=main_menu())

    # Сначала выводим новое окно, потом удаляем предыдущее
    if data.get("last_message_id"):
        delete_message_with_delay(message.chat.id, data["last_message_id"])

    data["last_message_id"] = sent.message_id
    data["last_date"] = datetime.now().strftime("%Y-%m-%d")
    save_data(data)


# --------------------- Обработка кнопок ---------------------
@bot.callback_query_handler(func=lambda c: True)
def callback_handler(call):
    data = load_data()

    if call.data == "edit":
        bot.edit_message_reply_markup(call.message.chat.id, call.message.message_id, reply_markup=edit_menu())

    elif call.data == "back":
        bot.edit_message_reply_markup(call.message.chat.id, call.message.message_id, reply_markup=main_menu())

    elif call.data == "reset":
        bot.send_message(call.message.chat.id, "Вы уверены, что хотите удалить все данные?",
                         reply_markup=confirm_delete_menu("all"))

    elif call.data.startswith("confirm_delete_"):
        _, _, rec_id = call.data.partition("_")
        if rec_id == "all":
            data["records"] = []
        else:
            data["records"] = [r for r in data["records"] if str(r["id"]) != rec_id]
        save_data(data)

        # Обновляем окно
        text = create_message_text(data)
        sent = bot.send_message(call.message.chat.id, text, reply_markup=main_menu())

        # Удаляем старое окно
        delete_message_with_delay(call.message.chat.id, call.message.message_id)

        data["last_message_id"] = sent.message_id
        save_data(data)

    elif call.data == "delete":
        if not data["records"]:
            bot.answer_callback_query(call.id, "Нет записей для удаления.")
            return
        kb = InlineKeyboardMarkup()
        for r in data["records"]:
            kb.add(InlineKeyboardButton(format_record(r), callback_data=f"confirm_delete_{r['id']}"))
        bot.edit_message_reply_markup(call.message.chat.id, call.message.message_id, reply_markup=kb)

    elif call.data == "export":
        export_csv(data)
        with open(CSV_FILE, "rb") as f:
            bot.send_document(call.message.chat.id, f)


# --------------------- Обработка транзакций ---------------------
@bot.message_handler(func=lambda m: True)
def handle_transaction(message):
    lines = message.text.strip().split("\n")
    data = load_data()
    added_any = False

    for line in lines:
        parts = line.split(maxsplit=1)
        if not parts:
            continue
        amount_str = parts[0]
        desc = parts[1] if len(parts) > 1 else ""
        if amount_str.startswith("+") or amount_str.startswith("-"):
            amount = float(amount_str)
        else:
            continue

        now = datetime.now()
        record = {
            "id": len(data["records"]) + 1,
            "date": now.strftime("%Y-%m-%d"),
            "time": now.strftime("%H:%M:%S"),
            "amount": amount,
            "desc": desc,
            "type": "приход" if amount > 0 else "расход"
        }
        data["records"].append(record)
        added_any = True

    # Создаём новое окно в любом случае
    save_data(data)
    text = create_message_text(data)
    sent = bot.send_message(message.chat.id, text, reply_markup=main_menu())

    # После создания нового окна удаляем старое
    if data.get("last_message_id"):
        delete_message_with_delay(message.chat.id, data["last_message_id"])

    data["last_message_id"] = sent.message_id
    save_data(data)


# --------------------- Flask webhook ---------------------
@app.route(f"/{TOKEN}", methods=["POST"])
def webhook():
    bot.process_new_updates([telebot.types.Update.de_json(request.data.decode("utf-8"))])
    return "!", 200


@app.route("/", methods=["GET"])
def index():
    bot.remove_webhook()
    bot.set_webhook(url=f"https://{request.host}/{TOKEN}")
    return "ФО БОТ №9.2 запущен!", 200


# --------------------- Запуск ---------------------
if __name__ == "__main__":
    app.run(host="0.0.0.0", port=int(os.environ.get("PORT", 5000)))