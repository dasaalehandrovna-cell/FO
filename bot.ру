# ---------- БОТ "ФО" v10 ----------
# Flask + webhook
# Содержит функционал из v9.5 + систему пересылки
# ----------------------------------------------

import os
import json
from flask import Flask, request
import telebot
from telebot.types import InlineKeyboardMarkup, InlineKeyboardButton

TOKEN = os.getenv("BOT_TOKEN")
bot = telebot.TeleBot(TOKEN)
app = Flask(__name__)

DATA_FILE = "data.json"


# --------------------- ВСПОМОГАТЕЛЬНЫЕ ---------------------

def load_data():
    if not os.path.exists(DATA_FILE):
        return {"entries": {}, "forward_chat_id": None, "next_id": 1}
    with open(DATA_FILE, "r", encoding="utf-8") as f:
        return json.load(f)


def save_data(data):
    with open(DATA_FILE, "w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)


def calculate_balance(data):
    return sum(entry["amount"] for entry in data["entries"].values())


# --------------------- МЕНЮ ---------------------

def main_menu():
    markup = InlineKeyboardMarkup()
    markup.row(
        InlineKeyboardButton("📊 Баланс", callback_data="balance"),
        InlineKeyboardButton("📋 Отчёт", callback_data="report")
    )
    markup.row(
        InlineKeyboardButton("📝 Редактировать", callback_data="edit"),
        InlineKeyboardButton("♻️ Сброс", callback_data="reset")
    )
    markup.add(InlineKeyboardButton("🔁 Пересылка", callback_data="forward_menu"))
    return markup


def forward_menu():
    markup = InlineKeyboardMarkup()
    markup.add(InlineKeyboardButton("🔧 Установить пересылку", callback_data="set_forward"))
    markup.add(InlineKeyboardButton("❌ Отключить пересылку", callback_data="disable_forward"))
    markup.add(InlineKeyboardButton("ℹ️ Проверить текущую", callback_data="check_forward"))
    markup.add(InlineKeyboardButton("⬅️ Назад", callback_data="back_main"))
    return markup


# --------------------- ОСНОВНОЕ ---------------------

@bot.message_handler(content_types=['text', 'photo', 'document', 'video', 'voice', 'audio', 'sticker'])
def handle_message(message):
    data = load_data()

    # --- ПЕРЕСЫЛКА ---
    if data.get("forward_chat_id"):
        try:
            bot.forward_message(
                data["forward_chat_id"],
                message.chat.id,
                message.message_id
            )
        except Exception as e:
            bot.send_message(message.chat.id, f"⚠️ Ошибка пересылки: {e}")

    # --- ОБРАБОТКА ЗАПИСЕЙ ---
    if message.content_type == "text":
        text = message.text.strip()
        if text.startswith("+") or text.startswith("-"):
            try:
                amount_str, *desc_parts = text.split()
                amount = float(amount_str.replace("+", ""))
                if text.startswith("-"):
                    amount = -amount
                desc = " ".join(desc_parts)

                entry_id = str(data["next_id"])
                data["entries"][entry_id] = {"id": entry_id, "amount": amount, "desc": desc}
                data["next_id"] += 1
                save_data(data)

                bot.send_message(message.chat.id, f"✅ Записано (ID {entry_id})", reply_markup=main_menu())
            except Exception:
                bot.send_message(message.chat.id, "⚠️ Неверный формат. Пример: +1000 хлеб", reply_markup=main_menu())
        else:
            bot.send_message(message.chat.id, "📋 Выберите действие:", reply_markup=main_menu())
    else:
        bot.send_message(message.chat.id, "📋 Файл получен.", reply_markup=main_menu())


# --------------------- CALLBACKS ---------------------

@bot.callback_query_handler(func=lambda call: True)
def handle_callback(call):
    data = load_data()
    chat_id = call.message.chat.id

    # --- Основное меню ---
    if call.data == "balance":
        balance = calculate_balance(data)
        bot.edit_message_text(f"💰 Баланс: {balance}", chat_id, call.message.id, reply_markup=main_menu())

    elif call.data == "report":
        if not data["entries"]:
            text = "Пока нет записей."
        else:
            text = "\n".join(
                [f"ID {e['id']}: {'+' if e['amount'] > 0 else ''}{e['amount']} {e['desc']}" for e in data["entries"].values()]
            )
        bot.edit_message_text(f"📋 Отчёт:\n{text}", chat_id, call.message.id, reply_markup=main_menu())

    elif call.data == "edit":
        if not data["entries"]:
            bot.edit_message_text("Нет записей для редактирования.", chat_id, call.message.id, reply_markup=main_menu())
        else:
            markup = InlineKeyboardMarkup()
            for e in data["entries"].values():
                text = f"{e['id']}: {e['amount']} {e['desc']}"
                markup.add(InlineKeyboardButton(text, callback_data=f"edit_{e['id']}"))
            markup.add(InlineKeyboardButton("⬅️ Назад", callback_data="back_main"))
            bot.edit_message_text("Выбери запись для редактирования:", chat_id, call.message.id, reply_markup=markup)

    elif call.data.startswith("edit_"):
        entry_id = call.data.split("_")[1]
        msg = bot.send_message(chat_id, f"✏️ Введи новые данные для ID {entry_id} (пример: +500 хлеб):")
        bot.register_next_step_handler(msg, edit_entry, entry_id)

    elif call.data == "reset":
        data["entries"] = {}
        data["next_id"] = 1
        save_data(data)
        bot.edit_message_text("♻️ Все данные сброшены.", chat_id, call.message.id, reply_markup=main_menu())

    # --- Меню пересылки ---
    elif call.data == "forward_menu":
        bot.edit_message_text("🔁 Управление пересылкой:", chat_id, call.message.id, reply_markup=forward_menu())

    elif call.data == "set_forward":
        msg = bot.send_message(chat_id, "🔧 Отправь мне сообщение из чата, куда пересылать (или напиши chat_id):")
        bot.register_next_step_handler(msg, set_forward_chat)

    elif call.data == "disable_forward":
        data["forward_chat_id"] = None
        save_data(data)
        bot.edit_message_text("❌ Пересылка отключена.", chat_id, call.message.id, reply_markup=main_menu())

    elif call.data == "check_forward":
        fwd = data.get("forward_chat_id")
        if fwd:
            text = f"📨 Текущий чат для пересылки: `{fwd}`"
        else:
            text = "🔕 Пересылка не установлена."
        bot.edit_message_text(text, chat_id, call.message.id, parse_mode="Markdown", reply_markup=forward_menu())

    elif call.data == "back_main":
        bot.edit_message_text("📋 Главное меню:", chat_id, call.message.id, reply_markup=main_menu())


# --------------------- РЕДАКТИРОВАНИЕ ---------------------

def edit_entry(message, entry_id):
    data = load_data()
    if entry_id not in data["entries"]:
        bot.send_message(message.chat.id, "⚠️ Запись не найдена.", reply_markup=main_menu())
        return

    try:
        amount_str, *desc_parts = message.text.split()
        amount = float(amount_str.replace("+", ""))
        if message.text.startswith("-"):
            amount = -amount
        desc = " ".join(desc_parts)
        data["entries"][entry_id] = {"id": entry_id, "amount": amount, "desc": desc}
        save_data(data)
        bot.send_message(message.chat.id, f"✅ Запись {entry_id} обновлена.", reply_markup=main_menu())
    except Exception:
        bot.send_message(message.chat.id, "⚠️ Ошибка формата. Пример: +500 хлеб", reply_markup=main_menu())


# --------------------- ПЕРЕСЫЛКА ---------------------

def set_forward_chat(message):
    data = load_data()
    chat_id = message.chat.id

    if message.forward_from_chat:
        forward_chat_id = message.forward_from_chat.id
    else:
        try:
            forward_chat_id = int(message.text.strip())
        except:
            bot.send_message(chat_id, "⚠️ Неверный chat_id. Попробуй снова.")
            return

    data["forward_chat_id"] = forward_chat_id
    save_data(data)
    bot.send_message(chat_id, f"✅ Пересылка установлена на chat_id: `{forward_chat_id}`", parse_mode="Markdown", reply_markup=main_menu())


# --------------------- FLASK WEBHOOK ---------------------

@app.route(f"/{TOKEN}", methods=["POST"])
def webhook():
    update = telebot.types.Update.de_json(request.stream.read().decode("utf-8"))
    bot.process_new_updates([update])
    return "ok"


@app.route("/", methods=["GET"])
def index():
    return "Бот работает!"


if __name__ == "__main__":
    app.run(host="0.0.0.0", port=int(os.getenv("PORT", 5000)))