# ============================================
# Финансовый бот — версия 9.8.5
# ID: Code_001
# С автоудалением вспомогательных сообщений (30 сек)
# ============================================

import telebot
from telebot import types
from flask import Flask, request
import os, json, threading, time

# -----------------------------
# ОТСЕК 1 — Настройки
# -----------------------------
TOKEN = os.getenv("BOT_TOKEN", "YOUR_BOT_TOKEN")
APP_URL = os.getenv("APP_URL", "https://your-app-url.onrender.com")
PORT = int(os.getenv("PORT", 5000))
VERSION = "9.8.5"
DATA_FILE = "finance_data.json"
CSV_FILE = "finance_data.csv"
TEMP_MSG_LIFETIME = 30  # автоудаление временных сообщений через 30 секунд

bot = telebot.TeleBot(TOKEN)
app = Flask(__name__)

# -----------------------------
# ОТСЕК 2 — Работа с данными
# -----------------------------
def load_data():
    if not os.path.exists(DATA_FILE):
        with open(DATA_FILE, "w", encoding="utf-8") as f:
            json.dump({"chats": {}}, f, ensure_ascii=False, indent=2)
    with open(DATA_FILE, "r", encoding="utf-8") as f:
        return json.load(f)

def save_data(d):
    with open(DATA_FILE, "w", encoding="utf-8") as f:
        json.dump(d, f, ensure_ascii=False, indent=2)

data = load_data()

def get_chat_store(chat_id):
    str_id = str(chat_id)
    if str_id not in data["chats"]:
        data["chats"][str_id] = {"balance": 0, "records": [], "next_id": 1, "forward_enabled": False}
    return data["chats"][str_id]

# -----------------------------
# ОТСЕК 3 — Автоудаление сообщений
# -----------------------------
def schedule_auto_delete(chat_id, message_id, delay=TEMP_MSG_LIFETIME):
    def delete_later():
        time.sleep(delay)
        try:
            bot.delete_message(chat_id, message_id)
        except:
            pass
    threading.Thread(target=delete_later, daemon=True).start()

def send_temp_message(chat_id, text):
    msg = bot.send_message(chat_id, text)
    schedule_auto_delete(chat_id, msg.message_id)

# -----------------------------
# ОТСЕК 4 — Клавиатуры
# -----------------------------
def build_main_keyboard(chat_id, confirm_reset=False):
    kb = types.InlineKeyboardMarkup(row_width=2)
    if confirm_reset:
        kb.add(
            types.InlineKeyboardButton("✅ Да", callback_data="confirm_reset"),
            types.InlineKeyboardButton("❌ Нет", callback_data="cancel_reset")
        )
        return kb

    kb.add(
        types.InlineKeyboardButton("💰 Баланс", callback_data="btn_balance"),
        types.InlineKeyboardButton("📄 Отчёт", callback_data="btn_report"),
    )
    kb.add(
        types.InlineKeyboardButton("📂 CSV", callback_data="btn_csv"),
        types.InlineKeyboardButton("⚙️ Форвард", callback_data="btn_toggle_forward"),
    )
    kb.add(
        types.InlineKeyboardButton("✏ Изменить", callback_data="btn_edit_list"),
        types.InlineKeyboardButton("🧹 Обнулить", callback_data="btn_reset")
    )
    kb.add(types.InlineKeyboardButton("ℹ️ Версия", callback_data="btn_cod"))
    return kb

# -----------------------------
# ОТСЕК 5 — Основное окно
# -----------------------------
def update_or_send_today_window(chat_id):
    store = get_chat_store(chat_id)
    text = (
        f"💰 Баланс: {store['balance']:.2f} ₽\n"
        f"📊 Записей: {len(store['records'])}\n"
        f"🔄 Форвард: {'вкл' if store.get('forward_enabled') else 'выкл'}"
    )
    kb = build_main_keyboard(chat_id)
    bot.send_message(chat_id, text, reply_markup=kb)

# -----------------------------
# ОТСЕК 6 — Команды
# -----------------------------
@bot.message_handler(commands=["start"])
def cmd_start(message):
    chat_id = message.chat.id
    send_temp_message(chat_id, "✅ Бот запущен и готов к работе!")
    update_or_send_today_window(chat_id)

@bot.message_handler(commands=["reset"])
def cmd_reset(message):
    chat_id = message.chat.id
    kb = build_main_keyboard(chat_id, confirm_reset=True)
    msg = bot.send_message(chat_id, "Вы точно хотите обнулить данные?", reply_markup=kb)
    schedule_auto_delete(chat_id, msg.message_id)

# -----------------------------
# ОТСЕК 7 — Обработка callback
# -----------------------------
@bot.callback_query_handler(func=lambda call: True)
def callback_handler(call):
    chat_id = call.message.chat.id
    data_store = get_chat_store(chat_id)
    cb = call.data

    if cb == "btn_balance":
        update_or_send_today_window(chat_id)

    elif cb == "btn_report":
        send_temp_message(chat_id, "📊 Отчёт пока не реализован.")

    elif cb == "btn_csv":
        try:
            with open(CSV_FILE, "rb") as f:
                bot.send_document(chat_id, f)
        except Exception as e:
            send_temp_message(chat_id, f"Ошибка CSV: {e}")

    elif cb == "btn_toggle_forward":
        data_store["forward_enabled"] = not data_store.get("forward_enabled", False)
        save_data(data)
        status = "включён" if data_store["forward_enabled"] else "выключен"
        send_temp_message(chat_id, f"Форвард {status}")
        update_or_send_today_window(chat_id)

    elif cb == "btn_edit_list":
        send_temp_message(chat_id, "✏ Функция редактирования пока не реализована.")

    elif cb == "btn_reset":
        kb = build_main_keyboard(chat_id, confirm_reset=True)
        m = bot.send_message(chat_id, "Вы уверены, что хотите обнулить данные?", reply_markup=kb)
        schedule_auto_delete(chat_id, m.message_id)

    elif cb == "confirm_reset":
        data["chats"][str(chat_id)] = {"balance": 0, "records": [], "next_id": 1, "forward_enabled": False}
        save_data(data)
        send_temp_message(chat_id, "✅ Данные успешно обнулены.")
        update_or_send_today_window(chat_id)

    elif cb == "cancel_reset":
        send_temp_message(chat_id, "❌ Обнуление отменено.")
        update_or_send_today_window(chat_id)

    elif cb == "btn_cod":
        send_temp_message(chat_id, f"🤖 Финансовый бот — версия {VERSION}")

    elif cb == "btn_start":
        update_or_send_today_window(chat_id)

    else:
        send_temp_message(chat_id, "⚠️ Неизвестная команда")

# -----------------------------
# ОТСЕК 8 — Вебхук
# -----------------------------
@app.route(f"/{TOKEN}", methods=["POST"])
def webhook():
    update = telebot.types.Update.de_json(request.get_data().decode("utf-8"))
    bot.process_new_updates([update])
    return "", 200

@app.route("/")
def index():
    return "✅ Бот работает"

# -----------------------------
# ОТСЕК 9 — Запуск
# -----------------------------
if __name__ == "__main__":
    bot.remove_webhook()
    bot.set_webhook(url=f"{APP_URL}/{TOKEN}")
    print(f"Webhook установлен: {APP_URL}/{TOKEN}")
    app.run(host="0.0.0.0", port=PORT)