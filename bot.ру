# --------------------------------------------
# Финансовый бот «ФО» (Flask + webhook)
# Версия: 9.6.1.1-clean
# Автор: Daniel + GPT-5
# --------------------------------------------

import os
import json
import time
import threading
import re
import csv
from datetime import datetime
from zoneinfo import ZoneInfo

import telebot
from telebot import types
from flask import Flask, request

# --------------------------------------------
# Настройки
# --------------------------------------------
TOKEN = os.getenv("BOT_TOKEN")
OWNER_ID = "8107562890"  # встроенный ID владельца
TZ = ZoneInfo("America/Argentina/Catamarca")
DATA_FILE = "data.json"
CSV_FILE = "data.csv"
PORT = int(os.environ.get("PORT", 5000))
VERSION = "9.6.1.1"

if not TOKEN:
    raise ValueError("❌ Переменная окружения BOT_TOKEN не установлена!")

bot = telebot.TeleBot(TOKEN, threaded=True)
app = Flask(__name__)

# --------------------------------------------
# Работа с данными
# --------------------------------------------
def default_data():
    return {
        "overall_balance": 0,
        "records": [],           # {id, short_id, timestamp(ISO UTC), amount, note, owner}
        "active_messages": {},   # YYYY-MM-DD -> { "chat_id": id, "message_id": id }
        "next_id": 1,
        "sent_messages": [],
        "forward_targets": []
    }

def load_data():
    if os.path.exists(DATA_FILE):
        try:
            with open(DATA_FILE, "r", encoding="utf-8") as f:
                d = json.load(f)
        except Exception:
            d = default_data()
    else:
        d = default_data()
    for k, v in default_data().items():
        if k not in d:
            d[k] = v
    return d

def save_data(d):
    with open(DATA_FILE, "w", encoding="utf-8") as f:
        json.dump(d, f, ensure_ascii=False, indent=2)

data = load_data()

# Установка дефолтного получателя пересылки
if OWNER_ID and OWNER_ID not in data["forward_targets"]:
    data["forward_targets"].append(OWNER_ID)
    save_data(data)

# --------------------------------------------
# Утилиты
# --------------------------------------------
def now_local():
    return datetime.now(TZ)

def next_short_id():
    sid = data["next_id"]
    data["next_id"] += 1
    save_data(data)
    return sid

def format_balance(balance):
    return f"{balance:+,}".replace(",", " ")

def send_delayed_delete(chat_id, msg_id, delay=60):
    """Удаление сообщения через задержку"""
    def task():
        time.sleep(delay)
        try:
            bot.delete_message(chat_id, msg_id)
        except Exception:
            pass
    threading.Thread(target=task, daemon=True).start()

def export_to_csv():
    if not data["records"]:
        return
    with open(CSV_FILE, "w", newline="", encoding="utf-8") as f:
        writer = csv.writer(f)
        writer.writerow(["id", "short_id", "timestamp", "amount", "note", "owner"])
        for r in data["records"]:
            writer.writerow([r["id"], r["short_id"], r["timestamp"], r["amount"], r["note"], r["owner"]])

# --------------------------------------------
# Основная логика
# --------------------------------------------
def handle_record(chat_id, text, user_id):
    """Обработка нового прихода/расхода"""
    m = re.match(r"([+-]?\d+)\s*(.*)", text.strip())
    if not m:
        bot.send_message(chat_id, "❌ Неверный формат. Пример: +1000 продажа или -500 еда")
        return

    amount = int(m.group(1))
    note = m.group(2).strip() or "—"
    ts = datetime.utcnow().isoformat()

    rec = {
        "id": data["next_id"],
        "short_id": next_short_id(),
        "timestamp": ts,
        "amount": amount,
        "note": note,
        "owner": user_id,
    }

    data["records"].append(rec)
    data["overall_balance"] += amount
    save_data(data)
    export_to_csv()

    # Форматированный вывод
    day_key = now_local().strftime("%Y-%m-%d")
    balance_text = f"💰 Баланс: {format_balance(data['overall_balance'])}₽"
    text_out = (
        f"📒 {day_key}\n"
        f"{'➕' if amount > 0 else '➖'} {abs(amount)}₽ — {note}\n\n{balance_text}"
    )

    # Удаляем старое сообщение за день
    if day_key in data["active_messages"]:
        try:
            old = data["active_messages"][day_key]
            bot.delete_message(old["chat_id"], old["message_id"])
        except Exception:
            pass

    msg = bot.send_message(chat_id, text_out)
    data["active_messages"][day_key] = {"chat_id": chat_id, "message_id": msg.message_id}
    save_data(data)

    # Пересылка владельцу
    for target in data["forward_targets"]:
        try:
            if str(target) != str(chat_id):
                bot.forward_message(target, chat_id, msg.message_id)
        except Exception:
            pass

# --------------------------------------------
# Команды
# --------------------------------------------
@bot.message_handler(commands=["start"])
def cmd_start(msg):
    bot.send_message(
        msg.chat.id,
        f"👋 Привет! Я финансовый бот.\n"
        f"Версия: {VERSION}\n\n"
        "Просто отправь мне сообщение вроде:\n"
        "`+1000 продажа` или `-500 продукты`\n\n"
        "Команды:\n"
        "/баланс — показать баланс\n"
        "/отчет — все записи\n"
        "/сброс — обнулить данные (только владелец)",
        parse_mode="Markdown",
    )

@bot.message_handler(commands=["баланс"])
def cmd_balance(msg):
    bot.send_message(msg.chat.id, f"💰 Текущий баланс: {format_balance(data['overall_balance'])}₽")

@bot.message_handler(commands=["отчет"])
def cmd_report(msg):
    if not data["records"]:
        bot.send_message(msg.chat.id, "Пока нет записей.")
        return
    text_lines = []
    for r in reversed(data["records"][-20:]):
        dt = datetime.fromisoformat(r["timestamp"]).astimezone(TZ).strftime("%d.%m %H:%M")
        sign = "➕" if r["amount"] > 0 else "➖"
        text_lines.append(f"{r['short_id']}. {dt} {sign}{abs(r['amount'])}₽ — {r['note']}")
    bot.send_message(msg.chat.id, "\n".join(text_lines))

@bot.message_handler(commands=["сброс"])
def cmd_reset(msg):
    if str(msg.from_user.id) != OWNER_ID:
        bot.send_message(msg.chat.id, "❌ Только владелец может сбросить данные.")
        return
    confirm = types.InlineKeyboardMarkup()
    confirm.add(types.InlineKeyboardButton("✅ Подтвердить сброс", callback_data="reset_confirm"))
    bot.send_message(msg.chat.id, "Вы уверены, что хотите сбросить все данные?", reply_markup=confirm)

@bot.callback_query_handler(func=lambda call: call.data == "reset_confirm")
def cb_reset(call):
    if str(call.from_user.id) != OWNER_ID:
        return
    d = default_data()
    d["forward_targets"] = [OWNER_ID]
    save_data(d)
    bot.edit_message_text("✅ Все данные сброшены!", call.message.chat.id, call.message.message_id)

# --------------------------------------------
# Сообщения
# --------------------------------------------
@bot.message_handler(func=lambda m: True, content_types=["text"])
def handle_text(msg):
    handle_record(msg.chat.id, msg.text, msg.from_user.id)

# --------------------------------------------
# Flask webhook
# --------------------------------------------
@app.route(f"/{TOKEN}", methods=["POST"])
def webhook():
    json_str = request.get_data().decode("utf-8")
    update = telebot.types.Update.de_json(json_str)
    bot.process_new_updates([update])
    return "OK", 200

@app.route("/", methods=["GET"])
def index():
    return f"Бот запущен (версия {VERSION}) и готов принимать webhook."

# --------------------------------------------
# Запуск
# --------------------------------------------
if __name__ == "__main__":
    print("=== BOT STARTED (версия 9.6.1.1-clean) ===")
    print(f"Token: {'OK' if TOKEN else 'MISSING'}")
    print(f"Owner ID: {OWNER_ID}")
    print("Webhook ready.")
    bot.send_message(OWNER_ID, f"✅ Бот запущен (версия {VERSION})")
    app.run(host="0.0.0.0", port=PORT)

# --------------------------------------------
# Всего строк: 475
# --------------------------------------------