# 9.8.6 fo_4 — чистые окна при навигации, выбор дня кнопкой, CSV за день, «Сегодня»
# 1241 строк
#============================================
# Финансовый бот — версия 9.8.6-fo_4
# Обновлено:
#  • Навигация (⬅️/➡️/📅 Сегодня/Открыть) создаёт новое окно дня и удаляет прежние окна (цели и источника)
#  • Кнопка «📆 Выбрать день» с ручным вводом даты YYYY-MM-DD
#  • Поддержка /view, /prev, /next, CSV за день, форвард, пересчёт балансов
# ============================================

import os
import json
import csv
import logging
import re
import threading
import time
from datetime import datetime, timedelta
from zoneinfo import ZoneInfo

import requests
import telebot
from flask import Flask, request
from telebot import types

# --- Конфигурация ---
TOKEN = os.getenv("BOT_TOKEN")
OWNER_ID = os.getenv("OWNER_ID")  # строка с числом или None
APP_URL = os.getenv("APP_URL", "https://yourapp.onrender.com")
TZ = ZoneInfo("America/Argentina/Catamarca")
DATA_FILE = "data.json"
CSV_FILE = "data.csv"
LOG_FILE = "log.txt"
PORT = int(os.environ.get("PORT", 5000))
VERSION = "9.8.6-fo_4"

# Keep-alive
KEEP_ALIVE_INTERVAL_SECONDS = int(os.getenv("KEEP_ALIVE_INTERVAL_SECONDS", 11 * 60))
KEEP_ALIVE_SEND_TO_OWNER = True

# Управление поведением навигации
DELETE_SOURCE_WINDOW_ON_NAV = True   # при переходе удалять окно исходного дня
DELETE_TARGET_OLD_WINDOW_ON_NAV = True  # при переходе удалять прежнее окно выбранного дня
ALWAYS_CREATE_NEW_ON_NAV = True      # при навигации не редактировать, а создавать новое сообщение

if not TOKEN:
    raise ValueError("BOT_TOKEN не установлен!")

bot = telebot.TeleBot(TOKEN, threaded=True)
app = Flask(__name__)

# -------- Автоудаление и вспомогательные --------
def delete_message_safe(chat_id, message_id):
    try:
        bot.delete_message(chat_id, message_id)
    except:
        pass

def send_temp_message(chat_id, text, delay=5, **kwargs):
    """Отправка временного сообщения, которое удаляется через delay секунд. Возвращает объект сообщения."""
    try:
        msg = bot.send_message(chat_id, text, **kwargs)
        if delay and delay > 0:
            threading.Timer(delay, lambda: delete_message_safe(chat_id, msg.message_id)).start()
        return msg
    except Exception as e:
        logging.error(f"send_temp_message: не удалось отправить/удалить сообщение: {e}")
        return None

def today_key():
    return datetime.now(TZ).strftime("%Y-%m-%d")

def parse_date_key(s: str):
    try:
        return datetime.strptime(s, "%Y-%m-%d").date()
    except Exception:
        return None

def shift_date_key(day_key: str, days: int):
    d = parse_date_key(day_key)
    if not d:
        d = datetime.now(TZ).date()
    d2 = d + timedelta(days=days)
    return d2.strftime("%Y-%m-%d")

def fmt_num(value):
    try:
        return f"{value:,.0f}".replace(",", ".")
    except Exception:
        return str(value)

# -----------------------------
# Логирование
# -----------------------------
logging.basicConfig(
    level=logging.INFO,
    format="[{asctime}] {levelname}: {message}",
    style="{",
    handlers=[logging.FileHandler(LOG_FILE, encoding="utf-8"), logging.StreamHandler()]
)
def log_info(msg): logging.info(msg)
def log_error(msg): logging.error(msg)
log_info(f"Старт приложения. Версия {VERSION}")

# -----------------------------
# Структура данных и IO
# -----------------------------
def default_data():
    return {
        "overall_balance": 0,
        "records": [],
        "chats": {},
        "active_messages": {},  # {day_key: {chat_id: message_id}}
        "processed_messages": [],
        "forward_targets": [],
        "tracked_messages": {},
        "next_id": 1
    }

def _load_json(path, fallback):
    try:
        if os.path.exists(path):
            with open(path, "r", encoding="utf-8") as f:
                return json.load(f)
    except Exception as e:
        log_error(f"_load_json: {path}: {e}")
    return fallback

def _save_json(path, payload):
    try:
        with open(path, "w", encoding="utf-8") as f:
            json.dump(payload, f, ensure_ascii=False, indent=2)
    except Exception as e:
        log_error(f"_save_json: {path}: {e}")

def load_data():
    d = _load_json(DATA_FILE, default_data())
    base = default_data()
    for k, v in base.items():
        if k not in d:
            d[k] = v
    # миграция active_messages (если была старая форма)
    if isinstance(d.get("active_messages"), dict):
        for k, v in list(d["active_messages"].items()):
            if not isinstance(v, dict) or (len(k) != 10 or "-" not in k):
                d["active_messages"] = {}
                break
    return d

def save_data(d):
    _save_json(DATA_FILE, d)

data = load_data()

# если задан OWNER_ID — добавим его в глобальные цели (совместимость со старым кодом)
if OWNER_ID:
    try:
        oi = int(OWNER_ID)
        if oi not in data.get("forward_targets", []):
            data["forward_targets"].append(oi)
            save_data(data)
    except Exception:
        pass

# -----------------------------
# Вспомогательные хранилища/окна
# -----------------------------
def get_chat_store(chat_id):
    cid = str(chat_id)
    if cid not in data["chats"]:
        data["chats"][cid] = {
            "balance": 0,
            "records": [],
            "next_id": 1,
            "daily_records": {},        # {day_key: [records]}
            "active_windows": {},       # для совместимости
            "edit_wait": None,
            "edit_target": None,
            "forward_enabled": False,
            "forward_target_id": None,
            "current_view_day": today_key()  # запоминаем последний открытый день
        }
        save_data(data)
    store = data["chats"][cid]
    if "forward_enabled" not in store: store["forward_enabled"] = False
    if "forward_target_id" not in store: store["forward_target_id"] = None
    if "current_view_day" not in store: store["current_view_day"] = today_key()
    save_data(data)
    return store

def get_active_window_id(chat_id, day_key):
    return data.setdefault("active_messages", {}).setdefault(day_key, {}).get(str(chat_id))

def set_active_window(chat_id, day_key, message_id):
    data.setdefault("active_messages", {}).setdefault(day_key, {})[str(chat_id)] = message_id
    save_data(data)

def clear_active_window(chat_id, day_key):
    am = data.setdefault("active_messages", {}).setdefault(day_key, {})
    if str(chat_id) in am:
        am.pop(str(chat_id), None)
        save_data(data)

def delete_active_window_if_exists(chat_id, day_key):
    old_id = get_active_window_id(chat_id, day_key)
    if old_id:
        delete_message_safe(chat_id, old_id)
        clear_active_window(chat_id, day_key)

def set_current_view_day(chat_id, day_key):
    store = get_chat_store(chat_id)
    store["current_view_day"] = day_key
    save_data(data)

def get_current_view_day(chat_id):
    store = get_chat_store(chat_id)
    return store.get("current_view_day", today_key())

# -----------------------------
# CSV
# -----------------------------
def export_to_csv(d):
    """Полный экспорт всех чатов/дней -> data.csv"""
    try:
        with open(CSV_FILE, "w", newline='', encoding="utf-8") as f:
            writer = csv.writer(f)
            writer.writerow(["chat_id","ID","short_id","timestamp","amount","note","owner","day_key"])
            for cid, cdata in d.get("chats", {}).items():
                for day_key, records in cdata.get("daily_records", {}).items():
                    for r in records:
                        writer.writerow([cid, r.get("id"), r.get("short_id"), r.get("timestamp"),
                                         r.get("amount"), r.get("note"), r.get("owner"), day_key])
    except Exception as e:
        log_error(f"Ошибка экспорта CSV: {e}")

def export_day_csv(chat_id, day_key):
    """Экспорт одного дня конкретного чата -> data_YYYY-MM-DD.csv (в корне)"""
    store = get_chat_store(chat_id)
    day_records = store.get("daily_records", {}).get(day_key, [])
    filename = f"data_{day_key}.csv"
    try:
        with open(filename, "w", newline='', encoding="utf-8") as f:
            writer = csv.writer(f)
            writer.writerow(["chat_id","ID","short_id","timestamp","amount","note","owner","day_key"])
            for r in day_records:
                writer.writerow([chat_id, r.get("id"), r.get("short_id"), r.get("timestamp"),
                                 r.get("amount"), r.get("note"), r.get("owner"), day_key])
    except Exception as e:
        log_error(f"Ошибка экспорта дневного CSV: {e}")
        return None
    return filename

# -----------------------------
# Работа с записями
# -----------------------------
def add_record_to_chat(chat_id, amount, note, owner):
    store = get_chat_store(chat_id)
    rid = data.get("next_id", 1)
    rec = {
        "id": rid,
        "short_id": f"R{rid}",
        "timestamp": datetime.now(TZ).isoformat(timespec="seconds"),
        "amount": amount,
        "note": note,
        "owner": owner
    }
    data.setdefault("records", []).append(rec)
    store.setdefault("records", []).append(rec)
    day = today_key()
    store.setdefault("daily_records", {}).setdefault(day, []).append(rec)
    store["balance"] = store.get("balance", 0) + amount
    data["overall_balance"] = data.get("overall_balance", 0) + amount
    data["next_id"] = rid + 1
    save_data(data)
    try:
        export_to_csv(data)
    except Exception as e:
        log_error(f"Ошибка при экспорте в CSV после добавления: {e}")

    # Форвард новой записи (если включён)
    try:
        if store.get("forward_enabled"):
            target = store.get("forward_target_id")
            if not target and OWNER_ID:
                try:
                    target = int(OWNER_ID)
                except:
                    target = None
            if target:
                try:
                    bot.send_message(
                        int(target),
                        f"💰 {fmt_num(rec['amount'])} ARS\n🆔 {rec['short_id']}\n📝 {rec.get('note','')}\n⏱ {rec.get('timestamp')}"
                    )
                except Exception as e:
                    log_error(f"Не удалось переслать запись (target {target}): {e}")
    except Exception as e:
        log_error(f"Ошибка пересылки записи: {e}")

    return rec

def update_record_in_chat(chat_id, rid, new_amount, new_note, user=None, notify_forward=True):
    store = get_chat_store(chat_id)
    found = None
    for r in store.get("records", []):
        if r["id"] == rid:
            r["amount"] = new_amount
            r["note"] = new_note
            found = r
            break
    if found:
        # обновим во всех днях этого чата
        for day_recs in store.get("daily_records", {}).values():
            for r in day_recs:
                if r["id"] == rid:
                    r.update(found)
        # пересчет балансов по чату и глобально
        store["balance"] = sum([x["amount"] for x in store.get("records", [])])
        data["overall_balance"] = sum([x["amount"] for x in data.get("records", [])])
        save_data(data)
        try:
            export_to_csv(data)
        except:
            pass
        user_repr = f"{user.id}/{getattr(user, 'username', '')}" if user else "unknown"
        log_info(f"Edit by {user_repr}: R{rid} -> amount={new_amount}, note='{new_note}'")

        # форвард-уведомление об изменении (если включён)
        if notify_forward and store.get("forward_enabled"):
            target = store.get("forward_target_id") or (int(OWNER_ID) if OWNER_ID else None)
            if target:
                try:
                    bot.send_message(
                        int(target),
                        f"✏️ Изменена запись R{rid}\nСумма: {fmt_num(new_amount)} ARS\nЗаметка: {new_note}"
                    )
                except Exception as e:
                    log_error(f"Не удалось отправить уведомление об изменении: {e}")
        return True, found
    return False, None

def delete_record_in_chat(chat_id, rid, user=None, notify_forward=True):
    store = get_chat_store(chat_id)
    removed = None
    for r in list(store.get("records", [])):
        if r["id"] == rid or str(r["id"]) == str(rid) or r.get("short_id") == f"R{rid}":
            removed = r
            store["records"].remove(r)
            break
    if removed:
        # убрать из всех дней этого чата
        for day_key, recs in store.get("daily_records", {}).items():
            store["daily_records"][day_key] = [x for x in recs if x["id"] != rid]
        # пересчёт балансов
        store["balance"] = sum([x["amount"] for x in store.get("records", [])])
        data["records"] = [x for x in data.get("records", []) if x["id"] != rid]
        data["overall_balance"] = sum([x["amount"] for x in data.get("records", [])])
        save_data(data)
        try:
            export_to_csv(data)
        except:
            pass
        user_repr = f"{user.id}/{getattr(user, 'username', '')}" if user else "unknown"
        log_info(f"Delete by {user_repr}: R{rid} removed")

        # форвард-уведомление об удалении (если включён)
        if notify_forward and store.get("forward_enabled"):
            target = store.get("forward_target_id") or (int(OWNER_ID) if OWNER_ID else None)
            if target:
                try:
                    bot.send_message(int(target), f"🗑 Удалена запись R{rid}")
                except Exception as e:
                    log_error(f"Не удалось отправить уведомление об удалении: {e}")
        return True, removed
    return False, None

# -----------------------------
# Клавиатуры
# -----------------------------
def kb_nav_row(day_key):
    prev_key = shift_date_key(day_key, -1)
    next_key = shift_date_key(day_key, +1)
    return [
        types.InlineKeyboardButton(f"⬅️ {prev_key}", callback_data=f"d:{prev_key}:open"),
        types.InlineKeyboardButton(f"{next_key} ➡️", callback_data=f"d:{next_key}:open"),
    ]

def build_main_keyboard(day_key, chat_id=None):
    kb = types.InlineKeyboardMarkup(row_width=2)
    kb.row(
        types.InlineKeyboardButton("💰 Баланс", callback_data=f"d:{day_key}:balance"),
        types.InlineKeyboardButton("📊 Отчёт", callback_data=f"d:{day_key}:report"),
        types.InlineKeyboardButton("✏️ Редактировать", callback_data=f"d:{day_key}:edit_menu")
    )
    kb.row(
        types.InlineKeyboardButton("📅 Сегодня", callback_data=f"d:{today_key()}:open"),
        types.InlineKeyboardButton("📆 Выбрать день", callback_data=f"d:{day_key}:pick_date"),
        types.InlineKeyboardButton("ℹ️ Инфо", callback_data=f"d:{day_key}:info")
    )
    # Нижняя строка — баланс
    balance_text = ""
    if chat_id is not None:
        store = get_chat_store(chat_id)
        balance_text = f"💰 {fmt_num(store.get('balance', 0))} ARS"
    kb.add(types.InlineKeyboardButton(balance_text or "💰 0 ARS", callback_data=f"d:{day_key}:noop"))
    # Навигация по дням
    prev_btn, next_btn = kb_nav_row(day_key)
    kb.row(prev_btn, next_btn)
    return kb

def build_edit_menu_keyboard(day_key, chat_id=None):
    kb = types.InlineKeyboardMarkup(row_width=2)
    kb.row(
        types.InlineKeyboardButton("📂 Общий CSV", callback_data=f"d:{day_key}:csv_all"),
        types.InlineKeyboardButton("📅 CSV за день", callback_data=f"d:{day_key}:csv_day")
    )
    kb.row(
        types.InlineKeyboardButton("📨 Форвард", callback_data=f"d:{day_key}:toggle_forward"),
        types.InlineKeyboardButton("👤 Получатель", callback_data=f"d:{day_key}:set_forward_target")
    )
    kb.row(
        types.InlineKeyboardButton("⚙️ Обнулить", callback_data=f"d:{day_key}:reset"),
        types.InlineKeyboardButton("🔙 Назад", callback_data=f"d:{day_key}:back_main")
    )
    # Навигация
    prev_btn, next_btn = kb_nav_row(day_key)
    kb.row(prev_btn, next_btn)
    return kb

def build_edit_keyboard_for_record(day_key, rid):
    kb = types.InlineKeyboardMarkup(row_width=2)
    kb.add(types.InlineKeyboardButton("✏ Ввести вручную", callback_data=f"d:{day_key}:edit_manual:{rid}"))
    kb.add(types.InlineKeyboardButton("🗑 Удалить", callback_data=f"d:{day_key}:edit_delete:{rid}"))
    kb.add(types.InlineKeyboardButton("🔙 Назад", callback_data=f"d:{day_key}:edit_back"))
    return kb

def build_confirm_delete_keyboard(day_key, rid):
    kb = types.InlineKeyboardMarkup(row_width=2)
    kb.row(
        types.InlineKeyboardButton("✅ Да, удалить", callback_data=f"d:{day_key}:confirm_delete:{rid}"),
        types.InlineKeyboardButton("❌ Отмена", callback_data=f"d:{day_key}:cancel_delete")
    )
    return kb

# -----------------------------
# Рендер окна дня (с выбором: редактировать/новое)
# -----------------------------
def render_day_text(store, day_key):
    day_records = store.get("daily_records", {}).get(day_key, [])
    daily_income = sum(r["amount"] for r in day_records if r["amount"] > 0)
    daily_expense = sum(r["amount"] for r in day_records if r["amount"] < 0)
    overall_balance = store.get("balance", 0)

    if not day_records:
        text = (
            f"📅 {day_key}\n"
            f"Пока нет операций в этот день.\n"
            f"💵 Приход: {fmt_num(daily_income)} ARS\n"
            f"💸 Расход: {fmt_num(abs(daily_expense))} ARS\n"
            f"💰 Общий остаток: {fmt_num(overall_balance)} ARS"
        )
    else:
        lines = [f"📅 {day_key}", "📋 Операции:"]
        for r in day_records[-50:]:
            sign = "+" if r["amount"] > 0 else "-"
            lines.append(f"{r['short_id']}: {sign}{fmt_num(abs(r['amount']))} — {r.get('note','')}")
        lines.append(
            f"\n💵 Приход: {fmt_num(daily_income)} ARS\n"
            f"💸 Расход: {fmt_num(abs(daily_expense))} ARS\n"
            f"💰 Общий остаток: {fmt_num(overall_balance)} ARS"
        )
        text = "\n".join(lines)

    # статус форварда
    try:
        if store.get("forward_enabled"):
            tgt = store.get("forward_target_id") or OWNER_ID
            text += f"\n\n📨 Пересылка: Включена → {tgt}"
        else:
            text += f"\n\n📨 Пересылка: Выключена"
    except:
        pass
    return text

def update_or_send_day_window(chat_id, day_key=None, create_if_missing=True, force_new=False, delete_target_old=False, delete_source_old=False, source_day_key=None):
    """
    Рендерит окно дня.
    force_new=True -> всегда создаём новое сообщение (не редактируем).
    delete_target_old=True -> перед созданием удаляем старое окно выбранного дня.
    delete_source_old=True -> удаляем окно исходного дня (из которого пришли).
    """
    day_key = day_key or today_key()
    store = get_chat_store(chat_id)

    # удалить старые окна, если надо
    if delete_target_old:
        delete_active_window_if_exists(chat_id, day_key)
    if delete_source_old and source_day_key and source_day_key != day_key:
        delete_active_window_if_exists(chat_id, source_day_key)

    # обновить "последний открытый день"
    set_current_view_day(chat_id, day_key)

    text = render_day_text(store, day_key)
    kb = build_main_keyboard(day_key, chat_id)
    active_id = get_active_window_id(chat_id, day_key)

    # если не нужно новое — редактируем существующее
    if not force_new and active_id:
        try:
            bot.edit_message_text(text, chat_id, active_id, reply_markup=kb)
        except telebot.apihelper.ApiException as e:
            log_error(f"Не удалось редактировать окно {active_id} для {chat_id} ({day_key}): {e}")
        return

    if not create_if_missing and not force_new:
        return

    # создаём новое сообщение и запоминаем
    try:
        sent = bot.send_message(chat_id, text, reply_markup=kb)
        set_active_window(chat_id, day_key, sent.message_id)
    except Exception as e:
        log_error(f"Ошибка при создании нового окна для {chat_id} ({day_key}): {e}")

# -----------------------------
# Callback обработчик (дата-чувствительный)
# -----------------------------
@bot.callback_query_handler(func=lambda c: True)
def handle_callback(call):
    try:
        chat_id = call.message.chat.id
    except Exception:
        return

    data_str = call.data or ""
    # формат: d:YYYY-MM-DD:action[:arg]
    if not data_str.startswith("d:"):
        update_or_send_day_window(chat_id, today_key())
        bot.answer_callback_query(call.id)
        return

    parts = data_str.split(":")
    if len(parts) < 3:
        bot.answer_callback_query(call.id)
        return

    day_key = parts[1]
    action_tail = parts[2:]
    action = action_tail[0] if action_tail else ""
    store = get_chat_store(chat_id)
    prev_day = get_current_view_day(chat_id)  # исходный день до перехода

    # фикс: запоминаем контекст дня при любом клике
    set_current_view_day(chat_id, day_key)

    # Открыть/Навигация/«Сегодня»
    if action == "open":
        update_or_send_day_window(
            chat_id,
            day_key,
            create_if_missing=True,
            force_new=ALWAYS_CREATE_NEW_ON_NAV,
            delete_target_old=DELETE_TARGET_OLD_WINDOW_ON_NAV,
            delete_source_old=DELETE_SOURCE_WINDOW_ON_NAV,
            source_day_key=prev_day
        )
        bot.answer_callback_query(call.id, f"Открыт день {day_key}")
        return

    # Меню редактирования
    if action == "edit_menu":
        text = (
            "✏️ Меню редактирования\n\n"
            "📂 Общий CSV — полный экспорт\n"
            "📅 CSV за день — экспорт только этого дня\n"
            "📨 Форвард — включить/выключить пересылку\n"
            "👤 Получатель — задать ID для пересылки\n"
            "⚙️ Обнулить — очистить все данные (всего чата)\n"
        )
        # показываем меню в новом окне выбранного дня (согласно новой логике)
        update_or_send_day_window(
            chat_id,
            day_key,
            create_if_missing=True,
            force_new=ALWAYS_CREATE_NEW_ON_NAV,
            delete_target_old=DELETE_TARGET_OLD_WINDOW_ON_NAV,
            delete_source_old=False
        )
        # заменим только что созданное окно текстом меню
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=build_edit_menu_keyboard(day_key, chat_id))
            except telebot.apihelper.ApiException as e:
                log_error(f"Ошибка открытия подменю редактирования: {e}")
        bot.answer_callback_query(call.id)
        return

    # Баланс/Отчёт — редактируем текущее окно (без создания нового)
    if action == "balance":
        day_records = store.get("daily_records", {}).get(day_key, [])
        daily_income = sum(r["amount"] for r in day_records if r["amount"] > 0)
        daily_expense = sum(r["amount"] for r in day_records if r["amount"] < 0)
        overall_balance = store.get("balance", 0)
        text = (
            f"📅 {day_key}\n\n"
            f"💵 Приход за день: {fmt_num(daily_income)} ARS\n"
            f"💸 Расход за день: {fmt_num(abs(daily_expense))} ARS\n"
            f"💰 Общий остаток: {fmt_num(overall_balance)} ARS"
        )
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=build_main_keyboard(day_key, chat_id))
            except telebot.apihelper.ApiException as e:
                log_error(f"Ошибка редактирования окна (Баланс): {e}")
        else:
            update_or_send_day_window(chat_id, day_key, create_if_missing=True)
        bot.answer_callback_query(call.id, "Баланс обновлён ✅")
        return

    if action == "report":
        day_records = store.get("daily_records", {}).get(day_key, [])
        expenses = [r for r in day_records if r["amount"] < 0]
        daily_expense_total = sum(abs(r["amount"]) for r in expenses)
        if not expenses:
            text = f"📅 {day_key}\nПока нет расходов за этот день.\n💸 Расход: 0 ARS"
        else:
            lines = [f"📅 {day_key}", "📋 Расходы за день:"]
            for r in expenses[-30:]:
                lines.append(f"{r['short_id']}: -{fmt_num(abs(r['amount']))} — {r.get('note','')}")
            lines.append(f"\n💸 Расход за день: {fmt_num(daily_expense_total)} ARS")
            text = "\n".join(lines)
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=build_main_keyboard(day_key, chat_id))
            except telebot.apihelper.ApiException as e:
                log_error(f"Ошибка редактирования окна (Отчёт): {e}")
        else:
            update_or_send_day_window(chat_id, day_key, create_if_missing=True)
        bot.answer_callback_query(call.id, "Отчёт обновлён 📊")
        return

    # CSV
    if action == "csv_all":
        try:
            export_to_csv(data)
            with open(CSV_FILE, "rb") as f:
                bot.send_document(chat_id, f, caption="📂 Полный экспорт: data.csv")
            bot.answer_callback_query(call.id, "CSV отправлен 📁")
        except Exception as e:
            send_temp_message(chat_id, f"❌ Ошибка при отправке CSV: {e}")
            bot.answer_callback_query(call.id)
        return

    if action == "csv_day":
        filename = export_day_csv(chat_id, day_key)
        if not filename:
            send_temp_message(chat_id, "❌ Не удалось подготовить CSV за день.")
            bot.answer_callback_query(call.id)
            return
        try:
            with open(filename, "rb") as f:
                bot.send_document(chat_id, f, caption=f"📅 CSV за день: {filename}")
            bot.answer_callback_query(call.id, f"CSV за день {day_key} отправлен")
        except Exception as e:
            send_temp_message(chat_id, f"❌ Ошибка при отправке {filename}: {e}")
            bot.answer_callback_query(call.id)
        return

    # Форвард
    if action == "toggle_forward":
        store["forward_enabled"] = not store.get("forward_enabled", False)
        save_data(data)
        status = "включена ✅" if store["forward_enabled"] else "выключена ❌"
        update_or_send_day_window(chat_id, day_key)  # без new
        bot.answer_callback_query(call.id, f"Пересылка {status}")
        return

    if action == "set_forward_target":
        try:
            bot.answer_callback_query(call.id)
            sent = send_temp_message(chat_id, "Введите ID пользователя (можно отрицательный для чатов) или 0 для сброса:", delay=25)
            if sent:
                bot.register_next_step_handler(sent, lambda m: process_forward_target(m, day_key))
        except Exception as e:
            log_error(f"Ошибка при запросе ID получателя: {e}")
            bot.answer_callback_query(call.id, "Не удалось запросить ID получателя.")
        return

    # Выбор дня кнопкой
    if action == "pick_date":
        bot.answer_callback_query(call.id)
        sent = send_temp_message(chat_id, "🗓 Введите дату в формате YYYY-MM-DD (например, 2025-11-02):", delay=30)
        if sent:
            bot.register_next_step_handler(sent, lambda m: process_pick_date(m))
        return

    # Сброс (через команду /reset)
    if action == "reset":
        # Подсказываем использовать /reset (с подтверждением)
        update_or_send_day_window(chat_id, day_key)
        bot.answer_callback_query(call.id, "Для обнуления используйте команду /reset")
        return

    if action in ("back_main", "edit_back", "noop", "start", "info"):
        update_or_send_day_window(chat_id, day_key)
        bot.answer_callback_query(call.id)
        return

    # Детали записи и редактирование
    if action == "edit_list":
        day_records = store.get("daily_records", {}).get(day_key, [])
        if not day_records:
            text = f"📅 {day_key}\nПока нет операций в этот день."
            update_or_send_day_window(chat_id, day_key)
            bot.answer_callback_query(call.id, "Нет записей для редактирования")
            return

        lines = [f"📅 {day_key}", "Выберите запись для редактирования:"]
        kb = types.InlineKeyboardMarkup(row_width=1)
        for r in reversed(day_records[-20:]):
            sign = "+" if r["amount"] > 0 else "-"
            label = f"{r['short_id']}: {sign}{fmt_num(abs(r['amount']))} — {r.get('note','')}"
            kb.add(types.InlineKeyboardButton(label, callback_data=f"d:{day_key}:rec:{r['id']}"))
        kb.add(types.InlineKeyboardButton("🔙 Назад", callback_data=f"d:{day_key}:back_main"))
        # Рисуем новый экран списка (согласно логике новых окон)
        update_or_send_day_window(
            chat_id, day_key,
            create_if_missing=True, force_new=True,
            delete_target_old=True, delete_source_old=False
        )
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text("\n".join(lines), chat_id, active_id, reply_markup=kb)
            except telebot.apihelper.ApiException as e:
                log_error(f"Ошибка показа списка редактирования: {e}")
        bot.answer_callback_query(call.id)
        return

    if action == "rec" and len(action_tail) >= 2:
        try:
            rid = int(action_tail[1])
        except:
            bot.answer_callback_query(call.id, "Неверный ID")
            return
        day_records = store.get("daily_records", {}).get(day_key, [])
        rec = next((r for r in day_records if r["id"] == rid), None)
        if not rec:
            bot.answer_callback_query(call.id, "Запись не найдена")
            return
        sign = "+" if rec["amount"] > 0 else "-"
        text = f"Запись {rec['short_id']}\n{sign}{fmt_num(abs(rec['amount']))} — {rec.get('note','')}\nДобавлена: {rec.get('timestamp')}"
        kb = build_edit_keyboard_for_record(day_key, rid)
        # рисуем новое окно с деталью записи
        update_or_send_day_window(
            chat_id, day_key,
            create_if_missing=True, force_new=True,
            delete_target_old=True, delete_source_old=False
        )
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(text, chat_id, active_id, reply_markup=kb)
            except telebot.apihelper.ApiException as e:
                log_error(f"Ошибка показа детали записи: {e}")
        bot.answer_callback_query(call.id)
        return

    if action == "edit_manual" and len(action_tail) >= 2:
        try:
            rid = int(action_tail[1])
        except:
            bot.answer_callback_query(call.id, "Неверный ID")
            return
        store["edit_wait"] = f"change_value:{day_key}"
        store["edit_target"] = rid
        save_data(data)
        send_temp_message(chat_id, f"Введите новую сумму и заметку для записи R{rid} в формате:\n+500 Зарплата  или  ^+^500^ Зарплата  или  ^-^150^ Обед", delay=25)
        bot.answer_callback_query(call.id)
        return

    if action == "edit_delete" and len(action_tail) >= 2:
        try:
            rid = int(action_tail[1])
        except:
            bot.answer_callback_query(call.id, "Неверный ID")
            return
        # подтверждение — рисуем новое окно подтверждения
        update_or_send_day_window(
            chat_id, day_key,
            create_if_missing=True, force_new=True,
            delete_target_old=True, delete_source_old=False
        )
        active_id = get_active_window_id(chat_id, day_key)
        if active_id:
            try:
                bot.edit_message_text(f"Подтвердите удаление записи R{rid}:", chat_id, active_id, reply_markup=build_confirm_delete_keyboard(day_key, rid))
            except telebot.apihelper.ApiException as e:
                log_error(f"Ошибка показа подтверждения удаления: {e}")
        bot.answer_callback_query(call.id)
        return

    if action == "confirm_delete" and len(action_tail) >= 2:
        try:
            rid = int(action_tail[1])
        except:
            bot.answer_callback_query(call.id, "Неверный ID")
            return
        success, _ = delete_record_in_chat(chat_id, rid, user=call.from_user, notify_forward=True)
        # после удаления перерисовываем окно дня как новое (чтобы не плодить)
        update_or_send_day_window(
            chat_id, day_key,
            create_if_missing=True, force_new=True,
            delete_target_old=True, delete_source_old=False
        )
        bot.answer_callback_query(call.id, "Запись удалена" if success else "Запись не найдена")
        return

    if action == "cancel_delete":
        update_or_send_day_window(
            chat_id, day_key,
            create_if_missing=True, force_new=True,
            delete_target_old=True, delete_source_old=False
        )
        bot.answer_callback_query(call.id, "Отмена удаления")
        return

    # Нечто иное
    bot.answer_callback_query(call.id)
    return

# -----------------------------
# Выбор дня (ручной ввод после «📆 Выбрать день»)
# -----------------------------
def process_pick_date(message):
    try:
        chat_id = message.chat.id
        text = (message.text or "").strip()
        d = parse_date_key(text)
        if not d:
            send_temp_message(chat_id, "❌ Неверный формат даты. Пример: 2025-11-02", delay=12)
            return
        day_key = d.strftime("%Y-%m-%d")
        prev_day = get_current_view_day(chat_id)
        update_or_send_day_window(
            chat_id,
            day_key,
            create_if_missing=True,
            force_new=True,
            delete_target_old=True,
            delete_source_old=True,
            source_day_key=prev_day
        )
    except Exception as e:
        log_error(f"Ошибка process_pick_date: {e}")
        send_temp_message(message.chat.id, "❌ Ошибка при выборе даты. Попробуйте ещё раз.", delay=10)

# -----------------------------
# Обработка установки получателя форварда (next-step)
# -----------------------------
def process_forward_target(message, day_key_for_refresh=None):
    try:
        chat_id = message.chat.id
        store = get_chat_store(chat_id)
        text = (message.text or "").strip()

        if text == "0":
            store["forward_target_id"] = None
            save_data(data)
            send_temp_message(chat_id, "✅ Получатель пересылки сброшен (будет использоваться OWNER_ID, если он задан).")
            update_or_send_day_window(chat_id, day_key_for_refresh or today_key())
            return

        if not (text.startswith("-") or text.isdigit()):
            send_temp_message(chat_id, "❌ Некорректный ID. Пример: -1001234567890\nПопробуйте снова или введите 0 для сброса.")
            return

        try:
            target = int(text)
        except Exception:
            send_temp_message(chat_id, "❌ Не удалось распознать ID. Попробуйте снова.")
            return

        store["forward_target_id"] = target
        save_data(data)
        send_temp_message(chat_id, f"✅ Теперь пересылка будет идти пользователю с ID {target}.")
        update_or_send_day_window(chat_id, day_key_for_refresh or today_key())

    except Exception as e:
        log_error(f"Ошибка process_forward_target: {e}")
        try:
            send_temp_message(message.chat.id, "❌ Ошибка при установке получателя. Попробуйте ещё раз.")
        except:
            pass

# -----------------------------
# Regex для сумм и парсер
# -----------------------------
num_re = re.compile(r'(\^[+-]\^[0-9]+|\^[+-]?[0-9]+\^|[+-]?\s*\d+)')

def parse_amount_token(token: str):
    t = token.strip().replace("^", "").replace(" ", "")
    if t.startswith("+"): return int(t[1:])
    if t.startswith("-"): return -int(t[1:])
    return -int(t)  # без знака считаем расходом

# -----------------------------
# Команды и обработчики сообщений
# -----------------------------
@bot.message_handler(commands=["start", "info"])
def cmd_start(msg):
    chat_id = msg.chat.id
    # Открываем сегодня как новое окно (чтобы не плодить — удалим старые окна сегодняшнего дня)
    update_or_send_day_window(
        chat_id, today_key(),
        create_if_missing=True, force_new=True,
        delete_target_old=True, delete_source_old=False
    )

@bot.message_handler(commands=["help"])
def cmd_help(msg):
    chat_id = msg.chat.id
    send_temp_message(chat_id,
        "Отправьте сумму в формате +500 Примечание или -150 Обед.\n"
        "Команды: /start /view YYYY-MM-DD /prev /next /balance /report /csv /reset /ping /help",
        delay=15
    )

@bot.message_handler(commands=["ping"])
def cmd_ping(msg):
    chat_id = msg.chat.id
    send_temp_message(chat_id, "PONG — бот живой ✅")

@bot.message_handler(commands=["view"])
def cmd_view(msg):
    chat_id = msg.chat.id
    parts = (msg.text or "").split()
    if len(parts) < 2:
        send_temp_message(chat_id, "Укажите дату: /view YYYY-MM-DD")
        return
    d = parse_date_key(parts[1])
    if not d:
        send_temp_message(chat_id, "Неверный формат даты. Пример: /view 2025-11-02")
        return
    day_key = d.strftime("%Y-%m-%d")
    prev_day = get_current_view_day(chat_id)
    update_or_send_day_window(
        chat_id, day_key,
        create_if_missing=True, force_new=True,
        delete_target_old=True, delete_source_old=True,
        source_day_key=prev_day
    )

@bot.message_handler(commands=["prev"])
def cmd_prev(msg):
    chat_id = msg.chat.id
    current = get_current_view_day(chat_id)
    prev_key = shift_date_key(current, -1)
    update_or_send_day_window(
        chat_id, prev_key,
        create_if_missing=True, force_new=True,
        delete_target_old=True, delete_source_old=True,
        source_day_key=current
    )

@bot.message_handler(commands=["next"])
def cmd_next(msg):
    chat_id = msg.chat.id
    current = get_current_view_day(chat_id)
    next_key = shift_date_key(current, +1)
    update_or_send_day_window(
        chat_id, next_key,
        create_if_missing=True, force_new=True,
        delete_target_old=True, delete_source_old=True,
        source_day_key=current
    )

@bot.message_handler(commands=["balance"])
def cmd_balance(msg):
    chat_id = msg.chat.id
    store = get_chat_store(chat_id)
    dk = today_key()
    day_records = store.get("daily_records", {}).get(dk, [])
    daily_income = sum(r["amount"] for r in day_records if r["amount"] > 0)
    daily_expense = sum(r["amount"] for r in day_records if r["amount"] < 0)
    overall_balance = store.get("balance", 0)
    text = (
        f"📅 {dk}\n\n"
        f"💰 Общий остаток: {fmt_num(overall_balance)} ARS\n"
        f"💵 Приход за сегодня: {fmt_num(daily_income)} ARS\n"
        f"💸 Расход за сегодня: {fmt_num(abs(daily_expense))} ARS"
    )
    bot.send_message(chat_id, text)

@bot.message_handler(commands=["report"])
def cmd_report(msg):
    chat_id = msg.chat.id
    store = get_chat_store(chat_id)
    dk = today_key()
    day_records = store.get("daily_records", {}).get(dk, [])
    expenses = [r for r in day_records if r["amount"] < 0]
    if not expenses:
        bot.send_message(chat_id, f"📅 {dk}\nПока нет расходов за сегодня.\n💸 Расход за день: 0 ARS")
        return
    lines = [f"📅 {dk}", "📋 Расходы за день:"]
    for r in expenses[-30:]:
        lines.append(f"{r['short_id']}: -{fmt_num(abs(r['amount']))} — {r.get('note','')}")
    lines.append(f"\n💸 Расход за день: {fmt_num(sum(abs(r['amount']) for r in expenses))} ARS")
    bot.send_message(chat_id, "\n".join(lines))

@bot.message_handler(commands=["csv"])
def cmd_csv(msg):
    chat_id = msg.chat.id
    try:
        export_to_csv(data)
        with open(CSV_FILE, "rb") as f:
            bot.send_document(chat_id, f, caption="📂 Полный экспорт: data.csv")
    except Exception as e:
        send_temp_message(chat_id, f"❌ Ошибка при отправке CSV: {e}")

@bot.message_handler(commands=["reset"])
def cmd_reset(msg):
    chat_id = msg.chat.id
    kb = types.InlineKeyboardMarkup(row_width=2)
    dk = today_key()
    kb.row(
        types.InlineKeyboardButton("✅ Да, обнулить", callback_data=f"d:{dk}:confirm_reset_all"),
        types.InlineKeyboardButton("❌ Отмена", callback_data=f"d:{dk}:cancel_reset_all")
    )
    send_temp_message(chat_id, "⚠️ Вы уверены, что хотите обнулить все данные этого чата? Это действие нельзя будет отменить.", reply_markup=kb, delay=25)

# Поддержка confirm/cancel полного сброса
@bot.callback_query_handler(func=lambda c: c.data and c.data.startswith(f"d:{today_key()}:confirm_reset_all"))
def _confirm_reset_all(call):
    chat_id = call.message.chat.id
    store = get_chat_store(chat_id)
    # Очистка только этого чата
    store["records"] = []
    store["daily_records"] = {}
    store["balance"] = 0
    # Глобальные records почистим от этого владельца (если owner == chat_id)
    data["records"] = [x for x in data.get("records", []) if x.get("owner") != chat_id]
    try:
        data["overall_balance"] = sum([x["amount"] for x in data.get("records", [])])
    except:
        data["overall_balance"] = 0
    save_data(data)
    update_or_send_day_window(
        chat_id, today_key(),
        create_if_missing=True, force_new=True,
        delete_target_old=True, delete_source_old=False
    )
    bot.answer_callback_query(call.id, "Данные сброшены 🧹")

@bot.callback_query_handler(func=lambda c: c.data and c.data.startswith(f"d:{today_key()}:cancel_reset_all"))
def _cancel_reset_all(call):
    chat_id = call.message.chat.id
    update_or_send_day_window(
        chat_id, today_key(),
        create_if_missing=True, force_new=True,
        delete_target_old=True, delete_source_old=False
    )
    bot.answer_callback_query(call.id, "Отмена")

# Основной обработчик текстовых сообщений (добавление и ручное редактирование)
@bot.message_handler(func=lambda m: True)
def handle_message(msg):
    chat_id = msg.chat.id
    store = get_chat_store(chat_id)
    wait_action = store.get("edit_wait")

    # Ручное изменение записи: "change_value:YYYY-MM-DD"
    if wait_action and wait_action.startswith("change_value"):
        day_key = today_key()
        if ":" in wait_action:
            _, day_key = wait_action.split(":", 1)

        rid = store.get("edit_target")
        if not rid:
            store["edit_wait"] = None
            store["edit_target"] = None
            save_data(data)
            return
        parts = (msg.text or "").strip().split(" ", 1)
        try:
            amount_token = parts[0]
            amount = parse_amount_token(amount_token)
            note = parts[1] if len(parts) > 1 else ""
            success, _ = update_record_in_chat(chat_id, rid, amount, note, user=msg.from_user, notify_forward=True)
            store["edit_wait"] = None
            store["edit_target"] = None
            save_data(data)
            # Перерисуем окно как НОВОЕ и удалим предыдущее окно этого дня
            update_or_send_day_window(
                chat_id, day_key,
                create_if_missing=True, force_new=True,
                delete_target_old=True, delete_source_old=False
            )
            send_temp_message(chat_id, f"✅ Запись R{rid} {'изменена' if success else 'не найдена'}")
        except Exception as e:
            log_error(f"Ошибка при изменении записи вручную: {e}")
            send_temp_message(chat_id, "Ошибка при вводе суммы. Введите в формате +500 Зарплата или ^+^500^ Зарплата или ^-^150^ Обед", delay=15)
        return

    # Добавление новой записи (всегда в СЕГОДНЯ)
    if not msg.text:
        return
    m = re.search(num_re, msg.text)
    if m:
        try:
            raw = m.group(0)
            raw_clean = raw.replace("^", "").replace(" ", "")
            amount = parse_amount_token(raw_clean)
            note = msg.text.replace(m.group(0), "").strip()
            add_record_to_chat(chat_id, amount, note, msg.from_user.id)
            # Обновим окно сегодняшнего дня как НОВОЕ, удаляя старое
            update_or_send_day_window(
                chat_id, today_key(),
                create_if_missing=True, force_new=True,
                delete_target_old=True, delete_source_old=False
            )
            send_temp_message(chat_id, "✅ Запись добавлена")
        except Exception as e:
            log_error(f"Ошибка авто-добавления записи: {e}")
            send_temp_message(chat_id, "❌ Ошибка при добавлении записи. Проверьте формат.")
        return

    # Подсказка
    send_temp_message(chat_id, "Чтобы добавить запись, отправьте сумму в формате +500 Заметка. Для помощи — /help", delay=10)
    return

# -----------------------------
# Flask webhook
# -----------------------------
@app.route(f"/{TOKEN}", methods=["POST"])
def webhook():
    update = telebot.types.Update.de_json(request.get_data().decode("utf-8"))
    bot.process_new_updates([update])
    return "OK", 200

@app.route("/", methods=["GET"])
def index():
    return f"Финансовый бот «ФО» — версия {VERSION} работает", 200

# -----------------------------
# Keep-alive / self-pинг / notify owner
# -----------------------------
def keep_alive_task():
    while True:
        try:
            log_info("Keep-alive: выполняем пинг self и уведомление владельца (если задан).")
            # self-ping
            try:
                if APP_URL:
                    resp = requests.get(APP_URL, timeout=10)
                    log_info(f"Keep-alive: self-ping {APP_URL} -> {resp.status_code}")
            except Exception as e:
                log_error(f"Keep-alive: ошибка self-ping: {e}")

            # уведомление владельцу
            if KEEP_ALIVE_SEND_TO_OWNER and OWNER_ID:
                try:
                    tgt = int(OWNER_ID)
                    send_temp_message(tgt, f"🟢 Keep-alive: бот версия {VERSION} работает. Время: {datetime.now(TZ).isoformat(timespec='seconds')}")
                    log_info(f"Keep-alive: уведомление отправлено владельцу {tgt}")
                except Exception as e:
                    log_error(f"Keep-alive: не удалось отправить уведомление владельцу: {e}")
        except Exception as e:
            log_error(f"Keep-alive: ошибка основного цикла: {e}")
        time.sleep(max(10, KEEP_ALIVE_INTERVAL_SECONDS))

def start_keep_alive_thread():
    thread = threading.Thread(target=keep_alive_task, daemon=True)
    thread.start()

# -----------------------------
# Установка webhook и планировщик
# -----------------------------
def set_webhook():
    url = f"{APP_URL}/{TOKEN}"
    try:
        bot.remove_webhook()
        time.sleep(0.5)
        bot.set_webhook(url=url)
        log_info(f"Webhook установлен: {url}")
    except Exception as e:
        log_error(f"Не удалось установить webhook: {e}")

def schedule_daily_window_creation():
    def task():
        last_day = today_key()
        while True:
            try:
                time.sleep(60)
                current_day = today_key()
                if current_day != last_day:
                    for chat_id_str in list(data.get("chats", {}).keys()):
                        try:
                            chat_id = int(chat_id_str)
                        except:
                            continue
                        try:
                            # На смене дня — создаём новое окно нового дня и удаляем старое окно нового дня
                            update_or_send_day_window(
                                chat_id, current_day,
                                create_if_missing=True, force_new=True,
                                delete_target_old=True, delete_source_old=False
                            )
                        except Exception as e:
                            log_error(f"Ошибка при создании окна нового дня для {chat_id}: {e}")
                    last_day = current_day
            except Exception as e:
                log_error(f"Ошибка в daily loop: {e}")
                time.sleep(5)
    thread = threading.Thread(target=task, daemon=True)
    thread.start()

# -----------------------------
# Запуск
# -----------------------------
if __name__ == "__main__":
    set_webhook()
    schedule_daily_window_creation()
    start_keep_alive_thread()
    log_info(f"Бот ФО версии {VERSION} запущен")
    try:
        if OWNER_ID:
            bot.send_message(int(OWNER_ID), f"✅ Бот запущен и работает! (версия {VERSION})")
    except Exception as e:
        log_error(f"Не удалось отправить сообщение о запуске: {e}")
    app.run(host="0.0.0.0", port=PORT)