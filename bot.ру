import os
import json
import re
from datetime import datetime
from flask import Flask, request
import telebot

# === НАСТРОЙКИ ===
TOKEN = os.getenv("BOT_TOKEN")
WEBHOOK_URL = "https://fo-1.onrender.com"  # твой Render-домен
bot = telebot.TeleBot(TOKEN)
app = Flask(__name__)

DATA_FILE = "data.json"

# === ЗАГРУЗКА/СОХРАНЕНИЕ ===
def load_data():
    if not os.path.exists(DATA_FILE):
        return {"records": [], "balance": 0}
    with open(DATA_FILE, "r") as f:
        return json.load(f)

def save_data(data):
    with open(DATA_FILE, "w") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)

# === ВСПОМОГАТЕЛЬНЫЕ ===
def parse_number(text):
    """Извлекает первое число из текста (поддерживает . , и пробелы)"""
    match = re.search(r"[-+]?[\d.,\s]+", text)
    if not match:
        return None
    num_str = match.group().replace(" ", "").replace(",", ".")
    try:
        return float(num_str)
    except:
        return None

def format_amount(value):
    return f"{value:,.0f}".replace(",", ".")

def format_records(records):
    grouped = {}
    for rec in records:
        h = rec["time"][:5]
        grouped.setdefault(h, []).append(rec)
    lines = []
    for h, items in grouped.items():
        lines.append(f"\n🕓 {h}")
        for r in items:
            lines.append(f"{r['sign']}{format_amount(r['amount'])} {r['desc']}")
    return "\n".join(lines)

# === ОБРАБОТЧИК СООБЩЕНИЙ ===
@bot.message_handler(func=lambda m: True)
def handle_message(message):
    text = message.text.strip()
    if not text:
        return

    data = load_data()
    now = datetime.now()
    date_str = now.strftime("%Y-%m-%d")
    time_str = now.strftime("%H:%M")

    # Проверяем число
    amount = parse_number(text)
    if amount is None:
        bot.reply_to(message, "Не понял сумму 😅")
        return

    sign = "-" if not text.strip().startswith("+") else "+"
    desc = re.sub(r"[-+]?[\d.,\s]+", "", text).strip()

    # Запись
    record = {"date": date_str, "time": time_str, "amount": amount, "desc": desc, "sign": sign}
    data["records"].append(record)

    # Пересчёт
    total = sum(r["amount"] if r["sign"] == "+" else -r["amount"] for r in data["records"])
    data["balance"] = total
    save_data(data)

    # Формирование отчёта дня
    today_records = [r for r in data["records"] if r["date"] == date_str]
    income = sum(r["amount"] for r in today_records if r["sign"] == "+")
    expense = sum(r["amount"] for r in today_records if r["sign"] == "-")

    text_report = (
        f"📅 {date_str}\n\n"
        f"{format_records(today_records)}\n\n"
        f"📈 Приход дня: {format_amount(income)}\n"
        f"📉 Расход дня: {format_amount(expense)}\n"
        f"💵 Общий остаток: {format_amount(total)}"
    )

    # Кнопки
    markup = telebot.types.InlineKeyboardMarkup()
    markup.add(telebot.types.InlineKeyboardButton("✏️ Редактировать", callback_data="edit_menu"))

    # Удаляем предыдущее сообщение (если было)
    if "last_msg_id" in data:
        try:
            bot.delete_message(message.chat.id, data["last_msg_id"])
        except:
            pass

    sent = bot.send_message(message.chat.id, text_report, reply_markup=markup)
    data["last_msg_id"] = sent.message_id
    save_data(data)

# === МЕНЮ РЕДАКТИРОВАНИЯ ===
@bot.callback_query_handler(func=lambda call: call.data == "edit_menu")
def edit_menu(call):
    markup = telebot.types.InlineKeyboardMarkup()
    markup.add(
        telebot.types.InlineKeyboardButton("🗑 Удалить запись", callback_data="edit_delete"),
        telebot.types.InlineKeyboardButton("🔢 Изменить значение", callback_data="edit_change"),
    )
    markup.add(
        telebot.types.InlineKeyboardButton("♻️ Обнулить всё", callback_data="edit_reset")
    )
    markup.add(
        telebot.types.InlineKeyboardButton("⬅️ Назад", callback_data="cancel")
    )
    bot.edit_message_text("Выберите действие:", call.message.chat.id, call.message.message_id, reply_markup=markup)

# === УДАЛЕНИЕ ===
@bot.callback_query_handler(func=lambda call: call.data == "edit_delete")
def edit_delete(call):
    bot.edit_message_text("Удалить последнюю запись? (y/n)", call.message.chat.id, call.message.message_id)
    bot.register_next_step_handler(call.message, confirm_delete)

def confirm_delete(msg):
    if msg.text.lower() == "y":
        data = load_data()
        if data["records"]:
            data["records"].pop()
            save_data(data)
            bot.reply_to(msg, "✅ Удалено.")
        else:
            bot.reply_to(msg, "Нет записей.")
    else:
        bot.reply_to(msg, "❌ Отменено.")

# === ИЗМЕНЕНИЕ ===
@bot.callback_query_handler(func=lambda call: call.data == "edit_change")
def edit_change(call):
    bot.edit_message_text("Введите новое значение (пример: -500 продукты):", call.message.chat.id, call.message.message_id)
    bot.register_next_step_handler(call.message, handle_message)

# === ОБНУЛЕНИЕ ВСЕХ ДАННЫХ ===
@bot.callback_query_handler(func=lambda call: call.data == "edit_reset")
def edit_reset(call):
    bot.edit_message_text("Обнулить ВСЕ данные? (y/n)", call.message.chat.id, call.message.message_id)
    bot.register_next_step_handler(call.message, confirm_reset)

def confirm_reset(msg):
    if msg.text.lower() == "y":
        save_data({"records": [], "balance": 0})
        bot.reply_to(msg, "✅ Всё обнулено.")
    else:
        bot.reply_to(msg, "❌ Отменено.")

# === ПРОСТОЕ ВОЗВРАЩЕНИЕ ===
@bot.callback_query_handler(func=lambda call: call.data == "cancel")
def cancel_action(call):
    bot.delete_message(call.message.chat.id, call.message.message_id)

# === FLASK ДЛЯ WEBHOOK ===
@app.route("/" + TOKEN, methods=["POST"])
def webhook():
    bot.process_new_updates([telebot.types.Update.de_json(request.stream.read().decode("utf-8"))])
    return "ok", 200

@app.route("/", methods=["GET"])
def index():
    return "Bot is running!", 200

# === ЗАПУСК ===
if __name__ == "__main__":
    bot.remove_webhook()
    bot.set_webhook(url=f"{WEBHOOK_URL}/{TOKEN}")
    app.run(host="0.0.0.0", port=int(os.environ.get("PORT", 5000)))
