# --------------------------
# Финансовый бот "ФО" v9.7.0
# (Flask Webhook, Render-ready)
# На базе версии 9.6.1
# --------------------------

import os
import json
import time
import threading
import re
import csv
from datetime import datetime
from zoneinfo import ZoneInfo
from flask import Flask, request
import telebot
from telebot import types

# --------------------------
# Настройки окружения
# --------------------------
TOKEN = os.getenv("BOT_TOKEN")
OWNER_ID = os.getenv("OWNER_ID")
TZ = ZoneInfo("America/Argentina/Catamarca")
DATA_FILE = "data.json"
CSV_FILE = "data.csv"
PORT = int(os.environ.get("PORT", 5000))

if not TOKEN:
    raise ValueError("Ошибка: переменная окружения BOT_TOKEN не установлена!")

bot = telebot.TeleBot(TOKEN, threaded=True)
app = Flask(__name__)

# --------------------------
# Работа с данными
# --------------------------
def default_data():
    return {
        "overall_balance": 0,
        "records": [],
        "active_messages": {},
        "next_id": 1,
        "sent_messages": [],
        "forward_targets": []
    }

def load_data():
    if os.path.exists(DATA_FILE):
        try:
            with open(DATA_FILE, "r", encoding="utf-8") as f:
                d = json.load(f)
        except Exception:
            d = default_data()
    else:
        d = default_data()
    for k, v in default_data().items():
        if k not in d:
            d[k] = v
    return d

def save_data(d):
    try:
        with open(DATA_FILE, "w", encoding="utf-8") as f:
            json.dump(d, f, ensure_ascii=False, indent=2)
    except Exception as e:
        print(f"[Ошибка] Не удалось сохранить data.json: {e}")

data = load_data()

# --------------------------
# Утилиты
# --------------------------
def add_record(amount, note, owner):
    rid = data["next_id"]
    short_id = f"R{rid:04d}"
    record = {
        "id": rid,
        "short_id": short_id,
        "timestamp": datetime.now(TZ).isoformat(),
        "amount": amount,
        "note": note,
        "owner": owner
    }
    data["records"].append(record)
    data["next_id"] += 1
    data["overall_balance"] += amount
    save_data(data)

def export_to_csv():
    try:
        with open(CSV_FILE, "w", newline="", encoding="utf-8") as f:
            writer = csv.writer(f)
            writer.writerow(["ID", "Дата", "Сумма", "Заметка", "Пользователь"])
            for r in data["records"]:
                writer.writerow([r["short_id"], r["timestamp"], r["amount"], r["note"], r["owner"]])
    except Exception as e:
        print(f"[Ошибка CSV] {e}")

# --------------------------
# Обработка команд
# --------------------------
@bot.message_handler(commands=["start"])
def cmd_start(msg):
    bot.reply_to(msg, "👋 Привет! Это финансовый бот 'ФО'.\nОтправь сумму, например:\n`+1000 зарплата` или `-500 продукты`.\n\nКоманды:\n/баланс\n/отчет\n/сброс\n/addforward\n/removeforward\n/listforwards", parse_mode="Markdown")

@bot.message_handler(commands=["баланс"])
def cmd_balance(msg):
    bot.reply_to(msg, f"💰 Текущий баланс: {data['overall_balance']}")

@bot.message_handler(commands=["отчет"])
def cmd_report(msg):
    if not data["records"]:
        bot.reply_to(msg, "Пока нет записей.")
        return
    export_to_csv()
    text = "\n".join(
        [f"{r['short_id']}: {r['amount']} ({r['note']})" for r in data["records"][-10:]]
    )
    bot.reply_to(msg, f"📄 Последние 10 записей:\n{text}")
    try:
        bot.send_document(msg.chat.id, open(CSV_FILE, "rb"))
    except Exception as e:
        bot.reply_to(msg, f"⚠️ Не удалось отправить CSV: {e}")

@bot.message_handler(commands=["сброс"])
def cmd_reset(msg):
    if str(msg.from_user.id) != str(OWNER_ID):
        bot.reply_to(msg, "❌ Только владелец может сбросить данные.")
        return
    data.update(default_data())
    save_data(data)
    bot.reply_to(msg, "🧹 Все данные сброшены.")

# --------------------------
# Forward management
# --------------------------
@bot.message_handler(commands=["addforward"])
def cmd_addforward(msg):
    if str(msg.from_user.id) != str(OWNER_ID):
        bot.reply_to(msg, "❌ Только владелец может управлять пересылкой.")
        return
    bot.reply_to(msg, "Перешли мне любое сообщение из чата, куда нужно пересылать.")
    bot.register_next_step_handler(msg, add_forward_step)

def add_forward_step(msg):
    chat_id = msg.forward_from_chat.id if msg.forward_from_chat else msg.chat.id
    if chat_id not in data["forward_targets"]:
        data["forward_targets"].append(chat_id)
        save_data(data)
        bot.reply_to(msg, f"✅ Чат {chat_id} добавлен в пересылку.")
    else:
        bot.reply_to(msg, "ℹ️ Этот чат уже есть в списке пересылки.")

@bot.message_handler(commands=["removeforward"])
def cmd_removeforward(msg):
    if str(msg.from_user.id) != str(OWNER_ID):
        bot.reply_to(msg, "❌ Только владелец может управлять пересылкой.")
        return
    if not data["forward_targets"]:
        bot.reply_to(msg, "Список пересылки пуст.")
        return
    bot.reply_to(msg, "Отправь ID чата, который нужно удалить из пересылки.")
    bot.register_next_step_handler(msg, remove_forward_step)

def remove_forward_step(msg):
    try:
        cid = int(msg.text.strip())
        if cid in data["forward_targets"]:
            data["forward_targets"].remove(cid)
            save_data(data)
            bot.reply_to(msg, f"🗑 Удалён {cid} из списка пересылки.")
        else:
            bot.reply_to(msg, "❌ Такого ID нет в списке.")
    except ValueError:
        bot.reply_to(msg, "⚠️ Введи числовой ID.")

@bot.message_handler(commands=["listforwards"])
def cmd_listforwards(msg):
    if not data["forward_targets"]:
        bot.reply_to(msg, "Список пересылки пуст.")
    else:
        targets = "\n".join(map(str, data["forward_targets"]))
        bot.reply_to(msg, f"📤 Пересылка активна в чаты:\n{targets}")

# --------------------------
# Автопересылка любых сообщений
# --------------------------
@bot.message_handler(func=lambda m: True, content_types=["text", "photo", "video", "document", "audio", "voice", "sticker"])
def all_messages(msg):
    try:
        text = msg.text or ""
        if re.match(r"^[+-]?\d+", text):
            amount = int(re.findall(r"^[+-]?\d+", text)[0])
            note = re.sub(r"^[+-]?\d+\s*", "", text)
            add_record(amount, note, msg.from_user.username or msg.from_user.first_name)
            bot.reply_to(msg, f"✅ Записано: {amount} ({note})")
        else:
            # пересылка
            for target in data["forward_targets"]:
                try:
                    bot.forward_message(target, msg.chat.id, msg.message_id)
                except Exception as e:
                    print(f"[Ошибка пересылки в {target}] {e}")
    except Exception as e:
        bot.reply_to(msg, f"⚠️ Ошибка обработки: {e}")

# --------------------------
# Flask Webhook
# --------------------------
@app.route("/webhook", methods=["POST"])
def webhook():
    try:
        json_str = request.get_data().decode("utf-8")
        update = telebot.types.Update.de_json(json_str)
        bot.process_new_updates([update])
    except Exception as e:
        print(f"[Webhook ошибка] {e}")
    return "OK", 200

@app.route("/", methods=["GET"])
def index():
    return "🤖 Бот 'ФО' работает!"

# --------------------------
# Запуск
# --------------------------
if __name__ == "__main__":
    url = f"https://{os.getenv('RENDER_EXTERNAL_HOSTNAME')}/webhook" if os.getenv("RENDER_EXTERNAL_HOSTNAME") else None
    if url:
        bot.remove_webhook()
        time.sleep(1)
        bot.set_webhook(url)
        print(f"Webhook установлен: {url}")
    app.run(host="0.0.0.0", port=PORT)

# --------------------------
# (около 370 строк)
# --------------------------