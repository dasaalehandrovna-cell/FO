# ============================================
# Финансовый бот — версия 9.8.7
# Обновлено: добавлен keep_alive с уведомлением владельцу
# ============================================

import os
import time
import json
import csv
import logging
import threading
import requests
import telebot
from telebot import types
from datetime import datetime

# -----------------------------
# ОТСЕК 1 — Настройки
# -----------------------------
TOKEN = os.getenv("BOT_TOKEN", "ВАШ_ТОКЕН_ЗДЕСЬ")
OWNER_ID = int(os.getenv("OWNER_ID", "123456789"))  # ваш Telegram ID

DATA_FILE = "data.json"
CSV_FILE = "records.csv"

bot = telebot.TeleBot(TOKEN)

# -----------------------------
# ОТСЕК 2 — Функции хранения
# -----------------------------
def load_data():
    if os.path.exists(DATA_FILE):
        with open(DATA_FILE, "r", encoding="utf-8") as f:
            return json.load(f)
    return {}

def save_data(data):
    with open(DATA_FILE, "w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)

store = load_data()

# -----------------------------
# ОТСЕК 3 — Главное меню
# -----------------------------
def main_keyboard():
    kb = types.ReplyKeyboardMarkup(resize_keyboard=True)
    kb.add("💰 Добавить", "📋 Отчёт", "✏️ Редактировать")
    return kb

@bot.message_handler(commands=["start"])
def start(msg):
    bot.send_message(msg.chat.id, "Добро пожаловать в Финансовый Бот 💼", reply_markup=main_keyboard())

# -----------------------------
# ОТСЕК 4 — Добавление расходов
# -----------------------------
@bot.message_handler(func=lambda m: m.text == "💰 Добавить")
def add_entry(msg):
    bot.send_message(msg.chat.id, "Введите сумму и описание (например: 250 Продукты):")
    bot.register_next_step_handler(msg, save_entry)

def save_entry(msg):
    try:
        parts = msg.text.split(maxsplit=1)
        amount = float(parts[0])
        note = parts[1] if len(parts) > 1 else ""
        date = datetime.now().strftime("%Y-%m-%d")
        user_id = str(msg.chat.id)
        if user_id not in store:
            store[user_id] = []
        store[user_id].append({"date": date, "amount": amount, "note": note})
        save_data(store)
        bot.send_message(msg.chat.id, f"✅ Добавлено: {amount} — {note}", reply_markup=main_keyboard())
    except Exception:
        bot.send_message(msg.chat.id, "Ошибка ввода. Попробуйте снова.")

# -----------------------------
# ОТСЕК 5 — Просмотр отчёта
# -----------------------------
@bot.message_handler(func=lambda m: m.text == "📋 Отчёт")
def show_report(msg):
    user_id = str(msg.chat.id)
    records = store.get(user_id, [])
    if not records:
        bot.send_message(msg.chat.id, "Записей пока нет.")
        return
    text = "📊 Ваши расходы:\n\n"
    total = 0
    for r in records[-10:]:
        text += f"• {r['date']} — {r['amount']} ({r['note']})\n"
        total += r["amount"]
    text += f"\n💵 Итого: {total}"
    bot.send_message(msg.chat.id, text)

# -----------------------------
# ОТСЕК 6 — Редактирование
# -----------------------------
@bot.message_handler(func=lambda m: m.text == "✏️ Редактировать")
def edit_menu(msg):
    kb = types.ReplyKeyboardMarkup(resize_keyboard=True)
    kb.add("🧾 CSV", "♻️ Обнулить", "✏️ Изменить", "🔙 Назад")
    bot.send_message(msg.chat.id, "Выберите действие:", reply_markup=kb)

@bot.message_handler(func=lambda m: m.text == "🔙 Назад")
def back_to_main(msg):
    bot.send_message(msg.chat.id, "Возврат в главное меню.", reply_markup=main_keyboard())

@bot.message_handler(func=lambda m: m.text == "🧾 CSV")
def export_csv(msg):
    user_id = str(msg.chat.id)
    records = store.get(user_id, [])
    if not records:
        bot.send_message(msg.chat.id, "Нет данных для экспорта.")
        return
    with open(CSV_FILE, "w", newline="", encoding="utf-8") as f:
        writer = csv.writer(f)
        writer.writerow(["Дата", "Сумма", "Описание"])
        for r in records:
            writer.writerow([r["date"], r["amount"], r["note"]])
    with open(CSV_FILE, "rb") as f:
        bot.send_document(msg.chat.id, f)

@bot.message_handler(func=lambda m: m.text == "♻️ Обнулить")
def reset_data(msg):
    user_id = str(msg.chat.id)
    store[user_id] = []
    save_data(store)
    bot.send_message(msg.chat.id, "Все записи удалены.", reply_markup=main_keyboard())

@bot.message_handler(func=lambda m: m.text == "✏️ Изменить")
def edit_entry(msg):
    user_id = str(msg.chat.id)
    records = store.get(user_id, [])
    if not records:
        bot.send_message(msg.chat.id, "Нет записей для изменения.")
        return
    text = "Выберите номер записи для изменения:\n"
    for i, r in enumerate(records[-10:], 1):
        text += f"{i}) {r['date']} — {r['amount']} ({r['note']})\n"
    bot.send_message(msg.chat.id, text)
    bot.register_next_step_handler(msg, process_edit_choice)

def process_edit_choice(msg):
    user_id = str(msg.chat.id)
    try:
        index = int(msg.text) - 1
        record = store[user_id][index]
        bot.send_message(msg.chat.id, f"Введите новые данные (например: 300 Обновлено):")
        bot.register_next_step_handler(msg, lambda m: update_record(m, index))
    except:
        bot.send_message(msg.chat.id, "Неверный номер.")

def update_record(msg, index):
    user_id = str(msg.chat.id)
    try:
        parts = msg.text.split(maxsplit=1)
        amount = float(parts[0])
        note = parts[1] if len(parts) > 1 else ""
        store[user_id][index].update({"amount": amount, "note": note})
        save_data(store)
        bot.send_message(msg.chat.id, "✅ Запись обновлена.", reply_markup=main_keyboard())
    except:
        bot.send_message(msg.chat.id, "Ошибка обновления.")

# -----------------------------
# ОТСЕК 7 — Keep Alive
# -----------------------------
def keep_alive():
    while True:
        try:
            requests.get("https://your-render-app-url.onrender.com")  # URL вашего бота
            bot.send_message(OWNER_ID, "✅ Бот активен и работает стабильно.")
        except Exception as e:
            logging.warning(f"Ошибка keep_alive: {e}")
        time.sleep(900)  # каждые 15 минут

# Запуск keep_alive в отдельном потоке
threading.Thread(target=keep_alive, daemon=True).start()

# -----------------------------
# ОТСЕК 8 — Запуск бота
# -----------------------------
if __name__ == "__main__":
    bot.infinity_polling(timeout=60, long_polling_timeout=30)