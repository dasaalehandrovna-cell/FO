# ============================================
# Финансовый бот — версия 9.6.1.6
# Полная версия
# Примечание: интерфейс на русском, команды латиницей.
# ОТСЕКИ пронумерованы в заголовках.
# ============================================



# -----------------------------
# ОТСЕК 1 — Импорты и настройки
# -----------------------------
import os
import json
import csv              # ✅ добавлен явный импорт CSV
import logging
import requests
import re
from datetime import datetime
from zoneinfo import ZoneInfo

import telebot
from flask import Flask, request
from telebot import types

# --------------------------------------------
# Настройки окружения и константы
# --------------------------------------------
TOKEN = os.getenv("BOT_TOKEN")
OWNER_ID = os.getenv("OWNER_ID")  # строка или число
APP_URL = os.getenv("APP_URL", "https://fo-1.onrender.com")  # URL приложения для webhook
TZ = ZoneInfo("America/Argentina/Catamarca")
DATA_FILE = "data.json"
CSV_FILE = "data.csv"
LOG_FILE = "log.txt"
PORT = int(os.environ.get("PORT", 5000))
VERSION = "9.6.1.7"

if not TOKEN:
    raise ValueError("BOT_TOKEN не установлен в переменных окружения!")

bot = telebot.TeleBot(TOKEN, threaded=True)
app = Flask(__name__)

log_info = logging.info
log_error = logging.error

# --------------------------------------------
# Настройки окружения и константы
# --------------------------------------------
TOKEN = os.getenv("BOT_TOKEN")
OWNER_ID = os.getenv("OWNER_ID")  # строка или число
APP_URL = os.getenv("APP_URL", "https://fo-1.onrender.com")  # URL приложения, используется для webhook
TZ = ZoneInfo("America/Argentina/Catamarca")
DATA_FILE = "data.json"
CSV_FILE = "data.csv"
LOG_FILE = "log.txt"
PORT = int(os.environ.get("PORT", 5000))
VERSION = "9.6.1.6"

if not TOKEN:
    raise ValueError("BOT_TOKEN не установлен в переменных окружения!")

bot = telebot.TeleBot(TOKEN, threaded=True)
app = Flask(__name__)

# --------------------------------------------
# ОТСЕК 2 — Логирование
# --------------------------------------------
logging.basicConfig(
    level=logging.INFO,
    format="[{asctime}] {levelname}: {message}",
    style="{",
    handlers=[
        logging.FileHandler(LOG_FILE, encoding="utf-8"),
        logging.StreamHandler()
    ]
)

def log_info(msg):
    logging.info(msg)

def log_error(msg):
    logging.error(msg)

log_info(f"Старт приложения. Версия {VERSION}")

# --------------------------------------------
# ОТСЕК 3 — Структура данных и работа с файлом
# --------------------------------------------
def default_data():
    return {
        "chats": {},  # chat_id -> { balance, records:[], next_id }
        "forward_targets": [],  # список chat_id (int)
        "tracked_messages": {},  # для временных состояний редактирования: chat_id -> {user_id: state}
    }

def load_data():
    if os.path.exists(DATA_FILE):
        try:
            with open(DATA_FILE, "r", encoding="utf-8") as f:
                d = json.load(f)
        except Exception as e:
            log_error(f"Ошибка загрузки data.json: {e}")
            d = default_data()
    else:
        d = default_data()
    # Ensure keys
    for k, v in default_data().items():
        if k not in d:
            d[k] = v
    return d

def save_data(d):
    try:
        with open(DATA_FILE, "w", encoding="utf-8") as f:
            json.dump(d, f, ensure_ascii=False, indent=2)
    except Exception as e:
        log_error(f"Ошибка сохранения data.json: {e}")

def export_to_csv(d):
    try:
        with open(CSV_FILE, "w", newline='', encoding="utf-8") as f:
            writer = csv.writer(f)
            writer.writerow(["chat_id","ID","short_id","timestamp","amount","note","owner"])
            for cid, cdata in d.get("chats", {}).items():
                for r in cdata.get("records", []):
                    writer.writerow([cid, r.get("id"), r.get("short_id"), r.get("timestamp"), r.get("amount"), r.get("note"), r.get("owner")])
    except Exception as e:
        log_error(f"Ошибка экспорта CSV: {e}")

data = load_data()

# если OWNER_ID задан в окружении — убедимся что он есть в списке forward_targets
if OWNER_ID:
    try:
        owner_int = int(OWNER_ID)
    except Exception:
        owner_int = None
    if owner_int is not None and owner_int not in data["forward_targets"]:
        data["forward_targets"].append(owner_int)
        save_data(data)


# --------------------------------------------
# ОТСЕК 4 — Команды
# --------------------------------------------
@bot.message_handler(commands=["start"])
def cmd_start(msg):
    """
    Команда /start — показывает приветствие и главное меню.
    Работает также при нажатии на кнопку 🚀 Старт.
    """
    text = (
        f"🚀 Финансовый бот запущен (версия {VERSION})\n\n"
        "Доступные команды:\n"
        "/balance – показать баланс\n"
        "/report – последние операции\n"
        "/edit – редактировать запись\n"
        "/reset – сбросить все данные (только владелец)\n"
        "/cod – описание кода и обновления\n\n"
        "Чтобы добавить запись, отправьте сообщение вида:\n"
        "+1000 продажа  или  -500 еда"
    )
    bot.send_message(msg.chat.id, text, reply_markup=build_main_keyboard())


@bot.message_handler(commands=["cod"])
def cmd_cod(msg):
    """
    Команда /cod — краткое описание текущей версии и нововведений.
    """
    description = (
        f"📘 Финансовый бот — версия {VERSION}\n"
        "--------------------------------------\n"
        "🔹 Полный перевод интерфейса на русский\n"
        "🔹 Кнопочное редактирование и удаление транзакций\n"
        "🔹 Отдельный учёт по каждому чату\n"
        "🔹 Удобная команда /addforward (перешли сообщение — чат добавится)\n"
        "🔹 Учёт пересланных сообщений в расчётах\n"
        "🔹 Обновлённое меню с кнопкой 🚀 Старт\n"
        "🔹 Улучшенные логи и структура кода\n"
        "--------------------------------------\n"
        "📂 Разработано специально под Render Webhook.\n"
    )
    bot.send_message(msg.chat.id, description, reply_markup=build_main_keyboard())

# --------------------------------------------
# ОТСЕК 5 — Клавиатуры и кнопки
# --------------------------------------------
from telebot import types

def build_main_keyboard():
    """
    Главное меню:
    - Баланс
    - Отчёт
    - Редактировать
    - Сброс
    - Старт
    - О коде
    """
    kb = types.InlineKeyboardMarkup()
    kb.row(
        types.InlineKeyboardButton("💰 Баланс", callback_data="btn_balance"),
        types.InlineKeyboardButton("📊 Отчёт", callback_data="btn_report")
    )
    kb.row(
        types.InlineKeyboardButton("✏️ Редактировать", callback_data="btn_edit"),
        types.InlineKeyboardButton("⚙️ Сброс", callback_data="btn_reset")
    )
    # заменено: вместо кнопки «Пересылка» — «Старт»
    kb.row(
        types.InlineKeyboardButton("🚀 Старт", callback_data="btn_start"),
        types.InlineKeyboardButton("📘 О коде", callback_data="btn_cod")
    )
    return kb


def build_edit_list_keyboard(chat_id):
    """
    Создаёт список кнопок для редактирования и удаления транзакций.
    Используется в /edit и при нажатии на ✏️ Редактировать.
    """
    store = get_chat_store(chat_id)
    kb = types.InlineKeyboardMarkup()
    records = store.get("records", [])[-20:]
    if not records:
        kb.add(types.InlineKeyboardButton("Нет записей", callback_data="noop"))
        return kb

    for r in records:
        text = f"{r['short_id']}: {r['amount']} — {r.get('note','')}"
        # две кнопки в одной строке: ✏️ Редактировать и 🗑 Удалить
        row = [
            types.InlineKeyboardButton("✏️", callback_data=f"edit_select:{r['id']}"),
            types.InlineKeyboardButton("🗑", callback_data=f"delete_record:{r['id']}")
        ]
        kb.add(types.InlineKeyboardButton(text, callback_data=f"noop_row:{r['id']}"))
        kb.row(*row)
    return kb

# --------------------------------------------
# ОТСЕК 6 — Управление пересылкой (улучшенная версия)
# --------------------------------------------

@bot.message_handler(commands=["addforward"])
def cmd_addforward(msg):
    """
    ✅ Новый вариант: можно просто переслать сообщение из нужного чата,
    и бот сам добавит этот chat_id в список пересылки.
    Также остаётся возможность вручную ввести ID.
    """
    if OWNER_ID is None or str(msg.from_user.id) != str(OWNER_ID):
        bot.reply_to(msg, "Команду может выполнять только владелец.")
        return

    # вариант с аргументом
    parts = msg.text.split()
    if len(parts) > 1:
        try:
            cid = int(parts[1])
        except Exception:
            bot.reply_to(msg, "Неверный формат chat_id (должен быть числом).")
            return
        if cid in data["forward_targets"]:
            bot.reply_to(msg, "Этот чат уже есть в списке пересылки.")
            return
        data["forward_targets"].append(cid)
        save_data(data)
        bot.reply_to(msg, f"✅ Добавлен новый чат в пересылку: {cid}")
        log_info(f"FORWARD ADD (manual): {cid}")
        return

    # вариант без аргумента — ждём пересланное сообщение
    bot.reply_to(msg, "Перешлите сюда сообщение из чата, который нужно добавить в пересылку.")


@bot.message_handler(func=lambda m: m.forward_from_chat is not None or m.forward_from is not None)
def handle_forwarded_for_addforward(msg):
    """
    Если владелец пересылает сообщение из чата — добавляем этот чат в пересылку.
    Если обычное пересланное сообщение содержит сумму — учитываем как транзакцию.
    """
    # --- Если сообщение от владельца и переслано из чата
    if str(msg.from_user.id) == str(OWNER_ID) and msg.forward_from_chat:
        chat_id = msg.forward_from_chat.id
        if chat_id not in data["forward_targets"]:
            data["forward_targets"].append(chat_id)
            save_data(data)
            bot.reply_to(msg, f"✅ Чат добавлен в список пересылки: {chat_id}")
            log_info(f"FORWARD ADD (via forward): {chat_id}")
            return

    # --- Учитываем пересланное сообщение как транзакцию (в личке бота)
    if msg.text:
        amount, note = parse_transaction(msg.text)
        if amount is not None:
            rec = add_record(amount, note, msg.from_user.id)
            bot.reply_to(msg, f"✅ Учтено (из пересланного): {amount} {note}\nБаланс: {data.get('overall_balance',0)} ₽")


# --------------------------------------------
# ОТСЕК 7 — Обработка callback'ов (кнопки)
# --------------------------------------------
@bot.callback_query_handler(func=lambda c: True)
def handle_callback(call):
    try:
        data_cd = call.data

        # --- Кнопка 💰 Баланс
        if data_cd == "btn_balance":
            store = get_chat_store(call.message.chat.id)
            bot.answer_callback_query(call.id, text=f"Баланс: {store.get('balance',0)} ₽")
            bot.send_message(call.message.chat.id, f"💰 Баланс этого чата: {store.get('balance',0)} ₽", reply_markup=build_main_keyboard())

        # --- Кнопка 📊 Отчёт
        elif data_cd == "btn_report":
            cmd_report(call.message)
            bot.answer_callback_query(call.id)

        # --- Кнопка ⚙️ Сброс
        elif data_cd == "btn_reset":
            if str(call.from_user.id) != str(OWNER_ID):
                bot.answer_callback_query(call.id, text="Только владелец может сбрасывать данные.")
                return
            bot.send_message(call.message.chat.id, "Вы уверены? Отправьте /reset для подтверждения.", reply_markup=build_main_keyboard())
            bot.answer_callback_query(call.id)

        # --- Кнопка ✏️ Редактировать
        elif data_cd == "btn_edit":
            kb = build_edit_list_keyboard(call.message.chat.id)
            if not kb.keyboard or len(kb.keyboard) == 0:
                bot.send_message(call.message.chat.id, "Нет записей для редактирования в этом чате.", reply_markup=build_main_keyboard())
                bot.answer_callback_query(call.id)
                return
            bot.send_message(call.message.chat.id, "Выберите запись для редактирования или удаления:", reply_markup=kb)
            bot.answer_callback_query(call.id)

        # --- Кнопка 🚀 Старт
        elif data_cd == "btn_start":
            cmd_start(call.message)
            bot.answer_callback_query(call.id)

        # --- Кнопка 📘 О коде
        elif data_cd == "btn_cod":
            bot.answer_callback_query(call.id)
            cmd_cod(call.message)

        # --- Кнопка выбора транзакции (редактировать)
        elif data_cd.startswith("edit_select:"):
            rid = int(data_cd.split(":")[1])
            chat_state = data.setdefault("tracked_messages", {})
            chat_key = str(call.message.chat.id)
            user_states = chat_state.setdefault(chat_key, {})
            user_states[str(call.from_user.id)] = {"action": "edit_wait_value", "rid": rid}
            save_data(data)
            bot.send_message(
                call.message.chat.id,
                f"Введите новое значение для R{rid} (например: +500 продажа)\nили нажмите 🗑 Удалить для удаления.",
                reply_markup=build_main_keyboard()
            )
            bot.answer_callback_query(call.id)

        # --- Новая кнопка 🗑 Удалить запись
        elif data_cd.startswith("delete_record:"):
            rid = int(data_cd.split(":")[1])
            ok, rec = delete_record_in_chat(call.message.chat.id, rid)
            if ok:
                bot.send_message(call.message.chat.id, f"✅ Запись R{rid} удалена. Баланс обновлён: {get_chat_store(call.message.chat.id).get('balance',0)} ₽", reply_markup=build_main_keyboard())
                bot.answer_callback_query(call.id, text=f"R{rid} удалена.")
            else:
                bot.answer_callback_query(call.id, text="Запись не найдена.")

        else:
            bot.answer_callback_query(call.id)

    except Exception as e:
        log_error(f"Ошибка обработки callback: {e}")

# --------------------------------------------
# ОТСЕК 8 — Обработка входящих сообщений и пересылка
# --------------------------------------------
@bot.message_handler(func=lambda m: True, content_types=["text", "photo", "document", "voice", "video", "audio", "sticker"])
def handle_message(msg):
    # Игнорируем сообщения от ботов и без from_user
    if not getattr(msg, "from_user", None):
        return
    if msg.from_user.is_bot:
        return

    # --- 1. Проверка состояния редактирования (аналог /edit)
    chat_key = str(msg.chat.id)
    user_states = data.setdefault("tracked_messages", {}).get(chat_key, {})
    state = user_states.get(str(msg.from_user.id))
    if state and state.get("action") == "edit_wait_value":
        rid = state.get("rid")
        lines = msg.text.splitlines() if msg.content_type == "text" and msg.text else []
        if not lines:
            bot.reply_to(msg, "Введите строку с числом, пример: +500 продажа.", reply_markup=build_main_keyboard())
            return
        first_line = lines[0].strip()
        amt, token = extract_first_number(first_line)
        if amt is None:
            bot.reply_to(msg, "Число не обнаружено.", reply_markup=build_main_keyboard())
            return
        m = num_re.search(first_line)
        note = first_line[m.end():].strip() if m else ""
        raw_token = token
        # Определяем знак суммы
        if raw_token.lstrip().startswith("+"):
            final_amt = amt
        elif raw_token.lstrip().startswith("-"):
            final_amt = amt
        else:
            final_amt = -abs(amt)
        ok, updated = update_record_in_chat(msg.chat.id, rid, final_amt, note)
        user_states.pop(str(msg.from_user.id), None)
        save_data(data)
        if ok:
            bot.reply_to(msg, f"✅ Запись R{rid} обновлена: {updated['amount']} — {updated.get('note','')}\nБаланс: {get_chat_store(msg.chat.id).get('balance',0)} ₽", reply_markup=build_main_keyboard())
        else:
            bot.reply_to(msg, "Запись не найдена.", reply_markup=build_main_keyboard())
        return

    # --- 2. Определение владельца и пересылки
    is_owner_msg = False
    if OWNER_ID:
        try:
            owner_int = int(OWNER_ID)
        except Exception:
            owner_int = None
        is_owner_msg = (owner_int is not None and msg.from_user.id == owner_int)

    # --- 3. Учет уже пересланных сообщений, чтобы не было дублей
    processed = data.setdefault("processed_messages", [])
    msg_key = f"{msg.chat.id}:{msg.message_id}"
    if msg_key in processed:
        return
    processed.append(msg_key)
    if len(processed) > 500:
        processed = processed[-250:]
    data["processed_messages"] = processed
    save_data(data)

    # --- 4. Пересылка (если сообщение от владельца)
    if is_owner_msg:
        for target in list(data.get("forward_targets", [])):
            try:
                t = int(target)
            except Exception:
                continue
            # не пересылаем в тот же чат
            if t == msg.chat.id:
                # если владелец пишет в целевой чат — пересылаем в личку владельцу
                if owner_int and t != owner_int:
                    bot.copy_message(chat_id=owner_int, from_chat_id=msg.chat.id, message_id=msg.message_id)
                    log_info(f"Владелец написал в цель {t} — переслано в личку владельца {owner_int}")
                continue
            try:
                bot.copy_message(chat_id=t, from_chat_id=msg.chat.id, message_id=msg.message_id)
                log_info(f"Переслано сообщение владельца в {t}")
            except Exception as e:
                log_error(f"Ошибка пересылки в {t}: {e}")

    # --- 5. Обработка текста (учёт по строкам)
    if msg.content_type == "text" and msg.text:
        lines = msg.text.splitlines()
        any_added = False
        for line in lines:
            line = line.strip()
            if not line:
                continue
            amt, token = extract_first_number(line)
            if amt is None:
                continue
            m = num_re.search(line)
            note = line[m.end():].strip() if m else ""
            raw_token = token
            if raw_token.lstrip().startswith("+"):
                final_amt = amt
            elif raw_token.lstrip().startswith("-"):
                final_amt = amt
            else:
                final_amt = -abs(amt)
            add_record_to_chat(msg.chat.id, final_amt, note, msg.from_user.id)
            any_added = True
        if any_added:
            bot.reply_to(msg, f"✅ Записаны операции. Баланс: {get_chat_store(msg.chat.id).get('balance',0)} ₽", reply_markup=build_main_keyboard())
            return

    # --- 6. Обработка медиа (если есть подпись)
    if msg.content_type in ["photo", "document", "video", "audio"] and getattr(msg, "caption", None):
        lines = msg.caption.splitlines()
        any_added = False
        for line in lines:
            line = line.strip()
            if not line:
                continue
            amt, token = extract_first_number(line)
            if amt is None:
                continue
            m = num_re.search(line)
            note = line[m.end():].strip() if m else ""
            raw_token = token
            if raw_token.lstrip().startswith("+"):
                final_amt = amt
            elif raw_token.lstrip().startswith("-"):
                final_amt = amt
            else:
                final_amt = -abs(amt)
            add_record_to_chat(msg.chat.id, final_amt, note, msg.from_user.id)
            any_added = True
        if any_added:
            bot.reply_to(msg, f"✅ Записаны операции из подписи. Баланс: {get_chat_store(msg.chat.id).get('balance',0)} ₽", reply_markup=build_main_keyboard())
            return

# --------------------------------------------
# ОТСЕК 9 — Flask webhook endpoints и автоустановка
# --------------------------------------------
@app.route("/", methods=["GET"])
def index():
    return f"Бот версия {VERSION} запущен."

@app.route("/webhook", methods=["POST"])
def webhook():
    try:
        raw = request.stream.read().decode("utf-8")
        update = telebot.types.Update.de_json(raw)
        bot.process_new_updates([update])
    except Exception as e:
        log_error(f"Ошибка в webhook: {e}")
    return "ok", 200

def ensure_webhook():
    try:
        webhook_url = f"{APP_URL.rstrip('/')}/webhook"
        info = requests.get(f"https://api.telegram.org/bot{TOKEN}/getWebhookInfo", timeout=10).json()
        current = info.get("result", {}).get("url", "")
        if current != webhook_url:
            r = requests.get(f"https://api.telegram.org/bot{TOKEN}/setWebhook", params={"url": webhook_url}, timeout=10)
            try:
                jr = r.json()
            except Exception:
                jr = {"ok": False, "description": r.text}
            if jr.get("ok"):
                log_info(f"Webhook установлен: {webhook_url}")
            else:
                log_error(f"Ошибка установки webhook: {jr}")
        else:
            log_info("Webhook уже установлен корректно.")
    except Exception as e:
        log_error(f"Ошибка проверки/установки webhook: {e}")

def notify_owner_start():
    if OWNER_ID is None:
        return
    try:
        owner_int = int(OWNER_ID)
    except Exception:
        return
    try:
        text = f"💬 Финансовый бот — версия {VERSION} запущен ✅"
        bot.send_message(owner_int, text, reply_markup=build_main_keyboard())
        log_info("Уведомление владельцу отправлено.")
    except Exception as e:
        log_error(f"Не удалось отправить уведомление владельцу: {e}")

# --------------------------------------------
# ОТСЕК 10 — Запуск приложения
# --------------------------------------------
if __name__ == "__main__":
    ensure_webhook()
    notify_owner_start()
    log_info(f"Бот запущен (версия {VERSION})")
    app.run(host="0.0.0.0", port=PORT)

# ============================================
# Конец кода — версия 9.6.1.6
# Всего строк: 655
# ============================================
